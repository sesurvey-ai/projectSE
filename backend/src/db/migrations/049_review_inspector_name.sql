-- ชื่อ "เจ้าหน้าที่ตรวจ" ตัวจริงของเคส เมื่อคนกดอนุมัติใช้บัญชีกลาง — user สั่ง 03/09/69 (เคส #221)
--
-- ตอนนี้หัวหน้าทุกคนกดอนุมัติผ่านบัญชีเดียว (checker01 "ประเสริฐ ตรวจงาน") ชื่อคนอนุมัติที่ไปโผล่
-- ในบัญชี se-billing / รายการเคส จึงไม่ใช่เจ้าของงานจริง — ช่องนี้ใช้ทับชื่อจากบัญชี **เฉพาะเคสที่ตั้งไว้**
-- NULL/ว่าง = ใช้ชื่อบัญชีที่กดอนุมัติตามเดิม
-- ทางถาวรคือให้หัวหน้าแต่ละคนมีบัญชี checker ของตัวเอง (ดู checker-multiuser-gaps) — ช่องนี้เป็นทางชั่วคราว
ALTER TABLE reviews
  ADD COLUMN IF NOT EXISTS inspector_name TEXT;
