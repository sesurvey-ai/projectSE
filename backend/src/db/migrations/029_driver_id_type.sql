-- ชนิดบัตรผู้ขับขี่รถประกัน: 'thai' = บัตรประชาชน 13 หลัก (ตรวจ checksum ในแอป)
--                          'foreign' = บัตรต่างด้าว/หนังสือเดินทาง (พิมพ์อิสระ ไม่เกิน 13 ตัว)
-- ว่าง/NULL = งานเก่าที่บันทึกก่อนมีตัวเลือกนี้ → แอปถือว่าเป็นคนไทยตามพฤติกรรมเดิม
--
-- คู่กรณี/ผู้บาดเจ็บไม่ต้องมีคอลัมน์ — เก็บ key 'id_type' ไปกับ JSONB ของแต่ละ record อยู่แล้ว
--
-- ⚠️ ต้องรันด้วยมือบน production (ไม่มีตัวรันอัตโนมัติ)
ALTER TABLE survey_reports ADD COLUMN IF NOT EXISTS driver_id_type VARCHAR(10);
