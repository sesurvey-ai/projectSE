-- "ซ่อมที่" — ชื่ออู่/ศูนย์บริการที่นำรถประกันเข้าซ่อม
--
-- ผู้สำรวจถามลูกค้าที่หน้างานว่าจะเอารถเข้าที่ไหน แล้วจดไว้ในช่องหมายเหตุมาตลอด
-- (ค้นย้อนหลังไม่ได้ ผู้ตรวจต้องไล่อ่านเอง) → แยกเป็นคอลัมน์ของตัวเอง
--
-- VARCHAR(200) เท่า survey_company/assured_name — ชื่ออู่บางที่ยาว (มีสาขา/บริษัทเต็ม)
-- ว่าง/NULL = ยังไม่ระบุ (งานเก่าก่อนมีช่องนี้ หรือรถยังไม่เข้าซ่อม) ไม่ใช่ค่าผิด
--
-- ⚠️ ต้องรันด้วยมือบน production (ไม่มีตัวรันอัตโนมัติ)
ALTER TABLE survey_reports ADD COLUMN IF NOT EXISTS repair_shop VARCHAR(200);
