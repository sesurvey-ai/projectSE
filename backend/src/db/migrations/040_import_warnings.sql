-- เก็บ "เรื่องที่ต้องเติมก่อนอนุมัติ" ที่ตรวจพบตอนนำเข้าเคส
--
-- เดิมคำเตือนพวกนี้ (ไม่มีประเภทรถ · ไม่มีรายการความเสียหาย · อ่านจังหวัดไม่ออก ฯลฯ)
-- ถูกส่งกลับให้หน้าจอที่กดนำเข้า **ครั้งเดียวแล้วหายไป** — ใครมาเปิดรายการงานทีหลัง
-- ไม่มีทางรู้เลยว่าเคสไหนข้อมูลไม่ครบ ต้องเปิดเข้าไปทีละเคสถึงจะเห็นดอกจันแดง
-- เก็บไว้เพื่อให้หน้ารายการงานคัดได้ว่าเคสไหนหยิบมาทำได้เลย เคสไหนต้องไปตามข้อมูลก่อน
--
-- รันด้วยมือบน production:
--   psql "$DATABASE_URL" -f backend/src/db/migrations/040_import_warnings.sql

ALTER TABLE cases ADD COLUMN IF NOT EXISTS import_warnings JSONB;

COMMENT ON COLUMN cases.import_warnings IS
  'อาเรย์ข้อความเตือนตอนนำเข้า (ISURVEY/XML) — null = เคสที่ไม่ได้มาจากการนำเข้า';
