# Dependencies
node_modules/

# Next.js
.next/
out/

# Build
dist/
build/

# Environment
.env
.env.local
.env.*.local

# Uploads
backend/src/uploads/*
!backend/src/uploads/.gitkeep

# Avatar photos (รูปพนักงานจริง — เก็บนอก git เหมือน uploads, ของจริงเสิร์ฟจาก storage/DB)
web/public/avatars/*
!web/public/avatars/.gitkeep

# Flutter
mobile/build/
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/.packages
mobile/android/.gradle/
mobile/ios/Pods/
mobile/ios/.symlinks/

# IDE
.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db

# Firebase credentials
**/google-services.json
**/GoogleService-Info.plist
**/firebase-service-account.json

# Claude Code local config
.claude/

# Quarantined throwaway scripts (ของทิ้งที่ย้ายออกจาก working tree — ลบโฟลเดอร์นี้ได้เมื่อยืนยันว่าไม่ต้องการ)
.trash-scripts/
cloudVision.json

# การ์ด/ใบรับแจ้งของลูกค้าจริง (ชื่อ เบอร์ ทะเบียน เลขกรมธรรม์ เลขตัวถัง)
# ใช้เป็นตัวอย่างตอนพัฒนาตัวอ่าน OCR เท่านั้น — ห้ามขึ้น git
aioicard/

# รายงานงานจริงที่ user ดึงมาให้วิเคราะห์ (เลขเคลม + สถานที่เกิดเหตุของลูกค้าจริง หลายหมื่นรายการ)
# ใช้ตอบคำถามเรื่องกติกาเลขเซอร์เวย์เท่านั้น — ห้ามขึ้น git
สถานที่*.xlsx
