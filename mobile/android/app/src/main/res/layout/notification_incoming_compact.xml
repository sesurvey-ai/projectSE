<?xml version="1.0" encoding="utf-8"?>
<!-- แบบย่อ (collapsed + heads-up): หัวข้อ + ปุ่มรับงานเต็มปุ่ม ไม่ใส่รายละเอียด (heads-up สูงจำกัด) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/notification_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="15sp"
            android:textColor="#000000"
            android:textStyle="bold"
            android:singleLine="true"
            android:ellipsize="end" />

        <!-- ปุ่มปิด/เปิดเสียง — วางไอคอนไว้ 2 ตัวแล้วสลับ "ซ่อน/แสดง"
             ⛔ ห้ามใช้ setImageViewResource สลับรูป: บนเครื่องจริง (Samsung) มันไม่อัปเดต
                vector ใน RemoteViews เลย ตรรกะทำงานถูกแต่ไอคอนค้างที่เดิม (เจอ 01/09/69)
                setViewVisibility ทำงานแน่นอน -->
        <ImageView
            android:id="@+id/btn_mute"
            android:layout_width="34dp"
            android:layout_height="30dp"
            android:padding="6dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_bell"
            android:contentDescription="ปิดเสียงแจ้งเตือน"
            android:background="@drawable/btn_mute_notif_bg" />

        <ImageView
            android:id="@+id/btn_mute_off"
            android:layout_width="34dp"
            android:layout_height="30dp"
            android:padding="6dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_bell_off"
            android:contentDescription="เปิดเสียงแจ้งเตือน"
            android:background="@drawable/btn_mute_notif_bg"
            android:visibility="gone" />
    </LinearLayout>

    <TextView
        android:id="@+id/btn_accept"
        android:layout_width="match_parent"
        android:layout_height="46dp"
        android:gravity="center"
        android:text="รับงาน"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:background="@drawable/btn_accept_notif" />

</LinearLayout>
