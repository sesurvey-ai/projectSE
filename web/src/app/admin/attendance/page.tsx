// เวลาเข้า–ออกงาน (admin) — ตารางใช้ component ร่วมกับฝั่ง callcenter
import AttendanceTable from '@/components/attendance/AttendanceTable';

export default function AdminAttendancePage() {
  return <AttendanceTable />;
}
