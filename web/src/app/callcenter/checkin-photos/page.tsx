// รูปยืนยันลงเวลา (callcenter) — photo wall รอบเช็คอิน + เวลา (component แชร์กับฝั่ง admin)
import PhotoWall from './PhotoWall';

export default function CallcenterCheckinPhotosPage() {
  return <PhotoWall />;
}
