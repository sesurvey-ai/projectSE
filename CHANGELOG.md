# บันทึกการเปลี่ยนแปลง — SE Survey

ระบบสำรวจภัยรถยนต์: แอปมือถือ (Flutter) · เว็บ (Next.js) · backend (Node/TS + PostgreSQL)
เชื่อมกับ **EMCS** ผ่านบอท `se-autokey` และรองรับงานเก่าจากระบบ **ISURVEY**

ไล่จากใหม่ → เก่า · วันที่เป็น พ.ศ. ตามที่ใช้ในระบบ

---

## สิงหาคม 2569

### แก้ — หน้าคิวงานตรวจค้าง ไม่รู้ว่าคนอื่นทำอะไรไปแล้ว
หน้ารายการงานตรวจโหลดครั้งเดียวตอนเปิด ใครเปิดค้างไว้ก็เห็นภาพของเวลานั้นตลอดไป —
เปิดเรื่องที่หัวหน้าอีกคนตรวจจบไปแล้ว หรือไม่เห็นงานใหม่ที่เพิ่งส่งเข้ามาจนกว่าจะบังเอิญกด F5
ตอนมีหัวหน้าตรวจคนเดียวยังไม่เป็นไร แต่กำลังจะเพิ่มเป็นหลายคน

ตอนนี้คิวอัปเดตเองทันทีเมื่อมีอะไรเปลี่ยน — ช่างส่งงานเข้ามา · มีคนกดบันทึก/อนุมัติ/ตีกลับ ·
แอดมินปลดล็อก · บอทรายงานว่าเข้าระบบประกันแล้ว · มีเวลา "อัปเดตเมื่อ" กับปุ่มอัปเดตเองให้ด้วย
(ถ้าสัญญาณขาด ยังมีการโหลดซ้ำทุก 1 นาทีเป็นตาข่ายรอง — เฉพาะตอนเปิดแท็บนั้นอยู่จริง)

**หน้ารายละเอียดเคสเตือนแต่เนิ่น ๆ ด้วย** — มีคนอื่นบันทึกเคสที่คุณเปิดค้างอยู่เมื่อไหร่
ขึ้นแถบบอกทันที ไม่ต้องรอไปเจอตอนกดบันทึกแล้วโดนปฏิเสธหลังพิมพ์ไปครึ่งชั่วโมง
· หน้านี้**ไม่โหลดใหม่ให้เอง**โดยตั้งใจ เพราะสิ่งที่พิมพ์ค้างไว้จะหาย — คนกดเอง

### เพิ่ม — เปลี่ยนรหัสผ่านเองได้ · แอดมินตั้งรหัสใหม่ให้คนอื่นได้จากหน้ารายชื่อ
เดิม**ไม่มีทางเปลี่ยนรหัสผ่านของตัวเองเลย** ทุกคนต้องใช้รหัสที่แอดมินตั้งให้ตลอดไป
และรหัสนั้นก็มักถูกบอกต่อกันทางแชท ยิ่งกำลังจะแจกบัญชีหัวหน้าตรวจเพิ่มอีกหลายคน
ยิ่งกลายเป็นรหัสที่คนรู้กันทั้งออฟฟิศ

- ปุ่ม **"เปลี่ยนรหัสผ่าน"** มุมขวาบนของทุกหน้า (ใช้ได้ทั้งแอดมิน · รับแจ้ง · ผู้ตรวจสอบ)
  ต้องกรอกรหัสเดิมยืนยัน — จอที่เปิดค้างไว้ไม่ได้แปลว่าเป็นเจ้าของบัญชี
- แอดมินตั้งรหัสใหม่ให้คนอื่นได้จาก**ปุ่มในตารางรายชื่อผู้ใช้**เลย (เดิมทำได้เหมือนกัน
  แต่ซ่อนอยู่ในหน้าแก้ไขผู้ใช้ ต้องกดเข้าไปแล้วเลื่อนหา) · ช่องนี้ไม่ปิดดาว
  เพราะแอดมินต้องอ่านรหัสไปบอกเจ้าตัว
- **รหัสใหม่ต้องอย่างน้อย 8 ตัว ห้ามเป็นชื่อผู้ใช้ ห้ามเป็นคำที่เดาง่าย** (เดิม 6 ตัว
  ไม่มีกฎอื่นเลย) ใช้กติกาเดียวกันทุกทาง รวมถึงตอนแอดมินสร้างผู้ใช้ใหม่
  · ไม่บังคับตัวใหญ่/อักขระพิเศษ โดยตั้งใจ — คนใช้คือช่างที่พิมพ์บนมือถือกลางแดด
  กฎแบบนั้นได้รหัสที่จำยากแต่เดาง่าย แล้วลงเอยด้วยการจดใส่กระดาษ
- ⚠️ **บัญชีเดิมที่รหัสอ่อนอยู่แล้วยังเข้าได้ตามปกติ** กติกามีผลตอนตั้งรหัสใหม่เท่านั้น
- ⚠️ เปลี่ยนรหัสแล้ว **เครื่องอื่นที่ล็อกอินค้างไว้ยังใช้ได้จนกว่าจะออกจากระบบเอง**
  (กล่องเปลี่ยนรหัสบอกเรื่องนี้ไว้ตรง ๆ)
- ⚠️ **แอปมือถือยังไม่มีปุ่มนี้** ช่างสำรวจยังต้องให้แอดมินตั้งรหัสให้ — รอ APK รุ่นถัดไป

### แก้ — หัวหน้า 2 คนบันทึกเคสเดียวกัน งานของคนแรกหายเงียบ ๆ
หน้าตรวจเคสส่ง**ทุกช่อง**ไปพร้อมกันตอนกดบันทึก ไม่ใช่เฉพาะช่องที่แก้ พอสองคนเปิดเคสเดียวกัน
คนที่กดบันทึกทีหลังจึงทับงานของคนแรกทั้งหน้าด้วยค่าเก่าที่ค้างอยู่ในจอตัวเอง —
และทั้งคู่เห็นข้อความ "บันทึกสำเร็จ" เหมือนกัน ไม่มีใครรู้ว่าข้อมูลหายไป
ตามย้อนหลังก็ไม่ได้เพราะไม่มีบันทึกว่าใครแก้อะไร

ตอนนี้ถ้ามีคนบันทึกคั่นระหว่างที่เราเปิดหน้าค้างไว้ ระบบจะ**ไม่เขียนอะไรเลยสักช่อง**
แล้วขึ้นแถบแดงบอกว่าใครบันทึกไปเมื่อไหร่ พร้อมปุ่ม "โหลดข้อมูลล่าสุด" (ถามก่อนเสมอ
เพราะสิ่งที่พิมพ์ค้างไว้จะหาย) · ครอบคลุมทั้งช่องข้อมูลและตารางยอดเงิน

เดิมไม่เคยเจอเพราะมีหัวหน้าตรวจอยู่คนเดียว — จะเริ่มเจอทันทีที่เปิดให้หลายคนใช้พร้อมกัน

⚠️ ต้องรัน `042_report_rev.sql` บน production **ก่อน** deploy โค้ดชุดนี้

### แก้ — หน้าตรวจเตือนกฎ 24 ชั่วโมงของเคลมสดแล้ว
ระบบประกันบังคับว่าเคลมสดต้องมี 5 จังหวะเวลา (เกิดเหตุ · ลูกค้าแจ้ง · ประกันแจ้งสำรวจ ·
ถึงที่เกิดเหตุ · สำรวจเสร็จ) อยู่ในกรอบ 24 ชั่วโมง เกินแล้ว **ปัดตกทั้งหน้า**
เวลาที่แอปบันทึกคือเวลาจริงในสนาม แต่กว่าหัวหน้าจะตรวจและอนุมัติอาจข้ามวัน — เดิมไม่มีอะไรเตือน
รู้อีกทีตอนบอทยิงเข้าระบบประกันแล้วเด้งกลับ ต้องมาไล่แก้ทีหลัง
ตอนนี้ขึ้นเตือนที่ช่องวันที่เกิดเหตุตั้งแต่ตอนตรวจ

### แก้ — ตัวเลือกท้ายรายการในแอปถูกแถบปุ่มของเครื่องบัง
กล่องเลือก (บริษัทประกัน ยี่ห้อรถ จังหวัด ฯลฯ) ตัวเลือกสุดท้ายไปนอนอยู่ใต้แถบปุ่มของมือถือ
เลื่อนขึ้นไม่ได้ กดไม่โดน — โดนกับ "บริษัท ไอโออิ กรุงเทพ ประกันภัย" ซึ่งเป็นตัวสุดท้ายของ
รายการประกันคู่กรณีพอดี (1 ใน 2 บริษัทที่ทำงานจริง) เติมระยะเผื่อด้านล่างให้แล้ว

### แก้ — แอปไม่บังคับ "การเรียกร้องค่าเสียหายจากคู่กรณี" เมื่อรถประกันเป็นฝ่ายผิด
ระบบประกันบังคับติ๊กอย่างน้อย 1 ข้อ **ทุกงานที่มีคู่กรณี ไม่ได้ผูกกับผลคดี**
แต่แอปบังคับเฉพาะตอน "คู่กรณีผิด" — งานที่รถประกันเป็นฝ่ายผิดจึงส่งมาโดยไม่มีค่านี้
แล้วหัวหน้าต้องมานั่งเติมเองทุกใบตอนตรวจ ตอนนี้บังคับตั้งแต่บนแอปเมื่อมีคู่กรณี

### แก้ — ล็อกช่อง "ราคาพนักงาน" ของค่ารูปถ่าย
พนักงาน**ไม่มีค่ารูป** ค่ารูปเบิกได้เฉพาะฝั่งเรียกเก็บบริษัทประกัน แต่เดิมหน้าตรวจ
เปิดช่องนี้ให้กรอกได้ ใครใส่เลขลงไปก็บวกเข้ายอดจ่ายพนักงานทันที
ตอนนี้ล็อกไว้ (ชี้เมาส์แล้วบอกเหตุผล) และยอดเก่าที่เคยกรอกผิดจะถูกล้างตอนบันทึกครั้งถัดไป

### เพิ่ม — รู้แล้วว่างานไหน "ส่งถึงบริษัทประกันจริง" ไม่ใช่แค่ "นำเข้าแล้ว"
บอทไม่กดปุ่มส่งงานเอง (หัวหน้ากด) ระบบเราจึงไม่เคยรู้ว่าใบไหนถึงมือประกันแล้ว
ป้ายค้างที่ "นำเข้า EMCS แล้ว" ตลอดไป ต้องเปิดระบบประกันดูทีละใบ
ช่องเก็บข้อมูลกับทางรับมีอยู่แล้วตั้งแต่แรก แต่ไม่เคยมีอะไรมาป้อน — ต่อท่อให้ครบแล้ว
สั่งกวาดอ่านสถานะทุกใบที่ค้างได้ในรอบเดียว (อ่านอย่างเดียว ไม่เปิดเรื่อง จึงไม่ล็อกงานคนอื่น)

### แก้บั๊ก — ที่อยู่ยาวเกิน ทำให้ทั้งเคสเข้าระบบประกันไม่ได้
ระบบประกันจำกัดความยาวรายช่อง เกินแม้แต่ช่องเดียว **ตีกลับทั้งไฟล์** ไม่ใช่ตัดเฉพาะช่องนั้น
(กล่อง "กรุณาตรวจสอบ! ข้อมูลนำเข้ามีขนาดเกิน") เจอกับสถานที่เกิดเหตุที่อ่านมาจาก Google Maps
`"101 หมู่ที่ 8 Khlong Luang Rd, ... Pathum Thani 12120, Thailand"` ยาว 110 ตัว เกินโควตา 100
- ตัดให้พอดีโควตาก่อนส่ง (เฉพาะ**ช่องข้อความ** — ช่องตัวเลข/เงิน/รหัสไม่แตะ เพราะตัดแล้วค่าเพี้ยน)
- และขึ้นเตือนบนหน้าตรวจว่าช่องไหนยาวเกินเท่าไร ให้ย่อเองก่อนจะได้ไม่เสียข้อมูลท้ายประโยค
- โควตาทุกช่องอ่านจากฟอร์มจริงของระบบประกัน ไม่ได้เดา

### เพิ่ม — แก้ "ความเสียหายรถคู่กรณี" บนเว็บได้แล้ว
เดิมหัวการ์ดคู่กรณีขึ้นแค่ป้าย "ความเสียหาย N รายการ (แก้ในแอป)" — หัวหน้าตรวจไม่ได้ว่า
ช่างเลือกถูกหรือครบไหม ต้องไปเปิดแอปดู ทั้งที่ระบบประกันมีช่องรับของคู่กรณีแยกทุกคัน
ตอนนี้เป็นปุ่ม "ข้อมูลความเสียหาย (N)" กดแล้วเปิดหน้าต่างเดียวกับรถประกัน
(checklist 22 ชิ้น + ช่องอิสระ 30 ช่อง) แก้/เพิ่ม/ลบได้ครบ

### แก้บั๊ก — หน้าต่าง "ข้อมูลความเสียหาย" 3 อาการ
- **ติ๊กชิ้นส่วนแล้วระบบเลือกระดับความเสียหายให้เอง** (ติ๊ก "กันชนหน้า" แล้ว L ติดมาเลย)
  ทุกชิ้นจึงกลายเป็น "เสียหายน้อย ทั้งสองด้าน" ทั้งที่ไม่มีใครสั่ง → ตอนนี้ปล่อยว่าง
  ให้ผู้ตรวจสอบเลือกเอง และขึ้นป้ายเตือนที่หัวหน้าต่างว่ายังไม่ได้เลือกระดับกี่ชิ้น
- **ช่องอิสระ: พิมพ์ชื่อชิ้นส่วนแล้วด้าน "A" ถูกเลือกให้อัตโนมัติ** — เอาออกเช่นกัน
- 🔴 **พิมพ์ในช่องอิสระคอลัมน์ซ้าย แล้วตัวอักษรไปโผล่คอลัมน์ขวา**
  เพราะช่องอิสระถูกคำนวณใหม่ทุกครั้งที่พิมพ์โดยตัดช่องว่างทิ้ง ลำดับช่องเลยขยับ
  (ตารางเรียง 2 คอลัมน์สลับซ้าย-ขวาตามลำดับ) → เก็บเป็น 30 ช่องคงที่ ไม่ขยับอีก

### เพิ่ม — เว็บเตือนเลขบัตรประชาชนผิดของคู่กรณี/ผู้บาดเจ็บแล้ว
ระบบประกันตรวจหลักตรวจสอบของเลขบัตรเอง เจอเลขมั่วแล้ว **ไม่ยอมบันทึกทั้งบล็อกผู้บาดเจ็บ**
แอปมือถือกันไว้ตั้งแต่หน้ากรอกอยู่แล้ว แต่เว็บรับทุกอย่างที่พิมพ์ลงไป → หัวหน้าแก้เลขบนเว็บ
แล้วงานไปตายตอนบอทกดบันทึก ตอนนี้ช่องขึ้นแดงพร้อมเหตุผลทันทีที่เลขไม่ผ่าน

### เพิ่ม — เตือนตั้งแต่หน้าตรวจ เมื่อ "สำรวจภัยเสร็จ" ล้ำเวลาปัจจุบัน
ระบบประกันปัดตกทั้งหน้าพร้อมข้อความ "ต้องไม่เกินวันที่ ณ ปัจจุบัน" และเทียบถึง**ระดับนาที**
(ทดสอบสดแล้ว: ล้ำไป 45 นาทีก็ตก ล้ำไป 2 นาทีก็ตก) เดิมเว็บเราไม่ตรวจข้อนี้เลย
→ อนุมัติผ่าน แล้วไปตายตอนบอทกดบันทึก เสียเที่ยวทั้งรอบ
ตอนนี้ขึ้นเป็น "ผิดลำดับ" ที่การ์ดลำดับเวลา และกดอนุมัติไม่ได้จนกว่าจะแก้

### แก้บั๊ก — ยี่ห้อรถคู่กรณีบนเว็บเลือกไม่ได้เลย
ช่อง "ยี่ห้อ" ของคู่กรณีมีให้เลือกแค่ "-- ระบุ --" ทุกคัน ทั้งที่ของรถประกันมีครบทุกยี่ห้อ
เพราะรายชื่อยี่ห้อแยกตามประเภทรถด้วย**รหัส** (A/T/V...) แต่คู่กรณีเก็บประเภทรถเป็น
**ป้ายไทย** ("เก๋งเอเชีย") → หาไม่เจอ เลยได้ลิสต์ว่าง (แอปมือถือแปลงให้อยู่แล้ว เว็บไม่ได้แปลง)
→ หัวหน้าต้องปล่อยว่าง ยี่ห้อรถคู่กรณีไปไม่ถึงระบบประกัน

เจอจากการทดสอบสดตั้งแต่ callcenter → มือถือ → เว็บ → EMCS (19/08/69)

### แก้บั๊ก — ช่องติ๊กยอดเงินที่บันทึกไว้ไม่ขึ้นบนหน้าจอ
"นอกพื้นที่ · นอกเวลา · ตำบลพิเศษ · ส่งช้า · เอกสารไม่ครบ" ที่บันทึกไว้แล้ว **ไม่ขึ้นติ๊ก**
เพราะยอดเงินโหลดมาทีหลัง แล้วช่องติ๊กไม่รับค่าใหม่ (อาการเดียวกับ "ค่าคัดประจำวัน")
→ กดบันทึกทีเดียวล้างเป็นไม่ติ๊กทั้งหมด **ยอดจ่ายพนักงานหายไป 50-100 บาท** และเหตุผลหักเงินหาย

เจอจากการทดสอบสด — ตัวหักล้างในรอบไล่ตรวจตีตกข้อนี้ทิ้งไปก่อนหน้า (บอกว่าไม่ใช่บั๊ก)

### แก้บั๊ก — 2 ข้อสุดท้ายจากการไล่ตรวจ (ครบทุกข้อที่ยืนยันแล้ว)
- **เวลา "บ.ประกันแจ้งสำรวจภัย" ที่หัวหน้าแก้ ไปถึงไฟล์ส่งประกันแล้ว** — เดิมโดนคอลัมน์เวลาเก่า
  (เขียนตอนสร้างเคสจากการอ่านบัตร แล้วไม่มีที่ไหนแก้ได้อีก) ทับเสมอ → ไฟล์ได้วันที่ใหม่
  แต่เวลาเก่า ผิดเงียบ ๆ ทั้งที่เป็นเวลาที่ระบบประกันใช้วัดลำดับ
- **"-- ระบุ --" / "-- เขต --" เลิกกลายเป็นข้อมูลจริงในตารางคู่กรณี/ผู้บาดเจ็บ/ทรัพย์สิน**
  เดิม dropdown มี 2 บรรทัดหน้าตาเหมือนกันเป๊ะ ตัวล่างมีค่าเป็นข้อความจริง เลือกแล้วเก็บลงฐานข้อมูล
  → ตัวนับช่องบังคับคิดว่า "กรอกแล้ว" ป้ายเตือนหาย และรหัสจังหวัดในไฟล์ส่งประกันว่าง
  แก้ 2 ชั้น: ตัดออกจากลิสต์ตัวเลือก + ตัวล้างฝั่งเซิร์ฟเวอร์เดินลงในตารางย่อยได้แล้ว
  (เดิมล้างเฉพาะช่องเดี่ยว ไม่เคยแตะข้อมูลในตาราง)

### แก้บั๊ก — อีก 6 ข้อจากการไล่ตรวจ (ชุดที่ 2)

**เงิน**
- **ยอดที่ระบบแนะนำเป็นเรทฐานล้วนแล้ว** — เดิมแนะนำ "ยอดรวมทั้งงาน" แต่ไปแปะข้างช่อง
  ค่าบริการ พอหัวหน้าพิมพ์ตามที่แนะนำ ระบบก็บวกนอกพื้นที่/นอกเวลาเข้าไปอีกรอบ
  ใบเบิกเงินเกินจริง 50-150 บาทต่องาน

**ข้อมูลหาย**
- **ค่าที่ไม่มีในตัวเลือกไม่หายอีกแล้ว** (สีรถ · จังหวัด · ประเภทใบขับขี่ · สาเหตุการเกิดเหตุ)
  เดิมถ้าค่าที่บันทึกไว้ไม่ตรงตัวเลือกไหนเลย ช่องจะเด้งไปที่ "-- ระบุ --" เอง
  แล้วกดบันทึกทีเดียวข้อมูลเดิมหายถาวร ไม่มีอะไรเตือน (สีรถเจอบ่อยเพราะ OCR ส่งข้อความอิสระมา)

**ประตูอนุมัติ**
- **ช่องบังคับในตารางคู่กรณี/ผู้บาดเจ็บ/ทรัพย์สิน บล็อกการอนุมัติแล้ว** — เดิมการ์ดขึ้น
  "⚠ ยังขาด N ช่องบังคับ" แต่แถบบนขึ้นเขียว "ครบแล้ว" **หน้าจอขัดกันเอง** อนุมัติผ่านไปได้
  แล้วบอทไปตายที่หน้าคู่กรณีของระบบประกัน ต้องให้แอดมินปลดล็อกทำใหม่
- **บังคับให้มีรายการความเสียหายอย่างน้อย 1 ชิ้น** — เดิมดอกจันไปผูกกับกล่องข้อความ
  ที่ **ไม่เคยถูกส่งเข้าระบบประกันเลย** พิมพ์อะไรลงไปก็ปลดล็อกได้ ทั้งที่รายการยังว่าง
- **ตรวจลำดับเวลาครบ 7 คู่** (เดิม 4) และ **เวลาเท่ากันเป๊ะก็ผิด** ตามกติกาจริงของระบบประกัน
  ตัวอย่างที่เคยหลุด: ลูกค้าแจ้ง 14:20 แต่ประกันแจ้งสำรวจ 13:50 — เว็บเงียบ ระบบประกันเด้ง

**ไฟล์ส่งประกัน**
- **เลิกส่งชื่อสาขาไทยลงช่องรหัสสาขา** — ระบบประกันต้องการตัวเลข ส่งชื่อไทยไปจะนำเข้า
  ไม่ได้ทั้งไฟล์ · กระทบเฉพาะทางดาวน์โหลดไฟล์ไปนำเข้าเอง (ทางบอทไม่กระทบ บอทใส่รหัสจริงให้อยู่แล้ว)

### แก้บั๊ก — 7 ข้อจากการไล่ตรวจหน้าตรวจเคสทั้งหน้า
ใช้เอเจนต์ 13 ตัวไล่อ่านโค้ด 6 มุมพร้อมกัน แล้วให้อีกชุดพยายามหักล้างก่อนสรุป
เจอ 30 ข้อ ผ่านด่านหักล้าง 22 ข้อ · รอบนี้แก้ 7 ข้อแรกที่กระทบงานประจำวันที่สุด

**งานที่พิมพ์ค้างไว้ไม่หายอีกแล้ว**
- **อัปโหลดรูปแล้วงานหายทั้งหน้า** — เดิมทุกครั้งที่โหลดข้อมูลใหม่ (หลังบันทึก · หลังอัปรูป ·
  หลังตีกลับ) หน้าจะถูกถอดออกจากจอชั่วขณะ ทุกช่องกลับไปเป็นค่าที่บันทึกไว้
  ตอนนี้โชว์ "กำลังโหลด" เฉพาะครั้งแรกที่เปิดเคสเท่านั้น
- **สลับ "ครั้งที่" ไปดูครั้งก่อนแล้วกลับมา ค่าที่พิมพ์ไว้ยังอยู่ครบ** — เดิมหายทั้งราง
  ทั้งที่การเทียบยอดกับครั้งก่อนคือเหตุผลเดียวที่ฟีเจอร์นี้มี
- **"ค่าคัดประจำวัน" โชว์ค่าที่บันทึกไว้แล้ว** — เดิมค้างที่ "— ไม่มี —" ตลอด แล้วกดบันทึก
  ทีเดียวล้างของเดิมทิ้ง

**เวลาที่ส่งเข้าระบบประกัน**
- **พิมพ์นาทีหลักเดียว (16:5) ไม่ทำให้เวลากลายเป็นเที่ยงคืนอีกแล้ว** — เดิมระบบประกัน
  ได้ 00:00:00 ซึ่งย้อนหลังกว่าเวลาเกิดเหตุ = ตีกลับ ทั้งที่หน้าเว็บไม่ฟ้องอะไรเลย
  ตอนนี้เติมศูนย์ให้ทั้งตอนออกจากช่องและก่อนบันทึกลงฐานข้อมูล
- **ลบวันที่ในการ์ด "ลำดับเวลา" ได้จริงแล้ว** — เดิมตอบว่าบันทึกสำเร็จแต่ค่าเก่าไม่ถูกลบ
  และยังไหลเข้าระบบประกันต่อ

**ดอกจันที่ไม่เคยบังคับจริง**
- **"ฝ่ายประมาท" · "เพศผู้ขับขี่"** มีดอกจันแดงแต่ไม่เคยถูกนับ (เป็นปุ่มตัวเลือก ซึ่งตัวนับ
  มองไม่เห็น) → ว่างก็ขึ้น "ช่องบังคับครบแล้ว" อนุมัติผ่าน แล้วไปตกที่ระบบประกัน
  ฝ่ายประมาทว่างยังลามให้อีก 2 ช่องหลุดจากประตูอนุมัติด้วย
- **"ยี่ห้อรถ"** เป็นช่องบังคับของระบบประกันแต่ไม่มีดอกจัน — ว่างแล้วบอทหยุดรอคนไปเลือกเอง

### แก้บั๊ก — ชิ้นส่วนที่พิมพ์เองหายจากรายการความเสียหาย
ชิ้นส่วนที่ไม่มีในรายการสำเร็จรูป 22 ชิ้น (พิมพ์เองจากช่องอิสระ หรือมาจากแอป) เคยแสดงเป็น
**ช่องว่าง** ในหมวด "ความเสียหายรถประกัน" เพราะรายการตัวเลือกมีแค่ 22 ชื่อนั้น
ข้อมูลไม่ได้หาย (บันทึกถูกต้อง) แต่พอผู้ตรวจแตะแถวนั้นทีเดียวชื่อจะหายจริง

ตอนนี้แสดงชื่อที่พิมพ์ไว้พร้อมกำกับว่า **(ช่องอิสระ)** และ**เลือกด้านซ้าย/ขวาได้**
เหมือนช่องอิสระของระบบประกัน — เดิมถูกบังคับเป็น "ทั้งคู่" ทั้งที่ข้อมูลจริงเป็นซ้าย/ขวา

### เพิ่ม — สลับดู "ครั้งที่" ได้ในหน้าเดียว + ยอดสะสมทั้งเคลม
ตรวจของจริงกับเคลม 2026013144978 แล้วพบว่า **ระบบประกันเก็บทุกครั้งไว้ในเรื่องเดียว**
(เรื่อง S68426063383 มี 3 ครั้ง เลือกด้วย dropdown บนหน้าค่าใช้จ่าย) ส่วนระบบเก่ากับระบบเรา
เก็บเป็น **คนละเคส คนละเลขเรื่องเซอร์เวย์** — และเลขใบแจ้งหนี้ของแต่ละครั้งบนระบบประกัน
**คือเลขเรื่องเซอร์เวย์ของครั้งนั้น** นี่คือตัวเชื่อมของทั้ง 2 ฝั่ง

หน้าตรวจเคสจึงทำให้เหมือนกัน: เลือก "ครั้งที่" แล้ว **ผลการดำเนินงาน · ความเห็นทั้ง 2 ช่อง ·
ยอดเงินทั้งตาราง เปลี่ยนตามครั้งนั้นทันที** โดยไม่ต้องออกจากหน้า

**ครั้งที่ไม่ใช่เคสที่เปิดอยู่ = อ่านอย่างเดียว** ทุกช่องถูกล็อก ปุ่มบันทึก/อนุมัติ/ตีกลับปิด
มีปุ่ม "เปิดเคสครั้งนี้เพื่อแก้" ให้กระโดดไปแก้ที่เคสนั้นแทน — ถ้าปล่อยให้บันทึกได้
จะเขียนข้อมูลของครั้งที่กำลังดูทับเคสที่เปิดอยู่ และยอดเงินของเคสนั้นหายทั้งแถว

เพิ่ม **ยอดสะสมทั้งเคลม** (รวมทุกครั้ง แยกฝั่งพนักงาน/ฝั่งประกัน) ตามที่ระบบประกันมี

### เพิ่ม — "ครั้งที่" เลือกได้ + โชว์เลขเรื่องเซอร์เวย์ของครั้งนั้น
หัวการ์ดค่าใช้จ่ายเปลี่ยนเป็น **ค่าใช้จ่าย · เลขเรื่องเซอร์เวย์ · ครั้งที่ [เลือกได้]**
เลขเคลมเดียวมีได้หลายงาน (งานต่อเนื่อง) — ถ้าเคลมนี้มีหลายครั้ง ช่อง "ครั้งที่" จะกลายเป็น
ตัวเลือก แสดงเลขเรื่องเซอร์เวย์ของแต่ละครั้งกำกับไว้ · มีครั้งเดียวก็ยังเป็นป้ายเลขเหมือนเดิม

**เลือกครั้งอื่น = เปิดเคสของครั้งนั้น** (ทั้งหน้าเปลี่ยนตาม ไม่ใช่แค่ยอดเงินกับความเห็น)
เพราะในระบบเรา **แต่ละครั้งคือคนละเคส** คนละเลขเรื่องเซอร์เวย์ คนละรายงาน คนละยอดเงิน
สลับเฉพาะบางส่วนของหน้าไม่ได้ — กดบันทึกทีเดียวจะเขียนข้อมูลของครั้งอื่นทับเคสที่เปิดอยู่

ถามยืนยันก่อนสลับถ้ายังไม่ได้กด "บันทึกร่าง"

### ลบ — ปุ่ม "พิมพ์ข้อมูลความเสียหาย"
กดแล้วไม่มีอะไรเกิดขึ้นมาตั้งแต่แรก — ปุ่มที่กดไม่ได้ทำให้คนเข้าใจผิดว่าระบบพัง
เหลือปุ่ม "ข้อมูลความเสียหาย" ปุ่มเดียวที่ใช้งานได้จริง

### เพิ่ม — ปุ่ม "ข้อมูลความเสียหาย" เปิดหน้าจอเลือกเหมือนระบบประกัน
เดิมเป็นปุ่มที่กดแล้วไม่มีอะไรเกิดขึ้น · ตอนนี้เปิดหน้าต่างหน้าตาเดียวกับระบบประกัน:
**ติ๊กเลือกชิ้นส่วนสำเร็จรูป 22 ชิ้น** (พร้อมด้านซ้าย/ขวา/ทั้งคู่ และระดับความเสียหาย L/M/H/X)
ต่อด้วย **ช่องอิสระ 30 ช่อง** สำหรับชิ้นส่วนที่ไม่มีในรายการ (ปีกโคลน · คิ้ว · ฝาครอบ ฯลฯ)

มีไว้เติมของที่ผู้สำรวจเลือกมาไม่ครบ — สิ่งที่เลือกมาจากแอปจะติ๊กไว้ให้แล้ว
กด "ปิด" = ทิ้งที่แก้ · กด "ตกลง" = เอาเข้าฟอร์ม (ยังต้องกดบันทึกอีกที)

**เตือนเมื่อใช้ช่องอิสระเกิน 8 ช่อง** — ระบบประกันมี 30 ช่องก็จริง แต่**ฟอร์มตอนสร้าง
เรื่องใหม่มีแค่ 8** ส่วนที่เกินบอทกรอกให้ไม่ครบ ต้องไปเติมเองที่นั่น

### ปรับ — ช่อง "ราคาประกัน" เป็นน้ำเงินเข้ม คู่กับ "ราคาพนักงาน" ที่เป็นน้ำเงินอ่อน
เงิน 2 ฝั่งในตารางนี้เป็นคนละกระเป๋ากัน — ฝั่งหนึ่งคือยอดที่**เรียกเก็บบริษัทประกัน**
อีกฝั่งคือยอดที่**จ่ายให้ผู้สำรวจ** · เดิมฝั่งเรียกเก็บเป็นช่องขาวธรรมดาเหมือนช่องทั่วไป
ตอนนี้เป็นน้ำเงินชุดเดียวกันแต่คนละน้ำหนัก: **เข้ม = เรียกเก็บ · อ่อน = จ่ายออก**

### ปรับ — เก็บรายละเอียดแถบหัวเคสกับรางขวา
- **เอา "ทะเบียน" ออกจากแถบหัวเคส** — เลขเคลมกับเลขเซอร์เวย์พอสำหรับรู้ว่ากำลังตรวจเรื่องไหน
  (ช่องทะเบียนยังอยู่ในหมวดรถประกันตามเดิม)
- **ลดช่องว่างระหว่างการ์ด 2 ใบในรางขวา** 24 → 12px ให้ดูเป็นชุดเดียวกันมากขึ้น

### ปรับ — แยกตารางยอดเงินเป็นการ์ดของตัวเอง
ในรางขวา ช่องความเห็นกับตารางยอดเงินเคยอยู่การ์ดเดียวกัน แยกไม่ออกว่าเป็นคนละเรื่อง
ตอนนี้ตารางยอดเงินเป็น**การ์ดแยกที่มีเงารอบกรอบของตัวเอง** เหมือนการ์ดอื่นทั้งหน้า

ความกว้างตารางไม่เปลี่ยน — 2 การ์ดเต็มรางเท่ากัน ไม่ได้ซ้อนอยู่ในขอบของกันและกัน
(ถ้าซ้อน ช่อง "จำนวน" จะแคบจนอินพุตกับหน่วยล้นไปทับคอลัมน์ข้าง ๆ บนจอ 1500px)

ทั้งสองใบใช้**เงา ขอบ และมุมชุดเดียวกับแผงขวาในไฟล์ออกแบบ** — เงามาตรฐานจางเกินกว่า
จะเห็นบนพื้นเทาอ่อนของหน้านี้ ต้องเห็นชัดว่าเป็นการ์ดลอย 2 ใบ ไม่ใช่แผ่นเดียวยาว ๆ

### เพิ่ม — แอปมือถือบอกเหตุผลที่หัวหน้าตีกลับ (APK 1.0.55+57)
งานที่ถูกตีกลับจะขึ้นป้ายส้ม **"ตีกลับให้แก้"** แทนป้ายสถานะปกติ พร้อมกล่องข้อความ
**"หัวหน้าให้แก้: ..."** ตามที่หัวหน้าพิมพ์ไว้ — ไม่บอกเหตุผล ช่างก็ส่งของเดิมกลับมาเหมือนเดิม

### แก้บั๊ก — สถานะงานในแอปโชว์เป็นภาษาอังกฤษ
การ์ดงานแปลสถานะไว้ 4 ค่า แต่ **2 ค่าไม่มีอยู่จริงในระบบ** (`in_progress` / `completed`)
ส่วนค่าที่ใช้จริงอย่าง `surveyed` `reviewed` `declined` ไม่มีเลย เลยหลุดไปโชว์ดิบ ๆ
บนหน้าจอช่าง (เห็นจริงเป็นคำว่า "surveyed") · ตอนนี้ครบ 5 ค่า:
รอมอบหมาย · มอบหมายแล้ว · ส่งงานแล้ว · หัวหน้าอนุมัติแล้ว · ปฏิเสธงาน

### ปรับ — ลบการ์ดแจ้งเตือนท้ายหน้า · ปุ่ม "ตีกลับผู้สำรวจ" ขึ้นไปอยู่บนแถบหัวเคส
การ์ดสีเหลืองท้ายหน้าบอกเรื่องเดียวกับแถบแดง "ยังขาดช่องบังคับ" ที่อยู่บนสุดอยู่แล้ว — ตัดทิ้ง

ปุ่ม **"ตีกลับผู้สำรวจ"** ย้ายขึ้นไปอยู่ซ้ายสุดของแถบหัวเคส (หน้าปุ่ม "บันทึกร่าง")
กดแล้วช่องเหตุผลกางออกใต้แถบ กรอกแล้วกด "บันทึกแล้วตีกลับ" — ยังบังคับกรอกเหตุผลเหมือนเดิม
ทำเป็นปุ่มขอบบางไม่ใช่ปุ่มทึบ และวางไกลจาก "อนุมัติ" ที่อยู่ขวาสุด กันกดพลาด

### แก้บั๊ก — ปุ่มเปิดเมนูบังเลขเคลมบนแถบหัวเคส
ตอนเมนูยุบ ปุ่มแฮมเบอร์เกอร์เป็นปุ่มลอยทับมุมซ้ายบน เลยไปบังคำว่า "เคลม" กับเลขเคลม
ที่อยู่ซ้ายสุดของแถบหัวเคสพอดี

เปลี่ยนเป็นให้เมนูที่ยุบเหลือเป็น**แถบแคบ ๆ ที่ยังกินพื้นที่ตามปกติ** (ไม่ใช่หดเหลือศูนย์
แล้ววางปุ่มลอยทับ) เนื้อหาเลยถูกดันมาเองโดยไม่ต้องไปเผื่อที่ให้ปุ่มทีละหน้า

### ปรับ — ปุ่มบันทึก/อนุมัติเหลือจุดเดียว
ปุ่มชุดเดียวกัน (บันทึกร่าง · อนุมัติ · ป้ายยังอนุมัติไม่ได้) เคยโผล่ทั้งบนแถบหัวเคส
และในการ์ดท้ายหน้า — **เอาชุดล่างออก** เหลือที่แถบหัวเคสซึ่งติดขอบบนตลอด
เลื่อนไปตรงไหนของหน้าก็กดได้

เอาชุดบนออกแทนไม่ได้ เพราะจะกลับไปเป็นปัญหาเดิมคือต้องเลื่อนสุดหน้าเพื่อกดบันทึก

การ์ดท้ายหน้าเหลือของที่แถบบนไม่มี: **เหตุผล**ว่าทำไมยังกดอนุมัติไม่ได้ (แถบบนบอกแค่
จำนวนข้อ) และปุ่ม "ตีกลับให้ผู้สำรวจ" ที่ตั้งใจให้อยู่ไกลมือ

### ปรับ — หน้าตรวจเคส เก็บกวาดรอบหน้าจอ
- **เปิดเคสแล้วเมนูซ้ายยุบให้เอง** — หน้าเคสใช้พื้นที่ทั้งจอ (ฟอร์ม ~200 ช่อง + รางค่าใช้จ่าย)
  เมนูที่ไม่ได้ใช้ตอนนั้นเบียดที่อ่านไป 256px · ออกจากหน้าเคสแล้วกางคืนให้
  (ถ้าคนกดยุบเองไว้ที่หน้าอื่น ระบบจะไม่ไปกางแทรก)
- **เอาหัวข้อ "แผงควบคุม" ออกจากแถบบน** — ไม่ได้บอกอะไร ทุกหน้ามีหัวข้อของตัวเองแล้ว
- **รางขวารวมเป็นการ์ดเดียว** — หัวการ์ด "ค่าใช้จ่าย · ครั้งที่" ขึ้นไปอยู่บนสุดแทนหัวข้อ
  "การตรวจสอบ" ที่ถอดออก ข้างในเรียง 3 ช่องความเห็นแล้วต่อด้วยตารางยอดเงิน
- **3 ช่องความเห็นไม่มีข้อความจาง ๆ ในช่องแล้ว**

### ลบ — ช่อง "หมายเหตุเพิ่มเติม" ในแอปมือถือ
ตรวจแล้วหน้าจอที่มีช่องนี้ **ไม่มีทางเข้าถึงจากในแอปอยู่แล้ว** — ไม่มีปุ่มไหนพาไปถึง
(ตกค้างจากตอนพอร์ตหน้ากรอกเป็น Hub) ช่องจึงว่างเสมอและไม่เคยไปถึงระบบประกัน
ถอดโค้ดที่เหลือทิ้ง · **ไม่ต้องแจก APK ใหม่เพราะพฤติกรรมไม่เปลี่ยน** ข้อมูลเดิมใน DB ไม่ถูกลบ

### ปรับ — หมวด "การตรวจสอบ" ย้ายไปอยู่เหนือค่าใช้จ่ายในคอลัมน์ขวา
3 ช่องนี้ (ผลการดำเนินงาน · ความเห็นของผู้ตรวจสอบ · ความเห็นของเซอร์เวย์) กับยอดเงิน
คือ**ทุกอย่างที่ผู้ตรวจเขียนเอง**บนหน้านี้ — อยู่ด้วยกันในรางขวาที่ลอยตามการเลื่อนหน้า
เลื่อนอ่านข้อมูลเคสทางซ้ายไปพลาง เขียนไปพลางได้ · รางแคบกว่าเดิมจึงเรียงลงล่างแทน 3 คอลัมน์

### ปรับ — ช่องเงินที่เหลืออีก 3 ช่องก็ไม่ขึ้น 0.00 แล้ว
ค่าเสียหายส่วนแรก (กรมธรรม์) · ยอดเรียกร้องจากคู่กรณี · ค่าเสียหายประมาณของคู่กรณี/ทรัพย์สิน
รวม "จากจำนวนเงินเรียกร้องทั้งหมด" ที่เป็นช่องคู่กันด้วย

ผลพลอยได้: ติ๊ก "รับเงินจำนวน" แล้วปล่อยยอดเป็น 0 **จะขึ้นกรอบแดงแล้ว** — เดิม 0.00
นับเป็น "กรอกแล้ว" เลยผ่านด่านไปได้ทั้งที่ระบบประกันไม่รับ

### ปรับ — ช่องเงินไม่ขึ้น 0.00 แล้วทั้งตาราง
0.00 แปลว่า "ยังไม่ได้กรอก" ไม่ใช่ "ศูนย์บาท" — โชว์ไว้มีแต่ต้องลบทิ้งก่อนพิมพ์ทับ
ครอบทั้งฝั่งราคาพนักงาน 9 ช่องและฝั่งราคาประกัน 8 ช่อง · ไฟล์ที่ส่งเข้าประกันไม่กระทบ
(ค่าว่างถูกแปลงเป็น 0.00 ให้อยู่แล้ว)

### ลบ — ช่อง "หมายเหตุเพิ่มเติม" ในหน้าตรวจเคส
ตรวจแล้วช่องนี้**ไม่ได้ไปไหนเลย**: ไม่มีอยู่ในไฟล์ที่ส่งเข้าระบบประกัน (มีแต่คอมเมนต์
ห้ามเอาไปใช้แทน "ความเห็นของเซอร์เวย์" ตั้งแต่ 12/08/69) และบนฐานข้อมูลจริงมีข้อมูล
เพียง 1 จาก 9 ใบ ซึ่งเป็นเคสทดสอบ → ลบออกจากหน้า

⚠️ ช่องนี้ยังอยู่ในแอปมือถือ (ผู้สำรวจจดจากหน้างาน) — ข้อมูลเดิมใน DB ไม่ถูกลบ
ถ้าจะเอาออกจากแอปด้วยต้อง build APK ใหม่

### ปรับ — เอาแถบ "คิวตรวจวันนี้" ออกจากหน้าตรวจเคส
ตัวเลขชุดเดียวกัน (รอตรวจ / ติดปัญหา / อนุมัติแล้ววันนี้) มีอยู่ที่หน้ารายการงานแล้ว
ซ้ำสองที่คือกินพื้นที่เปล่า ๆ บนหน้าที่ยาวอยู่แล้ว · ตัวคิดเลขยังอยู่ที่เดิม หน้ารายการงานใช้ต่อ

### แก้บั๊ก — คอลัมน์ "ราคาพนักงาน" พิมพ์ไม่ได้ในงานจากไฟล์ XML
งานที่นำเข้าจากระบบเดิมถูกล็อกคอลัมน์ราคาพนักงานไว้ทั้งคอลัมน์ ด้วยเหตุผลว่า
"ยอดกรอกไปแล้วที่ระบบต้นทาง อยู่ที่ se-billing" — แต่เอาเข้าจริง**ยอดต้องกรอกที่นี่ได้ทุกงาน**
เพราะระบบประกันไม่มีช่องเก็บเรทพนักงานเลย ยอดที่จะไปโผล่หน้าค่าใช้จ่ายต้องออกจากระบบเรา

ตอนนี้กรอกได้ทุกที่มาของงาน · เหลือด่านเดียวคือ **อนุมัติแล้วห้ามแก้** (เหมือนข้อมูลส่วนอื่น)
ส่วนคำเตือน "ยังไม่ได้กรอกราคาพนักงาน" ยังทวงเฉพาะงานที่เกิดในระบบเราเหมือนเดิม —
งานเก่าที่ไม่เคยกรอกจะได้ไม่กลายเป็น "อนุมัติไม่ได้" ขึ้นมาทั้งกอง

นี่คือ**การล็อกผิดแกนครั้งที่ 3** ที่แก้ในสัปดาห์เดียว (ช่องทั่วไป → หักเงิน → ยอดรายรับ)
ทุกครั้งต้นเหตุเดียวกันคือเอา "ที่มาของงาน" ไปล็อกช่องกรอก ทั้งที่การล็อกที่ถูกมีอันเดียว
คือ "อนุมัติแล้ว" · ใส่การ์ดเทสห้ามไม่ให้มีธงล็อกช่องผูกกับที่มาของงานอีก

กันพลาดที่ตามมาด้วย: เคสที่กรอกแต่ "หักเงิน" โดยไม่มียอดรายรับเลย **ยอดรวมจะเป็นค่าว่าง
ไม่ใช่ติดลบ** — ไม่งั้นเลขติดลบจะไหลไปโผล่ในใบเบิกเงินเป็นยอดจ่ายจริง

### ปรับ — ช่องเปอร์เซ็นต์ค่าเรียกร้องไม่ขึ้น 0.00 แล้ว
0.00 ในช่องนี้แปลว่า "ยังไม่ได้กำหนด" ไม่ใช่ "ศูนย์เปอร์เซ็นต์" — โชว์ไว้มีแต่ต้องลบทิ้ง
ก่อนพิมพ์ทับทุกครั้ง (ส่วนใหญ่ใส่ 5 หรือ 10) · ช่องเงินอื่นยังแสดง 0.00 เหมือนเดิม

### เพิ่ม — ตีกลับให้ผู้สำรวจไปแก้เอง
เดิมหัวหน้าเจอข้อมูลผิดมีทางเดียวคือแก้เอง ซึ่งใช้ไม่ได้กับเรื่องที่ต้องถามคนที่ไปหน้างานจริง
(ทะเบียนไม่ตรงกับรูป · รูปไม่ครบ · เวลาที่จดมาผิด)

ปุ่ม **"ตีกลับให้ผู้สำรวจไปแก้ในแอป"** ที่ท้ายหน้าตรวจเคส
- **ต้องบอกเหตุผลทุกครั้ง** ว่าให้แก้อะไร — ไม่บอก ช่างก็ส่งของเดิมกลับมา
- กดแล้ว**บันทึกสิ่งที่หัวหน้าเพิ่งแก้ให้ก่อน** แล้วสถานะกลับเป็น "กำลังสำรวจ"
  งานโผล่ในรายการของช่าง แก้ในแอปแล้วส่งใหม่ได้ตามปกติ
- **ไม่มีการแจ้งเตือนเข้าเครื่อง** — แค่ให้เห็นในรายการงานพอ (ตกลงกันไว้)
- **หัวหน้ายังแก้เองได้ด้วย** — เคสที่ตีกลับไม่หายไปจากหน้าตรวจสอบ ย้ายไปอยู่แท็บ
  **"ตีกลับแล้ว"** แยกจาก "รอตรวจ" (งานอยู่กับช่าง ไม่ควรไปบวมอยู่ในคิวของหัวหน้า)
- เปิดหน้าเคสมาเห็นแถบส้มบอกทันทีว่าตีกลับไปแล้วและให้แก้อะไร · ส่งกลับมาแล้วยังเห็นย้อนหลัง
  ว่าเคสนี้เคยถูกตีกลับกี่ครั้ง

ตีกลับไม่ได้ 3 กรณี: อนุมัติแล้ว (ต้องให้แอดมินปลดล็อกก่อน) · ยังไม่ได้ส่งเข้ามาตรวจ ·
**เคสที่ไม่มีผู้สำรวจในระบบ** (งานนำเข้าจากระบบเก่า — ตีกลับแล้วจะไม่โผล่ในรายการของใครเลย)

แก้พ่วง: ส่งงานใหม่แล้ว**หมวดของรูปที่ผู้ตรวจอัปเองบนเว็บไม่หายอีกต่อไป** — เดิมรูปยังอยู่
แต่หมวดถูกล้างเป็นว่าง เพราะแอปไม่ได้ส่งหมวดของรูปที่ไม่ใช่ของตัวเองมาด้วย

⚠️ ต้องรัน `041_send_back_to_surveyor.sql` บน production ก่อนขึ้นโค้ด (รันแล้ว 18/08/69)

### ปรับ — หน้าตรวจเคส ค่าใช้จ่ายย้ายไปคอลัมน์ขวา (เฉพาะจอกว้าง)
บนจอกว้าง (ตั้งแต่ ~1500px ขึ้นไป) **ค่าใช้จ่ายย้ายไปอยู่คอลัมน์ขวา ลอยตามการเลื่อนหน้า**
เดิมอยู่ล่างสุด กรอกยอดทีต้องเลื่อนลงไปสุดหน้า แล้วเลื่อนกลับขึ้นมาดูข้อมูลเคส สลับไปมาทั้งวัน
ตอนนี้เห็นข้อมูลกับช่องกรอกยอดพร้อมกัน และหน้าสั้นลงเกือบครึ่ง

**จอแคบกว่านั้นเรียงลงล่างแบบเดิมเองอัตโนมัติ** — ไม่มีช่องไหนหายไปจากหน้าไม่ว่าจอขนาดไหน
(วัดจริงที่ 1499 / 1500 / 1920 / 2560px: ไม่มีคอลัมน์ล้นทับกัน สลับเลย์เอาต์ตรงจุดที่ตั้งไว้)

ห้ามถอดช่องยอดเงินออกจากหน้าตามขนาดจอเด็ดขาด — ตารางยอดเงินบันทึกทั้งแถว
ช่องที่ไม่ได้ถูกส่งไปด้วยจะกลายเป็นค่าว่างโดยไม่มีอะไรฟ้อง (มีการ์ดเทสนับช่องกันไว้ 16 ช่อง)

### เพิ่ม — แอดมินแก้เลขระบุตัวเคสได้ เมื่อผิดมาตั้งแต่ต้นทาง
5 ช่องที่ล็อกเป็นตัวหนังสือไปแล้ว (บริษัทประกัน · สาขา · เลขเรื่องเซอร์เวย์ · เลขที่รับแจ้ง ·
เลขที่เคลม) ต้องมีทางแก้เมื่อกรอกผิดมาแต่แรก — ปุ่ม **"แก้เลขระบุเคส (แอดมิน)"** บนหัวหมวด

- เปิดให้**แอดมินเท่านั้น** และกันที่ backend ด้วย ไม่ใช่แค่ซ่อนปุ่ม
- **เลขเรื่องเซอร์เวย์ห้ามซ้ำกับเคสอื่น** — ถ้าไปชนจะฟ้องพร้อมบอกว่าชนเคสไหน
  (เลขนี้ใช้อ้างอิงเบิกเงิน) · เลขเคลมซ้ำได้ตามกติกาเดิม 1 เคลมมีหลายครั้งที่
- **อนุมัติแล้วต้องปลดล็อกก่อน** — อนุมัติแล้วบอทยกเข้าระบบประกันไปแล้ว แก้ที่นี่ไม่ตามไปแก้ให้

### ปรับ — หน้าตรวจเคส หัวข้อหน้าตาเหมือนกันหมด
เอาแถบพื้นหลังสีฟ้าออกจาก 4 หมวด (บริษัท·เคลม · ค่าใช้จ่าย · การเรียกร้อง · รูปหลักฐาน)
ให้เป็นแถบเทาแบบเดียวกับหมวดอื่นทั้งหน้า — เดิมดูเหมือนคนละระบบมาต่อกัน · ดีไซน์ค่อยว่ากันทีหลัง

ตอนถอดสีฟ้าออกพบว่า **ปุ่มติ๊ก "ประเภทเคลม" ถูกวาดเองด้วยลูกเล่นซ่อนช่องจริงไว้ข้างหลัง**
เปลี่ยนกลับเป็นปุ่มติ๊กธรรมดา — นอกจากดูเข้ากับหมวดอื่นแล้ว ยังกดด้วยแป้นพิมพ์และอ่านออกเสียงได้ตามปกติ

### ปรับ — หน้าตรวจเคส เปลี่ยนจากตารางเป็นการ์ดทั้งหน้า
เดิมทุกหมวดเป็นตาราง 4 คอลัมน์แบบ `ป้าย : [ช่อง]` เรียงข้างกัน 2 ช่องต่อแถว
ตอนนี้เป็นการ์ด **ป้ายเล็กด้านบน / ช่องด้านล่าง เรียง 4 ช่องต่อแถว** ตามดีไซน์ใหม่

**ทุกช่องยังเห็นครบและกรอกได้เหมือนเดิม** — เปลี่ยนแค่การจัดวาง ไม่ได้ซ่อนหรือตัดช่องไหนทิ้ง
(ตรวจทุกก้อน: 187 ช่อง · ส่งตอนบันทึก 116 คีย์ เท่าเดิมตลอด)

แปลง 7 หมวด: รถประกัน · กรมธรรม์ · บริษัท·เคลม · ผู้ขับขี่ · อุบัติเหตุ ·
การเรียกร้องจากคู่กรณี · คดี·ตำรวจ·ติดตามงาน
เหลือตารางเดียวคือ **ค่าใช้จ่าย** ซึ่งดีไซน์กำหนดให้เป็นตาราง (รายการ/จำนวน/2 ราคา)

ระหว่างทางจัดกลุ่มช่องให้ตรงกับที่คนอ่านจริงด้วย:
- "ใบสั่ง" เคยอยู่ห่างจากกลุ่มใบขับขี่ 2 แถว ทั้งที่เป็นเรื่องเดียวกัน
- "โทรศัพท์ผู้สำรวจ" เคยยัดอยู่ในช่องเดียวกับชื่อ ทำให้ช่องชื่อแคบ
- 4 ช่องรถไฟฟ้าเคยแทรกกลางระหว่างปีจดทะเบียนกับเลขตัวถัง
- ข้อมูลบริษัทเรา (อ่านอย่างเดียว) เคยปนอยู่กับช่องที่กรอกได้ในตารางเดียวกัน
- "คดี·ตำรวจ" กับ "ติดตามงาน" เคยเป็น 2 การ์ด รวมเป็นการ์ดเดียวตามดีไซน์

**"การเรียกร้องค่าเสียหายจากคู่กรณี" จงใจไม่แตกเป็น 4 คอลัมน์** — ทั้งบล็อกเป็นกลุ่มเดียว
ที่ระบบประกันบังคับร่วมกัน (ติ๊ก ≥1 ข้อ · ติ๊ก "รับเงิน" แล้วต้องกรอกเงินทั้งคู่ และต้องไม่เกิน
ยอดเรียกร้องทั้งหมด) แยกเป็นช่อง ๆ แล้วกรอบเตือนที่คุมทั้งกลุ่มจะไม่มีความหมาย


### ปรับ — หน้าตรวจเคส เลิกยุบหมวด กางให้เห็นทุกช่องเสมอ
ถอด "โหมดสรุป" ที่เพิ่งทำไปออก — **หมวดที่ถูกยุบไว้คือหมวดที่ผู้ตรวจจะไม่เปิดดู**
แล้วปล่อยข้อมูลผิดผ่านไปได้ หน้านี้มีหน้าที่ตรวจ ไม่ใช่อ่านสรุป เห็นน้อยลง = พลาดง่ายขึ้น

- ทุกหมวดกางเสมอ ไม่มีปุ่มยุบ/เปิดฟอร์มเต็มอีกแล้ว
- **หัวหมวดยังอยู่** (บริษัท·เคลม · กรมธรรม์ · รถประกัน · ผู้ขับขี่ · อุบัติเหตุ ฯลฯ)
  พร้อมป้าย "ยังกรอกไม่ครบ" — ช่วยหาที่ ไม่ได้ซ่อนอะไร
- มีการ์ดเทสกันไม่ให้ใครเอาการยุบกลับมาใส่

### ปรับ — หน้าตรวจเคส ยุบได้ครบ 12 หมวด + การ์ดตัดสินใจท้ายหน้า
ทำต่อจากรอบก่อนจนครบทุกหมวดตามที่ดีไซน์ใหม่จัดไว้

- **ยุบได้ครบ 12 หมวด** — เพิ่ม อุบัติเหตุ · การเรียกร้องจากคู่กรณี · การตรวจสอบ ·
  คู่กรณี · ผู้บาดเจ็บ · ทรัพย์สิน · ความเสียหายรถประกัน
  (4 หมวดหลังสรุปเป็นรายแถว เช่น "คันที่ 1 · ญช4228 กรุงเทพ ฯ · ปิยะ มณีวงศ์")
- **รวม "คดี · ตำรวจ" กับ "ติดตามงาน" เป็นหมวดเดียว** ตามที่ดีไซน์จัดไว้
- **แถบ "คิวตรวจวันนี้"** บนหน้าเคส — รอตรวจ / ติดปัญหา / อนุมัติแล้ววันนี้
  กดแล้วกลับไปหน้ารายการงานได้ ไม่ต้องกดกลับไปดูว่าเหลืออีกกี่เรื่อง
- **การ์ดตัดสินใจท้ายหน้า** — ความเห็นผู้ตรวจสอบ + ปุ่ม + เหตุผลที่ยังกดอนุมัติไม่ได้
  อยู่ที่เดียวกัน · ปุ่มบันทึกเปลี่ยนชื่อเป็น **"บันทึกร่าง"** ให้ตรงกับสิ่งที่มันทำ

ตัวเลขคิวตรวจบนสองหน้าคิดจากไฟล์เดียวกัน (`reviewQueue.ts`) — เดิมถ้าปล่อยให้
ต่างหน้าต่างคิด วันหนึ่งจะแสดงไม่ตรงกันแล้วไม่มีใครรู้ว่าหน้าไหนถูก

### ปรับ — หน้าตรวจเคส ยุบหมวดที่กรอกครบแล้ว + เตือนตรงจุดที่ผิด
เปิดหน้ามาเจอช่องกรอก ~200 ช่องกางหมด ต้องเลื่อนยาวกว่าจะเห็นครบ ทั้งที่ส่วนใหญ่กรอกมาครบแล้ว

- **6 หมวดยุบเหลือสรุปอ่านอย่างเดียว** (บริษัท·เคลม · กรมธรรม์ · รถประกัน · ผู้ขับขี่ ·
  คดี·ตำรวจ · ติดตามงาน) — ค่าที่ยังไม่มีขึ้นเป็นขีดสีเทาจาง กดปุ่ม "เปิดฟอร์มเต็ม" เพื่อแก้
  **หมวดที่ยังกรอกไม่ครบจะกางไว้ให้เอง** ผู้ตรวจเห็นแต่สิ่งที่ต้องทำ
- **เตือนตรงจุดที่ผิด** — ลำดับเวลาผิดจะขึ้นข้อความใต้จังหวะนั้นเลย เช่น
  "⚠ ย้อนหลังกว่าเวลาเกิดเหตุ" เดิมบอกรวมไว้บนสุดของหน้า ส่วนตัวช่องได้แค่กรอบแดง
  ต้องเลื่อนขึ้นไปอ่านแล้วเลื่อนกลับลงมาแก้

ยอดเงินไม่ยุบโดยตั้งใจ (มีการ์ดเทสกันไว้) — ตารางยอดเงินบันทึกทั้งแถว
ถ้าซ่อนแล้วเผลอถอดช่องออกจากหน้า ยอดจะกลายเป็นว่างตอนกดบันทึกโดยไม่มีอะไรฟ้อง

### ปรับ — หน้าตรวจเคส จัดหน้าใหม่ ตรวจงานได้เร็วขึ้น
หน้านี้ยาวมาก (~200 ช่อง) ปุ่มบันทึกอยู่ล่างสุด และของที่ต้องดูคู่กันอยู่คนละที่

- **แถบหัวเคสติดขอบบน** — เลขเคลม · เลขเซอร์เวย์ · ทะเบียน · บริษัทประกัน · ที่มาของงาน
  พร้อม**ปุ่มบันทึก/อนุมัติ** เลื่อนไปตรงไหนของหน้าก็กดได้ ไม่ต้องเลื่อนกลับลงล่าง
- **การ์ด "ลำดับเวลา"** — 5 จังหวะเรียงซ้าย→ขวา (เกิดเหตุ → ลูกค้าแจ้ง → ประกันแจ้งสำรวจ
  → ถึงที่เกิดเหตุ → สำรวจเสร็จ) กรอกวันที่และเวลาได้ในการ์ดเดียว
  เดิมกระจายอยู่ 3 แถวคนละที่ในตาราง ทำให้ "ผิดลำดับ" มองด้วยตาไม่เห็น
  (เคส #141 ถึงที่เกิดเหตุก่อนเกิดเหตุ 10 นาที ผ่านการอนุมัติมาแล้ว)
- **การ์ดรูปหลักฐาน** บอกจำนวนรูปและแยกหมวดบนหัวการ์ด + เตือนเมื่อรูปน้อยผิดปกติ
  (งานที่ดึงจากระบบเก่าตอนยังไม่จบมีรูป 1–5 ใบ เทียบกับ 22–41 ใบตอนจบงาน)
- **"มีประกันภัยที่" ของคู่กรณีเป็น dropdown แล้ว — ใช้รายชื่อของ EMCS เป๊ะ ๆ (52 บริษัท)**
  บน EMCS ช่องนี้เป็น dropdown อยู่แล้ว สะกดเองเพี้ยนนิดเดียวบอทจะข้ามช่องนี้เงียบ ๆ
  งานจากระบบเก่าจะถูกแปลงชื่อให้ตรงลิสต์ EMCS **ตั้งแต่ตอนนำเข้า** (ตอนที่ยังมีคนตรวจอยู่)
  ไม่ใช่ปล่อยให้บอทเดาตอนกรอกซึ่งไม่มีใครดู · ชื่อที่แปลงไม่ได้ยังเก็บไว้ ไม่หายตอนบันทึก
  แต่จะขึ้นแดงว่า "ชื่อนี้ไม่มีใน EMCS — เลือกใหม่จากลิสต์"
  **จงใจไม่เดาให้**: ชื่อบริษัทประกันต่างกันแค่คำท้าย (ประกันภัย/ประกันสุขภาพ/ประกันชีวิต)
  ตอนทำเคยเดาพลาดจริง — "กรุงเทพประกัน**สุขภาพ**" ไปโผล่เป็น "กรุงเทพ**ประกันภัย**"
  ซึ่งคนละบริษัทกัน ถ้าหลุดไปคือเคลมผูกกับบริษัทผิดโดยไม่มีอะไรฟ้อง
- **ชื่อที่มีวงเล็บขึ้นแดงตรงช่องนั้นเลย** พร้อมบอกว่าตัวไหนใช้ไม่ได้ — EMCS ไม่ได้แค่
  ตัดอักขระทิ้ง แต่**ล้างชื่อทั้งช่อง**ตอนมีคนคลิกเข้า-ออก เดิมเตือนรวมไว้บนหัวหน้า
  ซึ่งคำนวณตอนโหลดหน้า แก้แล้วไม่หาย และไม่บอกว่าอยู่ช่องไหน
- **หักเงินเลิกใช้สีแดง** — ไม่ใช่ช่องบังคับ สีแดงบนหน้านี้สงวนให้ "ยังกรอกไม่ครบ" อย่างเดียว

### แก้บั๊ก — หน้าตรวจเคส ช่องคู่กรณี/ผู้บาดเจ็บ/ทรัพย์สิน และ dropdown เขต พิมพ์ไม่เข้าเลย
กดเข้าไปได้ เคอร์เซอร์กะพริบ แต่พิมพ์แล้วตัวอักษรไม่ขึ้น เลือกเขตแล้วเด้งกลับเป็น
"-- เขต --" — **ไม่มี error ไม่มีอะไรฟ้อง** เหมือนแป้นพิมพ์เสีย

ต้นเหตุ: ตัวทากรอบแดง "ช่องบังคับยังว่าง" ทำงานทุกครั้งที่มีการพิมพ์ และทำงาน
**เร็วเกินไป** — มันวาดหน้าใหม่ก่อนที่ตัวรับค่าจะได้เห็นสิ่งที่พิมพ์ การวาดใหม่นั้น
เขียนค่าเดิมทับสิ่งที่เพิ่งพิมพ์ พอถึงคิวตัวรับค่าจึงเห็นว่า "ไม่มีอะไรเปลี่ยน" แล้วทิ้งไป

โดนเฉพาะช่องที่ต้องพึ่งตัวรับค่า จึงพังเป็นหย่อม ๆ และดูไม่ออกว่าเกี่ยวกัน:

```
คู่กรณี (33 ช่อง/คัน) · ผู้บาดเจ็บ · ทรัพย์สินเสียหาย · ความเสียหายรถประกัน
ประเภทรถ · จังหวัด-เขต ที่เกิดเหตุ · จังหวัด-เขต ผู้ขับขี่
```

ช่องทั่วไป (ชื่อ ที่อยู่ สถานที่เกิดเหตุ) ไม่โดน — เลยดูเหมือน "บางช่องพิมพ์ไม่ได้"
เจอจริง 17/08/69 เคส #149 · มีการ์ดเทสกันไม่ให้ผูกกลับแบบเดิมอีก

### แก้บั๊ก — "หักเงิน" กรอกไม่ได้ในงานที่มาจากระบบเดิม
หักเงินถูกล็อกรวมไปกับยอดรายรับ ทั้งที่เป็นคนละเรื่อง — **หักเงินเป็นกติกาของเราเอง
ระบบเดิมไม่มีช่องนี้และ se-billing ไม่มีที่เก็บ** ล็อกไว้จึงแปลว่าหัวหน้าหักเงินงาน
จากระบบเดิมไม่ได้เลย

- หักเงิน (ส่งช้า · เอกสารไม่ครบ · ยอด · เหตุผล) กรอกได้ทุกที่มาของงาน
- **ไม่บังคับให้ระบุเหตุผลแล้ว** — เดิมหักเงินโดยไม่ใส่เหตุผลจะบันทึกทั้งหน้าไม่ผ่าน
- ยอด**รายรับ** 12 ช่องยังล็อกเหมือนเดิมสำหรับงานระบบเดิม (ยอดพวกนั้นอยู่ที่ se-billing)
  งานระบบเดิมที่ไม่มีการหักเงิน จะไม่สร้างรายการเงินเปล่า ๆ ทิ้งไว้

### เพิ่ม — หน้ารายการงานตรวจสอบ คัดงานได้โดยไม่ต้องเปิดทีละเคส
เดิมเป็นตารางเปล่า ๆ **ไม่มีค้นหา ไม่มีตัวกรอง ไม่มีแบ่งหน้า** และงานที่อนุมัติไปแล้ว
ปนอยู่กับงานที่ยังต้องทำ — พอเปิดใช้จริง (งานรอตรวจ ~106 เรื่อง/วัน หัวหน้า 8 คน)
จะหาของไม่เจอ

- **แถบสรุปด้านบน** — รอตรวจ / ข้อมูลยังไม่ครบ / อนุมัติแล้ว / draft ค้างที่ประกัน
- **แยกเป็น 3 แท็บ** รอตรวจ · อนุมัติแล้ว · ส่งประกันแล้ว (เดิมปนกันหมด)
- **ค้นหา**ด้วยเลขเคลม/เลขเซอร์เวย์/เลขรับแจ้ง/ทะเบียน/ชื่อผู้เอาประกัน
  + กรองตาม**ที่มา**และ**ช่างสำรวจ**
- **ป้ายบอกที่มาของงาน** (แอปมือถือ / ระบบเก่าสด / ไฟล์ XML) — จำเป็นเพราะ
  **กติกาเงินต่างกันตามที่มา** ไม่บอกไว้หัวหน้าจะงงว่าทำไมบางเคสกรอกยอดได้ บางเคสไม่ได้
- **คอลัมน์ "ต้องเติมก่อนอนุมัติ"** — รวมทุกอย่างที่ทำให้กดอนุมัติไม่ได้ไว้ที่เดียว:
  ยอดเงินที่ยังไม่กรอก · คำเตือนตอนนำเข้า · รูปน้อยผิดปกติ · จำนวนครั้งที่เคยปลดล็อก
- ปุ่ม "นำเข้า EMCS" โผล่เฉพาะเคสที่**อนุมัติแล้ว** — เดิมโผล่ตั้งแต่ยังไม่อนุมัติ
  กดไปก็เจอ error 403 โดยไม่รู้สาเหตุ

**คำเตือนตอนนำเข้าถูกเก็บไว้แล้ว** (migration 040) — เดิมส่งให้หน้าจอที่กดนำเข้าครั้งเดียว
แล้วหายไป ใครมาเปิดรายการงานทีหลังไม่มีทางรู้ว่าเคสไหนข้อมูลไม่ครบ

### แก้บั๊ก — บอทกรอกอำเภอภูมิลำเนาคู่กรณีผิด โดยไม่มีอะไรฟ้อง
ตัวแปลงรหัสอำเภอเชื่อว่า "สองระบบเรียงอำเภอเหมือนกัน" แล้วสลับแค่รหัสจังหวัด —
**ไม่จริง ผิด 186 จาก 924 อำเภอ**

```
เชียงใหม่  'เมืองเชียงใหม่' → เคยส่งเข้าประกันเป็น 'อำเภอดอยเต่า'
ชัยนาท    'วัดสิงห์'      → เคยส่งเข้าประกันเป็น 'อำเภอหันคา'
```

อันตรายตรงที่**เงียบสนิท** — ได้ชื่ออำเภอที่มีอยู่จริงในจังหวัดนั้น แค่คนละอำเภอ
ไม่มี error ให้จับ · กรุงเทพฯ รอดเพราะ 50 เขตบังเอิญเรียงตรงกัน ซึ่งเป็น 40% ของงาน
จึงไม่มีใครเจอมาตลอด

- เปลี่ยนเป็น**เปิดตารางจับคู่ด้วยชื่ออำเภอ** → ตรวจครบ 1,004 อำเภอ: **ถูก 926 ผิด 0**
  (ที่เหลือ 78 เป็นชื่อที่ระบบเก่ามีฝ่ายเดียว เช่น 'เทศบาลตำบล…' ปล่อยว่างให้คนเลือก)
- แก้ที่**ตัวสร้างไฟล์ด้วย** — ไม่งั้นวันหลังใครรันสร้างใหม่ บั๊กกลับมาเงียบ ๆ
- เพิ่มการ์ดกันพลาด 6 ข้อ ผูกกับเคสจริงที่เคยผิด
- กระทบ ~3 เรื่อง/วัน (เชียงใหม่เป็นหลัก) เฉพาะเคสที่มีคู่กรณีกรอกที่อยู่ · งานไอโออิล้วน

### แก้บั๊ก — งานจากระบบเก่าแก้อะไรไม่ได้เลยทั้งหน้า
ตัวล็อกช่องที่ตั้งใจใช้กับ **ยอดจ่ายพนักงาน** อย่างเดียว ดันถูกผูกไว้กับ **116 ช่อง
ที่ไม่เกี่ยวกับเงินเลย** — ชื่อ ที่อยู่ สถานที่เกิดเหตุ ลักษณะความเสียหาย ฯลฯ

ผลคือเคสที่มาจากระบบเก่า (ยอดอยู่ที่ se-billing จึงล็อกช่องเงินไว้) **พิมพ์อะไรไม่ได้
ทั้งหน้า** แม้จะกด "แก้ไขทั้งหมด" แล้วก็ตาม · เดิมไม่มีใครสังเกตเพราะดูเหมือนแค่
ยังไม่ได้เข้าโหมดแก้ไข พอเปลี่ยนเป็นพิมพ์ได้ทันทีเลยโผล่ออกมา

แยกเป็น 2 ตัวแล้ว: ช่องทั่วไปแก้ได้เสมอ · ช่องยอดจ่ายพนักงาน 16 ช่องยังล็อกตาม
ที่มาของงานเหมือนเดิม · เพิ่มการ์ดกันช่องทั่วไปหลุดไปใช้กติกาของเงินอีก

### ปรับ — หน้าตรวจเคส พิมพ์ได้ทุกช่องทันที ไม่ต้องกด "แก้ไขทั้งหมด" ก่อน
เดิมหน้าเปิดมาเป็นโหมดอ่าน ผู้ตรวจเห็นช่องแดงว่าอะไรขาด **แต่แก้ไม่ได้**
จนกว่าจะรู้ว่าต้องกดปุ่มก่อน · ตอนนี้พิมพ์ได้เลย เหลือปุ่ม บันทึก กับ อนุมัติ

### แก้บั๊ก (เชิงป้องกัน) — ดอกจันช่องบังคับผูกกับช่องของตัวเองแล้ว
ตัวหาช่องเดิม**เดาจากตำแหน่งใน HTML** (ไล่ดูช่องข้าง ๆ ในแถวเดียวกัน) ซึ่งใช้ได้เฉพาะ
กับตารางแบบปัจจุบัน — เคยทำให้ดอกจันไปทาแดงช่องข้าง ๆ จนกดอนุมัติไม่ได้ทั้งที่ข้อมูลครบ
(เคส #141 เมื่อ 15 ส.ค.)

ตอนนี้ดอกจันแต่ละตัวประกาศเองว่าคุมช่องไหน (29 ตัว คุม 46 ช่อง) — ย้ายตำแหน่งหรือ
เปลี่ยนหน้าตายังไงก็ยังชี้ถูก · เพิ่มการ์ดกันพลาด: ดอกจันที่ลืมผูก หรือผูกไปยังช่องที่
ไม่มีอยู่จริง (พิมพ์ผิด = ไม่คุมอะไรเลยแบบเงียบ ๆ) จะทำให้เทสแดงทันที

### เพิ่ม — ตั้งบัญชี ISURVEY และ EMCS จากหน้า "ตั้งค่า" ได้เลย ไม่ต้องแก้ไฟล์
เดิมแต่ละเครื่องต้องเปิดไฟล์ตั้งค่าแก้ชื่อผู้ใช้/รหัสผ่านเอง · ตอนนี้กรอกในหน้าเว็บได้
พร้อมปุ่ม **ทดสอบเข้าสู่ระบบ** ที่บอกกลับมาว่าเข้าได้ไหมและเป็นบัญชีของใคร

- **รหัสผ่านเดินทางทางเดียว** — หน้าเว็บส่งเข้าเครื่องได้อย่างเดียว
  ฝั่งโปรแกรมไม่เคยส่งรหัสกลับมาให้เบราว์เซอร์ (บอกแค่ "ตั้งไว้แล้วหรือยัง")
- ตั้งได้จากหน้าโปรแกรมในเครื่องตัวเองเท่านั้น · ไม่พิมพ์รหัสลงหน้าคอนโซล
- เว้นช่องรหัสว่าง = แก้แค่ชื่อผู้ใช้ รหัสเดิมไม่หาย
- เขียนทับไฟล์ตั้งค่าแบบคงบรรทัดอื่นและคอมเมนต์ไว้ครบ (ไฟดับกลางคันก็ไม่ได้ไฟล์ครึ่ง ๆ)
- **บัญชี EMCS ไม่มีปุ่มทดสอบเข้าสู่ระบบโดยตั้งใจ** — การทดสอบต้องเปิดเข้าระบบของ
  บริษัทประกันจริง ซึ่งตกลงกันว่าห้ามแตะโดยไม่จำเป็น · จะรู้ว่ารหัสถูกไหมตอนสั่งงานจริง
  (ISURVEY เป็นระบบของเราเอง จึงทดสอบได้)

### แก้บั๊ก — ไฟล์สำรองที่มีรหัสผ่านหลุดเข้า git ได้
ตัวกันเดิมดักแค่ชื่อ `.env` เป๊ะ ๆ · ไฟล์อย่าง `.env.bak` หรือไฟล์ชั่วคราวตอนเขียนทับ
มีรหัสผ่านครบเหมือนกันแต่**ไม่ถูกดัก** — เจอตอนทดสอบของใหม่ (ไฟล์สำรองโผล่ในรายการ
รอ commit จริง ๆ) แก้เป็นดัก `.env` ทุกแบบ ยกเว้นไฟล์ตัวอย่าง

### ปรับ — พิมพ์ค้นผู้สำรวจได้ ไม่ต้องไล่หาใน dropdown
รายชื่อผู้สำรวจในช่วง 3 วันมี ~85 คน เลื่อนหายาก · เพิ่มช่องพิมพ์ค้น
พิมพ์รหัส (`se2`) หรือชื่อ (`กรกฎ`) ก็ได้ · dropdown กับช่องค้นล้างกันเอง (ช่องนี้ไม่มีข้อความตัวอย่างในช่อง)
ไม่งั้นเลือกคนไว้แล้วพิมพ์ชื่ออีกคนจะได้ 0 แถวโดยไม่รู้สาเหตุ

### ปรับ — แท็บ "ดึงงานรอตรวจ" กรองงานได้เหมือนแท็บนำเข้า
เพิ่มตัวกรอง **ผู้สำรวจ** (พร้อมจำนวนงานในวงเล็บ) และ **เลขท้ายเลขเคลม** — ชุดเดียวกับ
แท็บ "นำเข้า ISURVEY" · จำค่าที่เลือกไว้ให้ ไม่ต้องตั้งใหม่ทุกวัน
· ⚠️ แท็บนี้กรองด้วย **ผู้สำรวจ** ไม่ใช่ "หัวหน้าตรวจ" เหมือนแท็บแรก เพราะงานสถานะ
  "รอตรวจข้อมูล" คืองานที่**ยังไม่มีใครตรวจ** ช่องหัวหน้าตรวจจึงว่างทุกแถว (0 จาก 406)

### เพิ่ม — ผู้ตรวจสอบเพิ่มรูปเองได้จากหน้าเคส
เดิมรูปเข้าระบบได้ทางเดียวคือจากแอปของช่างสำรวจ — ผู้ตรวจสอบทำได้แค่ดู
ทั้งที่รูปมาไม่ครบบ่อย (งานที่ดึงจากระบบเก่ามักมี 1-5 ใบ) และบางรูปหัวหน้าได้มา
ทาง LINE/อีเมล ซึ่งไม่มีวันไปโผล่ที่ระบบต้นทางให้ดึงได้เลย

- แถบ "เพิ่มรูป" อยู่หัวแกลเลอรี เลือกหมวดแล้วอัปได้ทีละหลายไฟล์ (jpg/png/webp)
- **ลบรูปได้ด้วย** — ชี้ที่รูปแล้วกดกากบาทมุมขวาบน หรือกด "ลบรูปนี้" ตอนเปิดจอเต็ม
  (ถามยืนยันก่อนเสมอ · ลบได้ทุกรูปของเคส ไม่ใช่เฉพาะที่เพิ่งอัป เพราะคนที่รับผิดชอบว่า
  อะไรจะถูกส่งเข้าระบบประกันคือผู้ตรวจสอบ)
- **เพิ่มอย่างเดียว ไม่ทับของเดิม** — คนละทางกับการอัปจากแอปที่เป็น "ล้างแล้วเขียนใหม่"
- เคสที่อนุมัติแล้วแถบนี้จะหายไป (ชุดรูปถูกรับรองไปแล้ว) ต้องให้แอดมินปลดล็อกก่อน
- เคสที่ยังไม่มีรูปสักใบก็เห็นแถบนี้ — เป็นเคสที่ต้องเพิ่มรูปมากที่สุด

### เพิ่ม — ดึงงานที่ระบบเก่ายัง "รอตรวจข้อมูล" มาตรวจบนเว็บเราแทน
เดิมหัวหน้าต้องตรวจงาน **2 ที่**: ตรวจ+ใส่เรทบนระบบเก่าก่อน แล้วค่อย export ไฟล์
มาอัปโหลดเข้าเว็บเรา · ตอนนี้ดึงงานได้ตั้งแต่ตอนที่ช่างเพิ่งส่งงาน (ก่อนหัวหน้าตรวจ)
มาโผล่ที่ "รายการงานตรวจสอบ" ให้ตรวจ/กรอกยอด/อนุมัติจบในที่เดียว

- โปรแกรมบนเครื่องผู้ตรวจมีแท็บใหม่ **"📤 ดึงงานรอตรวจ"** — เลือกช่วงวันที่ ติ๊กงาน
  แล้วกดดึงทีละหลายเรื่อง · **ไม่แตะระบบเก่าเลย** (อ่านอย่างเดียว งานต้นทางไม่เปลี่ยนสถานะ)
- ยอดเงินของเส้นทางนี้ **หัวหน้ากรอกเองบนเว็บได้** ต่างจากงานที่ปิดจบบนระบบเก่า
  (ซึ่งดูอย่างเดียว) เพราะจังหวะที่ดึงมา ยังไม่มีใครกรอกยอดที่ไหนเลย
- ได้ของที่ทางไฟล์ไม่เคยได้: **รายการความเสียหายรถประกัน** (ไฟล์ export ว่างเสมอ)
  และ **"ผู้ขับขี่/ผู้โดยสารรถคู่กรณี"** ของผู้บาดเจ็บ ที่ไฟล์ยุบเหลือ 3 แบบจนแยกไม่ออก
- ช่องที่แปลข้ามระบบไม่ได้จริง ๆ **ปล่อยว่าง + ขึ้นเตือน** ไม่เดาให้
  (ผลคดี "ไม่มีคู่กรณี" · ประเภทเคลม "เจรจาสินไหม" · ระดับบาดเจ็บที่เป็นเสียชีวิต)
- **ปุ่ม "ดึงรูปใหม่"** ต่อเคส — ต้นทางทยอยอัปรูปหลังช่างส่งงาน (ตอนรอตรวจมัก 1-5 ใบ
  พอจบงานกลายเป็น 20-40) กดซ้ำได้เรื่อย ๆ รูปที่มีแล้วจะไม่ซ้ำ
- เทียบกับไฟล์ที่ระบบเก่า export เอง **10 เคลม 146 จุด ตรงกันหมด**

### แก้บั๊ก — ชื่ออำเภอเพี้ยนข้ามระบบ (เจอตอนทำเส้นทางใหม่)
สองระบบเรียงลำดับอำเภอไม่เหมือนกัน การแปลงด้วย "ลำดับรหัส" จึงได้อำเภอผิด
**186 จาก 924 อำเภอ** เช่น ชัยนาท: 'วัดสิงห์' กลายเป็น 'หันคา' · พิษณุโลก: 'วังทอง'
กลายเป็น 'เนินมะปราง' — เส้นทางใหม่จับคู่ด้วย **ชื่อ** แทน (ตรง 926 ผิด 0)
· ⚠️ ตัวแปลงเดิมยังใช้อยู่ในเส้นทางอื่นของบอท (อำเภอภูมิลำเนาคู่กรณี) ยังไม่ได้แก้

### ✅ เคสแรกที่เดินครบเส้นทางใหม่ — จากแอปมือถือถึงมือบริษัทประกัน
เคลม `21BR10ACD-6906-000348` (16 ส.ค.) เป็นงานแรกที่ผ่านครบทุกด่านตามแผนศูนย์กลาง

```
แอปมือถือ → เว็บ se-survey → หัวหน้าตรวจ+อนุมัติ → บอทยกเข้าระบบประกัน
→ บอทอ่านกลับเทียบ (ตรง 35/35 ช่อง) → คนตรวจ → คนกดส่ง → ประกันรับงาน
```

ยอดรวมสุทธิที่ระบบประกันได้รับ **909.50 บาท ตรงกับที่ระบบเดิมอนุมัติไว้ทุกสตางค์**
· ความเห็นผู้ตรวจสอบ 8 บรรทัดลงถูกช่อง · ชื่อ-นามสกุลผู้ขับขี่แยกถูก · ลำดับเวลาถูกต้อง

### ปรับ — งานจากระบบเราเอง บอทกรอกหน้าค่าใช้จ่ายให้เต็ม (กลับกติกาเดิม)
เดิมบอทกรอกแค่เลขที่ใบแจ้งหนี้กับวันที่วางบิล เพราะตอนตั้งกติกา (3 ส.ค.) หัวหน้ายังไม่ได้
กรอกยอดบนเว็บเรา จะไปกรอกในระบบประกันเอง — **เงื่อนไขนั้นหมดไปแล้ว** ตอนนี้หัวหน้า
กรอกยอดและความเห็นบนเว็บก่อนอนุมัติ (และกดอนุมัติไม่ได้ถ้ายังไม่กรอกยอด)
· เจอตอนใช้จริง: กรอกเรทราคาบนเว็บครบแล้วแต่หน้าค่าใช้จ่ายในระบบประกันว่างเปล่า
· ช่องไหนไม่มีข้อมูลก็ข้ามให้เหมือนเดิม ไม่ทับของที่คนกรอกไว้

### แก้บั๊ก — งานจากระบบเดิมไม่เคยกรอก "เลขที่รับแจ้ง" เลย
ไม่มีใครรู้มาก่อนเพราะไอโออิยอมให้ช่องนี้ว่างได้ถ้ามีเลขเคลม · พอแก้ให้เลือกบริษัทถูก
เป็นไทยไพบูลย์ ช่องนี้กลายเป็นบังคับทันทีแล้วบันทึกไม่ผ่าน — **การแก้บั๊กบริษัทประกัน
คือสิ่งที่เปิดโปงบั๊กนี้** ทั้งที่ระบบเดิมส่งเลขมาให้อยู่แล้ว

### แก้บั๊ก — โหมดซ่อมงานเดิมไม่ได้ผ่านตัวตรวจอ่านกลับ
ตัวตรวจต่อไว้ 2 เส้นทาง แต่โหมด "เข้าไปเติมงานที่ค้าง" เป็นเส้นที่ 3 ที่หลุด
ทั้งที่เป็นเส้นที่**ยิ่งต้องตรวจ** เพราะเข้าไปแก้ของที่รอบก่อนพลาด
· และให้ตรวจอ่านกลับตอน**บันทึกไม่ผ่าน**ด้วย — เดิมตรวจเฉพาะตอนสำเร็จ ซึ่งพลาดทุกครั้ง
  ที่ต้องการข้อมูลมากที่สุด

### แก้บั๊ก — สั่งบอททำงานเดิมซ้ำ แล้วโฟลเดอร์รูปพังจนอัปไม่ได้
ตัวตั้งชื่อไฟล์รูปสำรองของเดิมไว้แล้วไม่ได้สร้างตัวใหม่ → รอบถัดไปหาไฟล์ไม่เจอ
ล้มทั้งงานก่อนถึงหน้าค่าใช้จ่าย พร้อม error ที่อ่านแล้วไม่รู้เลยว่าเกิดอะไรขึ้น
· ตอนนี้**กู้คืนเองได้** (เจอไฟล์สำรองแล้วตัวจริงหาย ก็คืนชื่อให้)
· ไฟล์ที่หายจริงข้ามเฉพาะไฟล์นั้น ไม่ล้มทั้งงานเพราะรูปเดียว
· ถ้ายังไม่ครบก็หยุดพร้อมบอกวิธีแก้ แทนที่จะไปพังตอนอัปโหลด

### แก้บั๊ก — กรอบแดงไปโผล่ผิดช่อง จนกดอนุมัติไม่ได้ทั้งที่ข้อมูลครบ
ตัวไฮไลต์หา "ช่องของดอกจัน" ด้วยการถอยขึ้นไปถึงทั้งแถว แล้วทาแดงทุกช่องในแถวนั้น
แต่หน้านี้ยัดป้าย/ช่องไว้ 2-3 คู่ต่อแถว เช่น `ผู้แจ้ง | [ช่อง] | ผู้สำรวจภัย * | [ช่อง]`
→ ดอกจันของ "ผู้สำรวจภัย" ไปทาแดงให้ "ผู้แจ้ง" ที่ไม่ได้บังคับด้วย
· ตอนเป็นแค่กรอบแดงก็แค่เตือนหลอก **พอเอาไปบล็อกปุ่มอนุมัติกลายเป็นเรื่องใหญ่** —
  เคสที่ข้อมูลครบแล้วกดอนุมัติไม่ได้ (เจอทันทีที่ลองใช้จริง เคสแรกเลย)
· ตอนนี้ดอกจันคุมเฉพาะช่องของตัวเอง · ช่องที่เคยขึ้นแดงหลอกหายไป 9 ช่อง

### แก้บั๊ก — ปลดล็อกแล้วหน้ายังล็อกอยู่ ปุ่มแก้ไขไม่โผล่
หน้าเว็บดูว่า "มีใบอนุมัติอยู่ = อนุมัติแล้ว" แต่ตอนปลดล็อกใบไม่ได้หายไป แค่เปลี่ยนสถานะ
(1 เคสมีใบเดียว ใช้ซ้ำเมื่ออนุมัติใหม่) → ปลดล็อกสำเร็จแต่หน้ายังอ่านอย่างเดียว
แก้อะไรไม่ได้เลย · ตอนนี้ดูสถานะจริงของใบแทน

### แก้บั๊ก — เคสที่บอทยกเข้าระบบประกันแล้ว ปลดล็อกไม่ได้เลย จนแก้อะไรไม่ได้
กติกาเดิมกัน "เคสที่เข้าระบบประกันแล้ว" ไม่ให้ปลดล็อก ซึ่งกว้างเกินไป — การที่บอท
สร้างเรื่องไว้ยัง**ไม่ได้แปลว่าบริษัทประกันได้รับงาน** ตราบใดที่ยังเป็นฉบับร่าง
การแก้ข้อมูลฝั่งเราแล้วให้บอทเข้าไปเติมของเดิมคือขั้นตอนปกติที่ออกแบบไว้อยู่แล้ว
· เจอตอนใช้จริงทันที: เรื่องที่ยกเข้าไปแล้วแต่ข้อมูลยังไม่ครบ กลายเป็นติดตาย
  แก้ไม่ได้ทั้งสองฝั่ง
· ตอนนี้กันเฉพาะเคสที่**ส่งงานให้บริษัทประกันไปแล้วจริง ๆ** ซึ่งถอยไม่ได้

### เพิ่ม — ตรวจ "ลำดับเวลา" และ "ยอดเงินของงานจากแอป" ก่อนอนุมัติ
เจอตอนนำเคสจากแอปมือถือเข้าระบบประกันจริง — เคสผ่านการอนุมัติมาแล้ว ช่องบังคับครบหมด
แต่ระบบประกันตีกลับ 3 ข้อรวด เพราะ **ผู้สำรวจไปถึงที่เกิดเหตุก่อนเวลาเกิดเหตุ 10 นาที**
(เกิดเหตุ 16:00 · ถึงที่เกิดเหตุ 15:50)

· เป็นความผิดพลาดที่**มองด้วยตาไม่เห็น** — แต่ละช่องดูปกติหมด ต้องเอามาเทียบกันถึงจะรู้
· ตอนนี้เว็บตรวจให้: ถึงที่เกิดเหตุต้องหลังเวลาเกิดเหตุ · หลังลูกค้าแจ้ง · หลังประกันแจ้งสำรวจ
  และสำรวจเสร็จต้องหลังถึงที่เกิดเหตุ · ผิดข้อไหนขึ้นแถบแดงบนสุดพร้อมบอกว่าผิดตรงไหน
· **งานจากแอปมือถือไม่มียอดเงินติดมา** — เตือนถ้ายังไม่ได้กรอกราคาพนักงาน/ราคาประกัน
· ทั้งสองอย่าง **กดอนุมัติไม่ได้จนกว่าจะแก้**

### เพิ่ม — บอทอ่านหน้าระบบประกันกลับมาเทียบ ก่อนถือว่างานเรียบร้อย
ระบบประกันเคย**กลืนข้อมูลเงียบ ๆ** มาแล้ว 3 ครั้ง (เครื่องหมาย + หาย · ยี่ห้อรถว่าง ·
ค่า "-- ระบุ --" ถูกบันทึกเป็นข้อมูลจริง) ทั้ง 3 ครั้ง**เจอเพราะมีคนมองจอ** —
วันที่บอทกดส่งงานเอง คนคนนั้นจะไม่อยู่แล้ว
· บอทจำทุกค่าที่พิมพ์ลงไป พอบันทึกเสร็จก็**อ่านค่าจริงจากหน้าจอกลับมาเทียบทีละช่อง**
· ต่างกันแค่รูปแบบ (750 → 750.00 · วันที่สลับตัวคั่น) ถือว่าตรง · **เนื้อหาหายถือว่าไม่ตรง**
· ไม่ตรง → ขึ้นบนการ์ดว่าช่องไหน กรอกอะไรไป บนหน้าเป็นอะไร ให้หัวหน้าตรวจก่อน
· **ไม่ตรง = บอทไม่กดส่งงานให้ ไม่ว่าใครสั่ง** (เปิดหน้าค้างไว้ให้แก้แล้วกดเอง)

### เพิ่ม — ให้ระบบประกันบอกกฎของตัวเอง แทนที่เราจะเดา
ทุกครั้งที่ระบบประกันฟ้องว่ากรอกไม่ครบ บอทจะเก็บข้อความนั้นไว้เป็นแฟ้มสะสม
พร้อมเลขเคลม/เลขเซอร์เวย์ · **ฟ้อง 1 ครั้ง = ได้กฎ 1 ข้อ** เอามาทำเป็นช่องบังคับ
บนเว็บได้โดยไม่ต้องเดา — จำเป็นเพราะกฎของเขามีสาขาแยกตามบริษัท และอ่านค่าธง
ภายในที่เรามองไม่เห็นจากข้างนอก อ่านโค้ดอย่างเดียวไม่มีวันครบ

### เพิ่ม — ช่องบังคับ "แบบมีเงื่อนไข" เตือนเป็นสี + กดอนุมัติไม่ได้ถ้ายังว่าง
เดิมช่องกลุ่มนี้มีแค่ดอกจันเหลืองเฉย ๆ ไม่มีใครรู้ว่าเมื่อไหร่ต้องกรอก จนไปตกตอนบอท
บันทึกที่ระบบประกันไม่ผ่าน (เจอจริง 15 ส.ค.) · กติกาอ่านจากตัวตรวจสอบของระบบประกันเอง
ไม่ได้เดา และไม่แยกตามบริษัท

**เมื่อไหร่บังคับ**
· **การเรียกร้องค่าเสียหายจากคู่กรณี** + **คู่กรณีคันที่** → บังคับเมื่อผลคดี =
  "รถคู่กรณีเป็นฝ่ายผิด" เท่านั้น (อีก 6 ผลคดีไม่บังคับ)
· **ช่องเงิน 2 ช่อง** → บังคับเมื่อติ๊ก "รับเงินจำนวน" · และยอดที่รับต้องไม่เกิน
  ยอดเรียกร้องทั้งหมด ไม่งั้นระบบประกันไม่ยอมให้บันทึก (เตือนให้เห็นตั้งแต่บนเว็บ)

**สีที่เห็น** — 🔴 บังคับตอนนี้แต่ยังว่าง · 🟡 ยังไม่บังคับแต่ยังว่าง · กรอกแล้วหายทันที
โดยไม่ต้องกดบันทึกก่อน

**กดอนุมัติไม่ได้ถ้ายังมีช่องแดงว่างอยู่** — ปุ่มจะเทาพร้อมบอกเหตุผลว่าขาดอะไรบ้าง
เพราะอนุมัติแล้วบอทหยิบไปเข้าระบบประกันทันที ถ้าข้อมูลไม่ครบก็ไปตกที่นั่นแล้วเสียเที่ยว
(ช่องเหลืองไม่บล็อก — ยังไม่บังคับ)

### แก้บั๊ก — บอทเลือก "บริษัทประกัน" ผิด เมื่อนำงานจากระบบเดิมเข้าระบบประกัน
เจอตอนทดสอบเคลมไทยไพบูลย์เคสแรกบนเส้นทางนี้ (15 ส.ค.) — บอท**เลือกบริษัทตายตัว**
เป็นไอโออิเสมอ ไม่ดูว่างานเป็นของใคร ที่ผ่านมาไม่มีใครเจอเพราะเส้นทางนี้รับแต่งานไอโออิ
· ถ้าบันทึกผ่าน **สำนวนไทยไพบูลย์จะไปอยู่ใต้ไอโออิ** และระบบประกันยังตรวจรูปแบบ
  เลขเคลมด้วยกติกาของบริษัทผิดอีกชั้น (เคสทดสอบเลยบันทึกไม่ผ่าน — ยังไม่มีอะไรค้าง)
· ตอนนี้ดูจาก**เลขเซอร์เวย์**: SETP = ไทยไพบูลย์ · SEABI = ไอโออิ · **ไม่รู้จัก = หยุด ไม่เดา**
  (เดาผิดคือยื่นสำนวนเข้าบริษัทที่ไม่ใช่เจ้าของงาน ซึ่งลบไม่ได้)
· เส้นทางงานจาก se-survey **ไม่ได้รับผลกระทบ** — เส้นนั้นเลือกบริษัทตามข้อมูลเคสถูกอยู่แล้ว

### เพิ่ม — "อนุมัติแล้วล็อก" ประตูเดียวก่อนงานออกไปหาบริษัทประกัน
ก้าวแรกของแผนใหม่: เว็บ se-survey เป็นศูนย์กลาง ทุกงานผ่านที่นี่ หัวหน้าอนุมัติ แล้วบอทยกเข้าระบบประกัน

**เดิมปุ่ม "อนุมัติ" ไม่ได้กั้นอะไรเลย** — บอทหยิบงานที่ยังไม่อนุมัติเข้าระบบประกันได้เท่ากับ
งานที่อนุมัติแล้ว และอนุมัติไปแล้วก็ยังกดแก้ไข/บันทึกทับได้ ลายเซ็นผู้อนุมัติจึงไม่ได้รับรองอะไร

· **บอทเห็นเฉพาะงานที่อนุมัติแล้ว** — งานที่ยังไม่อนุมัติ บอทดึงข้อมูลไปกรอกไม่ได้เลย
  และขึ้นข้อความบอกว่าให้หัวหน้าอนุมัติก่อน (ไม่ใช่ error รหัสดิบ)
· **อนุมัติแล้วล็อกทั้งหน้า** ทุกช่องกลายเป็นอ่านอย่างเดียว รวมถึงรูป (แอปอัปทับไม่ได้แล้ว)
· **ปลดล็อกได้เฉพาะแอดมิน** ปลดแล้วงานกลับไปสถานะรอตรวจ ผู้ตรวจต้องกดอนุมัติใหม่
  · ระบบจำว่าใครปลด เมื่อไหร่ และเคสนี้ถูกปลดมากี่รอบ
  · งานที่เข้าระบบประกันไปแล้วปลดไม่ได้ (ของที่นั่นลบไม่ได้ ปลดแล้วข้อมูล 2 ที่จะไม่ตรงกัน)
· **ถามยืนยันก่อนกดอนุมัติ** เพราะกดแล้วถอยเองไม่ได้
· **ยอดจ่ายพนักงาน: งานจากระบบเดิมดูอย่างเดียว** — หัวหน้ากรอกยอดไปแล้วที่ ISURVEY
  และยอดถูกเก็บที่ se-billing ถ้าแก้ที่นี่ได้ด้วยจะมียอด 2 ชุดที่ไม่มีใครรู้ว่าชุดไหนถูก
  **งานที่เกิดในระบบ se-survey** กรอกได้ตามปกติ (หัวหน้า/แอดมินเท่านั้น — ผู้สำรวจ
  ตั้งยอดจ่ายตัวเองไม่ได้ทั้งสองทาง แอปมือถือไม่มีหน้าค่าใช้จ่ายตั้งแต่แรก)
  · หัวตารางบอกที่มาให้เห็นเลยว่ายอดฝั่งนี้แก้ได้หรือดูอย่างเดียว
· ⚠️ ต้องรัน `039_approval_gate.sql` บน production ด้วยมือ

### แก้บั๊ก — นำเข้าทะเบียนพนักงาน: ชื่อเพี้ยน · เสนอปิดบัญชีหัวหน้าตัวเอง
เจอตอนนำเข้าไฟล์ของฝ่ายบุคคลรอบจริง 2 เรื่อง แก้ทั้งคู่แล้ว
· ชื่อบางคนมีอักขระล่องหนติดมาจากการก๊อปวาง (ในเอ็กเซลมองไม่เห็นเลย) ทำให้ตัดคำนำหน้า
  ไม่ออก บัญชีที่สร้างจะได้ชื่อ-นามสกุลเลื่อนไปทั้งคน — ตอนนี้ล้างก่อนอ่านทุกครั้ง
· คนที่ **เลื่อนเป็นหัวหน้า** ย่อมหลุดจากรายชื่อลูกทีมเป็นธรรมดา แต่ระบบนับว่า "ออกแล้ว"
  แล้วเสนอให้ปิดบัญชี · ถ้ากดตามที่เสนอ หัวหน้าจะล็อกอินไม่ได้ทั้งที่ยังทำงานอยู่
  ตอนนี้ข้ามคนที่ชื่อไปโผล่เป็นหัวหน้าในไฟล์เดียวกัน
· เพิ่มชุดทดสอบตัวอ่านไฟล์ ใช้เคสที่เจอจริงเป็นตัวตั้ง (`npm test` ในโฟลเดอร์ backend)

### แก้บั๊ก — เครื่องหมาย + หายตอนบอทบันทึกลงระบบประกัน
ระบบประกันกลืนเครื่องหมายบวกตอนกดบันทึก (ฟอร์มส่งข้อมูลด้วยรูปแบบที่ "+" แปลว่าเว้นวรรค
แล้วฝั่งเขาถอดรหัสซ้ำอีกรอบ) — พิมพ์ "ประเภท 2+" กลายเป็น "ประเภท 2 " เงียบ ๆ
ทดสอบยืนยันบนหน้าจริง อักขระอื่น (`:` `/` `(` `)` `.` `-`) รอดหมด มีแต่ + ที่หาย
· แก้ที่ระบบเขาไม่ได้ → บอทแปลง + เป็นคำว่า "พลัส" ก่อนพิมพ์ เฉพาะ 4 ช่องข้อความอิสระ
  (รายละเอียดการเกิดเหตุ · ผลการดำเนินงาน · ความเห็นผู้ตรวจสอบ · ความเห็นเซอร์เวย์)
· ข้อมูลต้นทางไม่เปลี่ยน คนเปิดดูบนเว็บ/แอปยังเห็น "2+" เหมือนเดิม
· **15 ส.ค. เพิ่มช่องที่ 5 — "ประเภทกรมธรรม์"** (ทั้งของรถประกันและของคู่กรณี) เจอตอน
  อ่านเคสจริง: ช่องนี้รับค่า "ประเภท 2+" ตรง ๆ ถ้า + หาย จะเหลือ "ประเภท 2"
  ซึ่ง**ผิดความคุ้มครอง** ไม่ใช่แค่อักขระหายเหมือน 4 ช่องแรก

### แก้บั๊ก — "บันทึกความเห็นหัวหน้างาน" จากระบบเก่า ลงผิดช่องในระบบประกัน
เดิมบอทเอาไปใส่ช่อง "ผลการดำเนินงาน" ซึ่งผิดความหมาย · ย้ายไปช่อง "ความเห็นของผู้ตรวจสอบ"
ส่วนผลการดำเนินงานปล่อยว่างให้คนกรอกเอง (ระบบเก่ามีช่องความเห็นหัวหน้าช่องเดียว
เลือกปลายทางได้ทางเดียว) · แก้ที่ตัวอ่านทั้ง 2 เส้นทางที่บอทใช้ ไม่ใช่ที่ปลายทาง
เพราะงานจากแอปเราส่ง "ผลการดำเนินงาน" ของจริงมาทางช่องเดียวกัน สลับปลายทางแล้วจะพังอีกเส้น

### เพิ่ม — หน้า "ทะเบียนพนักงานสำรวจ" + นำเข้าจากไฟล์ Excel ของฝ่ายบุคคล
เดิมทะเบียนพนักงานไม่มีที่อยู่จริงในระบบ — เบอร์โทรมีอยู่ 1 คนจาก 144 ทำให้ช่อง
"โทรศัพท์ผู้สำรวจภัย" ที่ระบบประกันบังคับ ต้องพิมพ์เองทุกเคส
· หน้าใหม่ที่เมนูแอดมิน: รหัส · ชื่อ · เบอร์ · หัวหน้า · สถานะ ค้นหาได้ บอกจำนวนคนที่ยังไม่มีเบอร์
· อัปโหลดไฟล์ Excel ที่ฝ่ายบุคคลทำอยู่ได้ตรง ๆ ไม่ต้องแปลงไฟล์
· **ดูแผนก่อนเสมอ** ระบบบอกว่าจะเติมเบอร์ใคร แก้ชื่อใคร สร้างใหม่กี่คน ปิดใช้งานกี่คน
  แล้วเลือกติ๊กเองว่าจะให้ทำหมวดไหน — ไม่ทำอะไรจนกว่าจะกดยืนยัน
· คนที่หายจากไฟล์ = **ปิดใช้งาน ไม่ลบทิ้ง** (ยังมีเคสกับประวัติการจ่ายเงินผูกอยู่)
· บัญชีใหม่ต้องกำหนดรหัสผ่านเริ่มต้นเอง ระบบไม่ตั้งให้
· **นำเข้าจริงครบแล้ว 14 ส.ค.** — เบอร์เข้าระบบ 133 คน (เดิมมี 1 คน) · สร้างบัญชีใหม่
  14 คน · ปิดบัญชีคนที่ออกแล้ว 6 คน · รวมผู้สำรวจที่เปิดใช้งานอยู่ 152 คน
  เหลือคนเดียวที่ยังไม่มีเบอร์ (SEC313 — ไฟล์ฝ่ายบุคคลเว้นว่างไว้เอง)
· รหัสผ่านบัญชีใหม่ตั้งเป็น **เลข 3 ตัวท้ายของรหัสพนักงาน** (SE483 → `483`) ตามที่ตกลงกัน
  ⚠️ อ่อนมากโดยตั้งใจ ใช้ช่วงตั้งระบบเท่านั้น — ก่อนเปิดใช้จริงต้องบังคับเปลี่ยนรหัส
· แก้ไฟล์ฝ่ายบุคคลให้ตรงกับความจริง 2 จุด: SE478 นามสกุล "ยืนสุข" (ไฟล์พิมพ์ ยีนสุข)
  และ SE175 → **SE170** (คนเดียวกัน คนละรหัส) ถ้าไม่แก้จะได้บัญชีซ้ำอีกใบ
  แล้วบัญชีเดิมโดนปิดทิ้ง · ไฟล์ต้นฉบับก่อนแก้ดูได้จากประวัติ git ของคอมมิตนี้

### ปรับ — ช่องบังคับที่ยังว่าง เพิ่มพื้นหลังแดง + ครอบถึงบล็อกคู่กรณี
เดิมมีแต่กรอบแดง มองข้ามง่ายเมื่ออยู่ปนกับช่องอื่นเต็มหน้า · และบล็อกคู่กรณี/ผู้บาดเจ็บ/
ทรัพย์สินไม่ได้สีเลยเพราะใช้คนละตัวสร้างฟอร์ม ตอนนี้ได้ทั้งกรอบและพื้นหลังเหมือนกันหมด
(ไม่รวมช่องที่บังคับเฉพาะบางบริษัท — ทาแดงไว้จะเป็นเตือนหลอกทุกเคส)

### ปรับ — ตารางค่าใช้จ่าย สลับคอลัมน์ให้ฝั่งพนักงานมาก่อน
"ราคา/หน่วย" เปลี่ยนชื่อเป็น **"ราคาประกัน"** ให้ตรงกับความหมาย (ยอดที่เรียกเก็บบริษัทประกัน)
และย้ายไปอยู่หลัง "ราคาพนักงาน" ทุกแถว รวมแถวหักเงิน

### เพิ่ม — หน้าตรวจงาน: ช่องบังคับที่ยังว่างขึ้นกรอบแดง + สรุปจำนวนไว้บนสุด
ดอกจันบอกว่าช่องไหน "บังคับ" แต่ไม่ได้บอกว่าช่องไหน "ยังไม่ได้กรอก" — หน้านี้มีช่องบังคับ
~49 ช่องกระจายหลายหมวด ต้องเลื่อนหาเอง แล้วไปรู้ตอนบอทนำเข้าระบบประกันไม่ผ่าน
· ตอนนี้ช่องบังคับที่ว่างขึ้น**กรอบแดง** พิมพ์ปุ๊บกรอบหายทันที ไม่ต้องรอกดบันทึก
· บนสุดมีแถบสรุป "ยังขาดช่องบังคับ N ช่อง" พร้อมปุ่มกระโดดไปช่องแรกที่ขาด
  ครบแล้วเปลี่ยนเป็นแถบเขียว "ช่องบังคับครบแล้ว"

### แก้บั๊ก — นำเข้า XML แล้ว "ผลการดำเนินงาน" กับ "ความเห็นของผู้ตรวจสอบ" หายไป
ฝั่งส่งออกเขียนความเห็นครบ 3 ช่อง แต่ฝั่งนำเข้าอ่านกลับแค่ช่องเดียว (ความเห็นเซอร์เวย์)
→ ไฟล์ที่ระบบเรา gen เองแล้วนำกลับเข้ามา (โหมดกู้/ซ่อมของบอท) ทำ 2 ช่องนี้หายทุกครั้ง
· **ไฟล์ของระบบเก่าจริงทั้ง 8 ใบที่มีอยู่ ปล่อย 2 ช่องนี้ว่างหมด** แก้แล้วช่องก็ยังว่าง
  เพราะต้นทางไม่ได้ส่งมา — ผู้ตรวจต้องพิมพ์เองบนเว็บเหมือนเดิม

### เพิ่ม — นำเข้า XML: มีแถบความคืบหน้าตอนอัปโหลด
zip รูปทั้งเคสหลายสิบ MB ระหว่างส่งหน้าจอนิ่งสนิทไม่มีอะไรขยับ ผู้ใช้นึกว่าเบราว์เซอร์ค้าง
แล้วกดซ้ำหรือปิดหน้าไปกลางคัน · ตอนนี้บอกเป็น % พร้อมขนาดที่ส่งไปแล้ว/ทั้งหมด
และพอส่งครบเปลี่ยนเป็น "กำลังแตกไฟล์รูปและสร้างเคส · อย่าปิดหน้านี้" แถบวิ่งไปมา
(ช่วงนี้เซิร์ฟเวอร์ยังทำงานอยู่ ค้างที่ 100% เฉย ๆ จะดูเหมือนแฮงก์)

### เพิ่ม — นำเข้า XML: เลือกบริษัทประกันให้เองจากเลขเซอร์เวย์ในไฟล์
ไฟล์ของระบบเก่าไม่มีชื่อบริษัทมาด้วย ต้องเลือกเองทุกครั้ง — **เลือกผิดทีเดียวเคสไปโผล่
ผิดบริษัทในระบบประกันแล้วลบไม่ได้** · ตอนนี้พอเลือกไฟล์ ระบบอ่านเลขเซอร์เวย์แล้วเติมให้
(`SETP-` = ไทยไพบูลย์ · `SEABI-` = ไอโออิ) พร้อมบอกว่าอ่านจากอะไร กดเปลี่ยนเองได้ตลอด
· เลือกสวนกับที่อ่านได้ → ถามยืนยันอีกครั้งก่อนนำเข้า
· คำนำหน้าที่ไม่รู้จัก → ไม่เดา ปล่อยให้เลือกเองเหมือนเดิม
· ตรวจกับไฟล์จริง 8 ใบ ถูกทั้งหมด
· แก้ระหว่างเทส: เปลี่ยนไฟล์โดยไม่รีเฟรชหน้า แล้วไฟล์ใหม่อ่านบริษัทไม่ออก ช่องเคย
  **ค้างบริษัทของไฟล์เก่า** ทั้งที่ข้อความบอกว่าต้องเลือกเอง → กดนำเข้าต่อได้โดยไม่มีอะไรทัก

### ปรับ — รายชื่อบริษัทประกันเหลือเฉพาะที่รับงานจริง (ไทยไพบูลย์ · ไอโออิ)
หน้า "นำเข้าจากไฟล์ XML" เคยมีให้เลือก 6 บริษัท และหน้าตรวจงานมี 7 (ลอกมาจากระบบประกัน)
ทั้งที่อีก 4-5 บริษัทไม่เคยมีงานเข้าสักเคส · **เลือกผิดทีเดียว เคสไปโผล่ผิดบริษัทในระบบประกัน
แล้วลบไม่ได้** เพราะเรื่องที่สร้างค้างไว้ในระบบเขาลบไม่ได้ · เคสเก่าที่ชื่อบริษัทเขียนไม่ตรง
รายการยังเก็บค่าเดิมไว้ ไม่หายตอนกดบันทึก

### ปรับ — "ประเภทกรมธรรม์" พิมพ์เองได้แล้ว เลิกเป็นรายการให้เลือก (แอป 1.0.54+56)
ระบบประกันเองก็ไม่มีรายการให้เลือกช่องนี้ — ของรถประกันเป็นป้ายอ่านอย่างเดียว
ของคู่กรณีเป็นช่องพิมพ์ธรรมดา · ของจริงมีนอกรายการ 7 ตัวที่เราตั้งไว้:
ใบแจ้งความเสียหายจริงเขียน **"ประเภท 2+ ซ่อมอู่"** คู่กับรหัส **52** ในไฟล์ใบเดียวกัน
เคสที่นำเข้าจึงเคยค้างเป็น "52 (ค่าเดิม)" เลือกไม่ได้ ต้องเลือกใหม่เองทุกเคส
· ตอนส่งเข้าระบบประกันแปลงให้เฉพาะป้ายที่**ยืนยันรหัสแล้วจากใบจริง** (ชั้น 1→01 · ชั้น 2+→52)
  ที่เหลือส่งตามที่กรอก **ไม่ตัดตัวเลขออกมาเองแล้ว** จึงไม่มีค่าหายเงียบเหมือนเดิม
  · ไม่เดารหัสของ "ชั้น 3+" — ส่งรหัสผิดเงียบ ๆ แย่กว่าส่งข้อความตามที่คนกรอก
· แก้พร้อมกัน 3 ที่: แอป (รถประกัน+คู่กรณี) · หน้าตรวจงาน (รถประกัน+คู่กรณี) · ไฟล์ส่งออก

### ปรับ — เคสที่นำเข้าจากไฟล์ระบบเก่า เหลือช่องบังคับให้กรอกเองน้อยลง
"คู่กรณีคันที่" เติมให้เองเมื่อผลคดีเป็นคู่กรณีผิดและมีคู่กรณีคันเดียว (คำตอบมีทางเดียว)
· **มากกว่า 1 คันยังต้องเลือกเอง** — ไฟล์ไม่มีอะไรบอกว่าคันไหนผิด เดาไม่ได้
· "เบอร์ผู้สำรวจภัย" หยิบจากทะเบียนพนักงานตามรหัสที่จับได้จากไฟล์
(ตอนนี้ทะเบียนมีเบอร์อยู่ 1 จาก 144 คน — ต้องกรอกเบอร์เข้าทะเบียนก่อนถึงจะได้ผลจริง)

### ปรับ — เลิกก๊อป "ความเห็นเซอร์เวย์" ลงช่องหมายเหตุตอนนำเข้า
เคสที่นำเข้าจากไฟล์ระบบเก่ามีข้อความยาวเท่ากันเป๊ะอยู่ 2 ที่ (วัดได้ 1,281 ตัวอักษรทั้งคู่)
ช่องหมายเหตุเป็นที่ที่คนของเราเขียนเอง ไม่ใช่สำเนาความเห็นที่ติดมากับไฟล์ของบริษัทประกัน

### แก้บั๊ก — เคสที่นำเข้าจากไฟล์ระบบเก่า ขึ้น "ต้องกรอกชื่อผู้ขับขี่" ทั้งที่มีชื่ออยู่แล้ว
ไฟล์ระบบเก่าส่งชื่อผู้ขับขี่รถประกันมาเป็นก้อนเดียว ระบบเก็บลงช่อง "ชื่อเต็ม" แต่หน้าตรวจงาน
กับแอปใช้ช่อง **ชื่อ** และ **นามสกุล** แยกกัน → สองช่องนั้นว่าง ติดดอกจันแดงค้างทุกเคสที่นำเข้า
ผู้ตรวจต้องพิมพ์ชื่อซ้ำเองทั้งที่ข้อมูลมาครบ · ตอนนี้แยกให้อัตโนมัติเหมือนที่ทำกับคู่กรณีอยู่แล้ว
(ตัดคำนำหน้าออกก่อน) · ไฟล์ที่ส่งเข้าระบบประกันได้ชื่อเดิมไม่เปลี่ยน

### เพิ่ม — การ์ดคู่กรณีโชว์ครบขึ้น (เพศ · วันเกิด · ประเภทใบขับขี่ · เลขตัวถัง)
วันเกิดเป็นช่องบังคับของระบบประกัน แต่การ์ดคู่กรณีตอนยังไม่กดแก้ไขกลับไม่แสดงเลย
ผู้ตรวจจึงไม่เห็นว่าขาดจนกว่าจะไปติดตอนนำเข้า

### ปรับ — รวมรายการตัวเลือกของแอปไว้ที่เดียว (แอป 1.0.53+55)
ลิสต์ที่ลอกมาจากระบบประกัน (ประเภทประกัน · คำนำหน้า · ความสัมพันธ์ 39 รายการ · ประเภทรถ ·
ประเภทรถไฟฟ้า) ถูกประกาศซ้ำในหน้าฟอร์มอีกชุด — แก้ที่ไฟล์กลางแล้ว**ไม่มีผลกับหน้าฟอร์ม**
เนื้อหายังตรงกันดีอยู่ แต่เป็นระเบิดเวลาแบบเดียวกับที่เคยทำให้รถประกันกับคู่กรณีเก็บค่าคนละแบบ
· ตรวจแล้วทั้ง 3 ลิสต์ตรงกันเป๊ะก่อนรวม (39/7/6 รายการ) ไม่มีค่าไหนเปลี่ยน

### แก้บั๊ก — เวลาแจ้งความ/เวลานัดหมาย หายทุกครั้งที่ผู้ตรวจกดบันทึก
ช่างกรอกเวลามาจากหน้างาน แต่หน้าตรวจงานไม่เคยดึงเวลามาแสดงในช่องชั่วโมง/นาที (ขึ้นว่างเสมอ)
พอกดบันทึกครั้งเดียว **เวลาถูกล้างทิ้งทั้งคู่** เหลือแต่วันที่ แล้วไหลต่อเป็นเวลาว่างเข้าระบบประกัน

### เพิ่ม — ระบบเติม "วันเวลาสำรวจเสร็จ" ให้เองตอนกดส่งงาน
ระบบประกันบังคับช่องนี้เสมอ แต่ไม่มีใครในระบบเรากรอก — ไฟล์ที่ส่งไปจึงมีช่องว่างและ
**บันทึกหน้าหลักไม่ผ่าน** (เรื่องถูกสร้างแล้วแต่ค้างกลางทาง) · ตอนนี้ระบบจับเวลาไทยให้ตอนช่างกดส่ง
ไม่ทับของเดิมถ้ามีอยู่แล้ว · หน้าตรวจงานติดดาวช่องนี้และไทม์ไลน์อีก 3 ช่องให้เห็นด้วย

### แก้บั๊ก — "ผลคดี" ที่ผู้ตรวจแก้บนเว็บ ทำให้แอปเลิกตรวจช่องบังคับ
เว็บกับแอปเก็บผลคดีคนละสำเนียง (เว็บเก็บป้ายเต็ม แอปเก็บค่าสั้น) พอผู้ตรวจแก้บนเว็บ
ตรรกะในแอปที่เทียบตรง ๆ ก็เลิกทำงาน → **เลิกบังคับ "คู่กรณีคันที่" กับ "การเรียกร้องค่าเสียหาย"**
ทั้งที่ระบบประกันยังบังคับ · ตอนนี้เว็บเก็บค่าเดียวกับแอป และแอปรองรับของเก่าที่ค้างอยู่ใน DB ด้วย

### แก้บั๊ก — ช่องที่พิมพ์อิสระแล้วค่าหายเงียบ (ประเภทประกัน · ประเภทรถไฟฟ้า)
ปลายทางอ่านเฉพาะรูปแบบที่กำหนด — พิมพ์ "ชั้นหนึ่ง" แทน "ชั้น 1" ได้ค่าว่างโดยไม่มีอะไรเตือน
· เปลี่ยนเป็นตัวเลือกที่ตรงกับระบบประกัน ทั้งของรถประกันและของคู่กรณี
· ถอดช่อง "สาขาผู้สำรวจ" ที่มีตัวเลือกเดียวและเขียนค่าว่างทับทุกครั้งออก

### เพิ่ม — ช่องบังคับของคู่กรณีครบตามระบบประกัน (แอป 1.0.52+54)
ระบบประกันบังคับ 8 ช่องต่อคัน แต่แอปเช็คแค่ 7 และ**ไม่มี "เจ้าของรถ"** — บอทเลยต้องใส่ `-` แทน
ทุกใบ สำนวนคู่กรณีของบริษัทประกันจึงได้ชื่อเจ้าของรถเป็นขีดกลางมาตลอด
- เพิ่ม **เจ้าของรถ** เข้าเงื่อนไขทั้งในแอปและหน้าแก้คู่กรณี
- **เวลาเกิดเหตุ** (ชั่วโมง/นาที) เดิมส่งงานได้ทั้งที่เว้นว่าง — ระบบประกันบังคับ
- **เลข พ.ร.บ.** เมื่อเปิดสวิตช์ "มี พ.ร.บ." — เดิมมีจุดแดงแต่ไม่มีใครตรวจ และเลขว่าง
  ทำให้ติ๊ก "มี พ.ร.บ." หายทั้งใบ
- **เลขทะเบียนรถของผู้บาดเจ็บ** (ยกเว้นบุคคลภายนอกรถ) — เดิมตรวจแค่ตอนกดบันทึกรายคน
  ระเบียนที่มาจากงานค้าง/นำเข้าไฟล์เลยหลุด
- **ถอดจุดแดงของ "ยี่ห้อรถ"** — ระบบประกันไม่ได้บังคับ และไม่เคยมีใครตรวจ = จุดแดงลอย

### แก้บั๊ก — บอทยังเอา "หมายเหตุ" ไปใส่ช่องความเห็นของเซอร์เวย์
เมื่อวานแก้ฝั่งไฟล์ XML ไปแล้วว่าหมายเหตุเป็นโน้ตภายใน ห้ามส่งเข้าระบบประกัน แต่ฝั่งบอท
ยังใช้ของเดิมอยู่ → เคสที่มีแต่หมายเหตุ ข้อความภายในจะถูกพิมพ์ลงสำนวนประกันแบบเงียบ ๆ
· และเติม 3 ช่องที่บอทลืมกรอกในเส้นทางงานมือถือ (อีเมลผู้เอาประกัน · ค่าเสียหายส่วนแรก · ลำดับผู้ขับขี่)

### เพิ่ม — แก้ข้อมูลคู่กรณีบนหน้าตรวจงานได้แล้ว (เดิมดูได้อย่างเดียว)
คู่กรณีเป็นบล็อกเดียวที่ผู้ตรวจแก้ไม่ได้เลย ทั้งที่ระบบประกันบังคับ 7 ช่องต่อคัน และ 4 ใน 7
(ประเภทรถ · เพศ · วันเกิด · อายุ) เป็นตัวเลือก/วันที่/ตัวเลข ที่ใส่ "-" แทนไม่ได้ — ขาดแล้ว
บอทค้างกลางทาง ผู้ตรวจได้แต่โทรตามให้ช่างแก้จากแอป
- แก้ได้ 25 ช่อง · เพิ่ม/ลบคันได้ · ติ๊ก KFK ได้ · **แก้ค้างไว้ได้จนกว่าจะกดบันทึก** เหมือนบล็อกอื่น
- ป้ายเตือน "ยังขาด N ช่องบังคับ" อัปเดตสด ๆ ระหว่างพิมพ์ กรอกครบแล้วป้ายหายเอง
- ตัวเลือกยี่ห้อรถเปลี่ยนตามประเภทรถ · เขต/อำเภอเปลี่ยนตามจังหวัด เหมือนบล็อกรถประกัน
- **รายการความเสียหายของคู่กรณียังแก้ได้เฉพาะในแอป** — หน้าเว็บแสดงจำนวนไว้เฉย ๆ
  แต่รับประกันว่าไม่หายตอนบันทึก

### เพิ่ม — ดอกจันช่องบังคับบนหน้าตรวจงาน (เดิมมีอยู่ช่องเดียวจาก 31 ช่อง)
ผู้สำรวจเห็นจุดแดงบนแอปมาตลอด แต่หน้าตรวจงานไม่เคยมี — ผู้ตรวจแก้ข้อมูลแล้วเผลอลบ
ช่องบังคับทิ้งได้โดยไม่มีอะไรเตือน กว่าจะรู้ก็ตอนส่งเข้าระบบประกันไม่ผ่าน
- **แดง 25 ช่อง** = บังคับเสมอ · **ส้ม 6 ช่อง** = บังคับเฉพาะบางกรณี (ชี้เมาส์ดูเงื่อนไขได้)
  เช่น เลขใบขับขี่ (เมื่อมีใบขับขี่) · คู่กรณีคันที่ + การเรียกร้อง (เมื่อคู่กรณีผิด) ·
  ชื่อพนักงานสอบสวน + สถานีตำรวจ (เมื่อรอสรุปผลคดี/มีการแจ้งความ)
- ผู้บาดเจ็บ/ทรัพย์สินติดดาว**รายคน-รายชิ้น** (เดิมมีดาวช่องเดียว) — ขาดช่องเดียว
  บันทึกทั้งบล็อกไม่ผ่าน ไม่ใช่แค่ช่องนั้น
- การ์ดคู่กรณีขึ้นเตือนว่า**คันไหนขาดช่องบังคับอะไรบ้าง** — 4 ใน 7 ช่องเป็นตัวเลือก/วันที่/ตัวเลข
  ที่ใส่ "-" แทนไม่ได้ ขาดแล้วบอทค้างกลางทาง · ⚠️ หน้านี้ยัง**แก้คู่กรณีไม่ได้** ต้องให้ผู้สำรวจ
  แก้จากแอป (ทำหน้าแก้คู่กรณีเป็นงานแยก)
- ลิสต์ทั้งหมดยกมาจากแอปมือถือซึ่ง sync กับตัวตรวจของระบบประกันอยู่แล้ว — ที่เดียวกัน ไม่แยกกติกา

### เพิ่ม — 5 ช่องที่แอปเก็บมาแล้วแต่หน้าตรวจงานไม่เคยแสดง
ข้อมูลอยู่ในระบบครบ แต่ผู้ตรวจมองไม่เห็นและแก้ไม่ได้
- **รถไฟฟ้า 3 ช่อง**: หมายเลขแบตเตอรี่ · วันเริ่มใช้แบตเตอรี่ · หมายเลขเครื่องชาร์จ
- **สัญชาติผู้ถือบัตร** (คนไทย/ต่างชาติ) — ตัวกำหนดว่าเลขบัตรต้องผ่านการตรวจ 13 หลักหรือไม่
- **หมายเหตุเพิ่มเติม** ที่ผู้สำรวจจดจากหน้างาน (เป็นโน้ตภายใน ไม่ส่งเข้าระบบประกัน)

### เพิ่ม — 3 ช่องความเห็นที่หัวหน้าเขียนบนเว็บ ส่งไปกับไฟล์ XML แล้ว
"ผลการดำเนินงาน" · "ความเห็นของผู้ตรวจสอบ" · "ความเห็นของเซอร์เวย์" — มีช่องให้กรอกบนหน้า
ตรวจงานมานานแล้ว แต่ไฟล์ที่ส่งออกไปกลับส่งค่าว่างเสมอ หัวหน้าต้องไปพิมพ์ซ้ำอีกรอบในระบบประกัน
· ตอนนี้กรอกที่เดียวจบ
- **ไม่มีข้อมูล = ส่งช่องว่าง** ซึ่งฝั่งบอทข้ามให้เอง **ไม่ไปลบของเดิม**ที่หัวหน้าเคยพิมพ์ไว้ในระบบประกัน
- **คงการขึ้นบรรทัดใหม่** — งานจริงเขียนเป็น bullet หลายบรรทัด ยุบบรรทัดแล้วอ่านไม่ออก
- "รายละเอียดการเกิดเหตุ" เป็นคนละช่อง (อยู่แท็บข้อมูลทั่วไป) — ต่อสายไว้อยู่แล้ว ไม่ได้แก้อะไร
- **"หมายเหตุเพิ่มเติม" ที่ผู้สำรวจจดในแอป จะไม่ถูกเอาไปใช้เป็น "ความเห็นของเซอร์เวย์" อีกแล้ว** —
  คนละเรื่องกัน อันหนึ่งเป็นโน้ตส่วนตัว อีกอันเป็นข้อความทางการบนสำนวนของบริษัทประกัน
  · ไม่ได้กรอกช่องความเห็น = ส่งว่างไปเลย ไม่หยิบของอื่นมาแทน

### แก้บั๊ก — "ความเห็นของเซอร์เวย์" ส่งออกไป 2 ที่ ค่าไม่ตรงกัน
ไฟล์มีช่องนี้อยู่ 2 จุดตามรูปแบบที่ระบบประกันกำหนด (ไฟล์ของระบบเก่าก็มี 2 จุดเหมือนกัน)
แต่ของเราจุดหนึ่งใส่ค่า อีกจุดปล่อยว่าง — **ผลที่ได้จึงขึ้นอยู่กับว่าปลายทางอ่านจุดไหน**
ซึ่งไม่มีใครรู้ · แก้เป็นค่าเดียวกันทั้ง 2 จุด ได้ผลเหมือนกันไม่ว่าอ่านทางไหน

### เพิ่ม — งานจากแอปมือถือส่งยอด "เรียกเก็บประกัน" ไปกับไฟล์ XML ได้แล้ว
เดิมยอดค่าใช้จ่ายจะติดไปกับไฟล์**เฉพาะงานที่นำเข้าจากระบบเก่า**เท่านั้น งานจากแอปมือถือ
ต่อให้ผู้ตรวจกรอกราคาไว้บนเว็บ ไฟล์ก็ออกไปเป็น 0.00 ทุกช่องแบบเงียบ ๆ ไม่มีอะไรฟ้อง
— กติกานั้นมาจากตอนที่หัวหน้ายังกรอกราคาในระบบประกันเอง แต่ตอนนี้กรอกบนเว็บเราแล้ว
- ช่อง "ราคา/หน่วย · เรียกเก็บประกัน" บนหน้าตรวจงาน**มีผลจริงกับไฟล์แล้ว**
- งานนำเข้าจากระบบเก่ายังทำงานเหมือนเดิมทุกประการ
- **ไฟล์ที่สกัดกลับมาจากระบบประกัน (ข้อมูลทดสอบ) ยังถูกกันไว้เหมือนเดิม** — ส่งกลับเข้าไป
  เท่ากับเอาตัวเลขของบริษัทประกันเองยัดกลับไปเป็นบิลใบใหม่
- ที่มาแบบใหม่ที่ยังไม่มีในระบบจะถูกกันไว้ก่อนโดยอัตโนมัติ (ได้ 0.00) ไม่ใช่ปล่อยผ่าน

### แก้บั๊ก — ล้างช่องค่าใช้จ่ายทิ้งแล้วยอดเดิมไม่หาย (ยังติดไปกับไฟล์อยู่)
กรอกราคาไว้แล้วเปลี่ยนใจ ลบตัวเลขออกให้หมดทุกช่องแล้วกดบันทึก — **ระบบไม่ทำอะไรเลย**
ยอดเดิมยังอยู่ในฐานข้อมูลและยังติดไปกับไฟล์ที่ส่งเข้าระบบประกัน ทั้งที่หน้าจอว่างแล้ว
· การล้างช่องเป็นวิธีเดียวที่หน้าเว็บมีให้ลบบิล จึงเป็นวิธีที่ห้ามถูกเมิน
· บันทึกฟอร์มอื่นที่ไม่มีช่องค่าใช้จ่าย (ส่งงานจากมือถือ / หน้ารีวิว) ยังไม่แตะยอดเดิมเหมือนเดิม

### เพิ่ม — เทสคุมตารางค่าใช้จ่ายในไฟล์ XML (เดิมไม่มีเลยสักตัว)
ยอดฝั่ง**จ่ายพนักงาน**มีเทสคุมอยู่แล้ว แต่ยอดฝั่ง**เรียกเก็บประกัน** ซึ่งเป็นตัวเลขที่วิ่ง
เข้าระบบของบริษัทประกันจริง กลับไม่มีอะไรคุม · เพิ่ม 2 ชุด: ไม่มียอด → ต้องเป็น 0.00 ทุกช่อง
(ไม่ใช่หายไปทั้งบล็อก) · มียอดครบ → ต้องออกตรงทุกช่อง
- **บวกการ์ดกันเงินสองฝั่งปนกัน** — ตารางยอดจ่ายพนักงานกับยอดเรียกเก็บประกันมีชื่อช่อง
  ซ้ำกันเป๊ะ 2 ตัว (ค่าโทรศัพท์ · ค่าประกันตัว) ซึ่งเป็นช่องที่ไปโผล่ในไฟล์พอดี
  ถ้าวันหน้ามีใครเผลอต่อสายผิดตาราง เงินเดือนพนักงานจะกลายเป็นบิลเรียกเก็บบริษัทประกัน
  **โดยไม่มี error อะไรเลย** — เทสตัวนี้คือด่านเดียวที่จับได้

### เพิ่ม — หน้า "เรทค่าตอบแทน" ของผู้ดูแลระบบ (แก้เรทเองได้ ไม่ต้องพึ่งคนเขียนโปรแกรม)
เดิมเรทอยู่ในฐานข้อมูลของระบบเก่าและ**ไม่มีหน้าจอให้แก้เลย** ใครจะเปลี่ยนต้องให้คนเขียน
โปรแกรมเข้าไปแก้ให้ ตอนนี้อยู่ในเมนูผู้ดูแลระบบ แบ่งเป็น 6 แท็บ
- **เรทรายอำเภอ** (337 อำเภอ) — กรองตามจังหวัด/ค้นหาชื่อได้ · เลือกจังหวัดหรือค้นหาแล้วจะเห็น
  **อำเภอที่ยังไม่มีเรท**ด้วย กดแก้เพื่อเพิ่มเรทให้พื้นที่ให้บริการใหม่ได้ทันที
- **เรทรายจังหวัด** (ตัวสำรองเมื่ออำเภอไม่มีเรทของตัวเอง) · **ตำบลพิเศษ** · **ทีมผู้สำรวจ**
- **ค่าคงที่** — ค่านอกพื้นที่/นอกเวลา และเรทที่เดิม**ฝังอยู่ในโค้ดส่วนขยาย** ซึ่งแก้ทีต้องสร้างใหม่
  แล้วแจกทุกเครื่อง
- **แยกสีให้เห็นว่าช่องไหนคือเงินพนักงาน ช่องไหนคือเงินที่เรียกเก็บประกัน** — เงินสองฝั่งนี้
  หน้าตาเหมือนกันแต่คนละเรื่อง กรอกสลับกันแล้วจ่ายผิดโดยไม่มีอะไรฟ้อง
- **เว้นว่าง ≠ ใส่ 0** — เว้นว่างคือไม่มีเรท (ไปหาจากระดับที่กว้างกว่า) ส่วน 0 คือมีเรทและเป็นศูนย์
- เตือนเมื่อมี**ผู้สำรวจรหัส SEC ที่ยังไม่ได้จับคู่ทีม** (คนกลุ่มนี้จะได้เรทกลางของอำเภอเสมอ
  ต่อให้อำเภอนั้นตั้งเรทรายทีมไว้) · รหัส SE ของกรุงเทพฯ ไม่นับเพราะใช้เรทกลางอยู่แล้ว
- แถบเตือนบนหัวหน้า: **เรทชุดนี้เป็นของ se-survey แยกต่างหาก** แก้ที่นี่แล้วระบบเก่าไม่เปลี่ยนตาม

### เพิ่ม — ประวัติการแก้เรท (ใครแก้ช่องไหน จากเท่าไหร่เป็นเท่าไหร่)
เรทคือเงินของพนักงาน แก้ผิดช่องเดียว = จ่ายผิดทั้งอำเภอโดยไม่มีใครรู้ · พอเปิดให้แก้ผ่าน
หน้าเว็บได้ ความเสี่ยงก็ขยับขึ้นทันที ทุกการแก้จึงถูกบันทึกไว้ดูย้อนหลังได้ในแท็บของมันเอง
**ไม่ใช่ระบบเวอร์ชันเรท** — ตัวคิดเงินยังใช้ค่าปัจจุบันค่าเดียวเหมือนเดิม แก้แล้วทับตามที่ตกลงกันไว้
- ⚠️ ต้องรัน migration `038_billing_rate_changes.sql` บน production (รันแล้ว)

### เพิ่ม — ใบเบิกเงินค่าตอบแทนผู้สำรวจ (ดาวน์โหลด Excel จากหน้ารายการงานตรวจสอบ)
เลือกช่วงวันที่แล้วกดปุ่มเดียว ได้ไฟล์ Excel เอาไปทำเรื่องเบิกจ่ายได้เลย
- **คอลัมน์เรียงตามใบเดิมที่ใช้กันอยู่** — คนทำเรื่องเบิกเงินอ่านใบนี้มาตลอด เปลี่ยนลำดับ
  หรือชื่อคอลัมน์แล้วต้องมาไล่หาใหม่ทุกเดือน
- แยกให้เห็นทั้ง **ฝั่งเรียกเก็บประกัน** และ **ฝั่งจ่ายพนักงาน** ในแถวเดียวกัน
  พร้อมค่านอกพื้นที่/นอกเวลา/หักเงิน/เหตุผล แยกคอลัมน์
- หักเงินแสดงเป็นเลขติดลบ · มีแถวรวมยอดท้ายตาราง · ตรึงหัวตารางไว้ตอนเลื่อน
- กรองตามวันที่คิดเงิน **เทียบเป็นวันตามเวลาไทย** ไม่ใช่ UTC — ไม่งั้นงานตอนเย็นตกไปอยู่วันถัดไป
- ครอบคลุมเฉพาะงานที่คิดเงินผ่านระบบนี้ · งานที่ยังทำผ่านระบบเก่ายังออกใบจากที่เดิม

### แก้บั๊ก — ติ๊ก "นอกพื้นที่/นอกเวลา" แล้วยอดไม่ขยับ
ระบบเก็บแค่ว่า "ติ๊กหรือไม่ติ๊ก" แต่ไม่มีที่เก็บ**จำนวนเงิน** — โค้ดจึงไปอ่านค่าที่ไม่มีอยู่จริง
แล้วได้ 0 ทุกครั้ง **ติ๊กไปก็เท่านั้น ยอดที่ระบบแนะนำเท่าเดิม** และใบเบิกเงินก็แยกไม่ออกว่า
จ่ายค่าอะไรไปเท่าไหร่ · ตอนนี้เก็บยอดจริงแยกช่อง (ไม่ใส่เลข = ใช้ค่าตั้งต้น นอกพื้นที่ 50 ·
นอกเวลา 100 แต่แก้เป็นเลขอื่นได้รายงาน เหมือนระบบเดิม)

### เพิ่ม — ช่อง "ราคาพนักงาน" บนหน้าตรวจงาน (ผู้ตรวจกรอกยอดจ่ายผู้สำรวจได้แล้ว)
ตารางค่าใช้จ่ายเดิมมีแต่ยอดที่**เรียกเก็บบริษัทประกัน** — ยอดที่**จ่ายผู้สำรวจ**ไม่มีที่กรอก
และไม่มีที่เก็บ ตอนนี้เพิ่มคอลัมน์ที่สองข้าง ๆ กัน เห็นทั้งสองฝั่งพร้อมกันในแถวเดียว
เหมือนที่ระบบเก่าทำ
- ครบทั้ง 8 รายการ: ค่าบริการ · ค่าเดินทาง · ค่ารูปถ่าย · ค่าโทรศัพท์ · ค่าประกันตัว ·
  ค่าเรียกร้อง · ค่าคัดประจำวัน · ค่าใช้จ่ายอื่นๆ
- **ใต้ตารางมีตัวปรับ 4 อย่าง**: นอกพื้นที่ · นอกเวลา · ตำบลพิเศษ · ค่าคัดประจำวัน (ถูก/ผิด/รอผล)
- **แยก "หักเงิน" เป็นแถวของตัวเอง** พร้อมเหตุผล (ส่งช้า · เอกสารไม่ครบ · หรือพิมพ์เอง) —
  ระบบเดิมยัดหักเงินไว้ในช่อง "ค่าใช้จ่ายอื่นๆ" เพราะส่วนขยายแทรกช่องใหม่ลงฟอร์มระบบเก่าไม่ได้
  ผลคือ**กรอกรายจ่ายอื่นจริง ๆ ไม่ได้เลย** เพราะจะกลายเป็นหักเงินทันที · เว็บนี้เราคุมเอง
  จึงแยกให้ถูกความหมาย และ **หักเงินโดยไม่ระบุเหตุผลจะบันทึกไม่ผ่าน**
- **บอกที่มาของยอดใต้ตาราง** — ใช้เรทของจังหวัด/อำเภอไหน ทีมไหน ระบบแนะนำเท่าไหร่
  รวมที่บันทึกไว้เท่าไหร่ · ถ้าแปลงพื้นที่ของเคสไม่ได้จะขึ้นแถบเหลืองบอกตรง ๆ ว่า
  หาเรทอัตโนมัติไม่ได้ ให้กรอกเอง — **ไม่เงียบแล้วปล่อยให้ยอดว่าง**
- เฉพาะผู้ตรวจกับผู้ดูแลระบบเท่านั้นที่เข้าถึงได้ — **ผู้สำรวจไม่เกี่ยวกับการตั้งยอดจ่ายตัวเอง**

### เพิ่ม — แปลงชื่อจังหวัด/อำเภอในเคส → รหัสพื้นที่ ให้หาเรทได้อัตโนมัติ
เคสเก็บพื้นที่เป็น**ข้อความไทย** ("อำเภอธัญบุรี") แต่ตารางเรทอ้าง**รหัส** — ถ้าแปลงไม่ได้
ก็หาเรทไม่เจอ คิดค่าตอบแทนไม่ได้ทั้งงาน · ตอนนี้แปลงให้อัตโนมัติแล้ว
- **ครอบคลุมทุกพื้นที่ที่เลือกได้ในแอป: 78/78 จังหวัด · 928/928 อำเภอ**
- รองรับที่เขียนไม่ตรงกัน: "อำเภอเมือง" ↔ "เมืองชลบุรี" · "กรุงเทพ ฯ" ↔ "กรุงเทพฯ" ·
  บางจังหวัดแอปเขียนอำเภอเมืองด้วยชื่อจังหวัด ("อำเภอบึงกาฬ" ↔ "เมืองบึงกาฬ")
- **"เบตง" ในแอปอยู่ในระดับจังหวัด** ทั้งที่จริงเป็นอำเภอหนึ่งของยะลา — จับให้ลงยะลา
  ไม่งั้นงานในพื้นที่นั้นหาเรทไม่เจอ

⚠️ **รหัสพื้นที่ในระบบมี 2 ชุด อย่าสลับกัน** — ชุดของพอร์ทัลประกัน (ชลบุรี = 9) กับ
ชุดมาตรฐานไทยที่ตารางเรทใช้ (ชลบุรี = 20) สลับแล้วจะได้เรทของจังหวัดอื่นแบบไม่มีใครรู้
มีเทสดักไว้แล้ว

### เพิ่ม — ระบบคิดค่าตอบแทนผู้สำรวจย้ายมาอยู่ที่เซิร์ฟเวอร์เรา
เดิมสูตรคิดเงินอยู่ในส่วนขยายเบราว์เซอร์ (2,119 บรรทัด) ทำงานได้เฉพาะตอนเปิดหน้าระบบเก่า —
**ปิดระบบเก่าเมื่อไหร่ก็ไม่มีใครคิดเงินได้ ถึงตารางเรทจะยังอยู่ครบ** ตอนนี้ย้ายมาคิดที่
เซิร์ฟเวอร์ของเราแล้ว ไม่ผูกกับหน้าเว็บไหนอีก
- คิดครบทุกกรณีที่ระบบเดิมทำ: เรทตามอำเภอ · เรทที่ต่างกันตามทีมผู้สำรวจ ·
  ตำบลที่จ่ายไม่เท่าอำเภอแม่ · ค่านอกพื้นที่ · ค่านอกเวลา · หักเงิน ·
  ค่าเรียกเก็บที่ต่างกันตามประเภทเคลม · งานของผู้รับเหมาที่ไม่คิดเรทให้อัตโนมัติ
- **บันทึกที่มาของยอดติดไปกับงาน** — ใช้เรทอะไร ของอำเภอไหน ทีมไหน ติ๊กอะไรไว้
  เวลาถูกถามว่า "ทำไมงานนี้ได้เท่านี้" ตอบได้ทันทีโดยไม่ต้องเดา

**ตรวจด้วยการเล่นซ้ำงานจริง 32 งานจากระบบเดิม — ยอดตรงกันทุกบาท (30/30 ที่เทียบได้
อีก 2 งานระบบเดิมไม่ได้เติมยอดให้)** เทสตัวนี้ผูกเข้า `npm test` แล้ว แก้สูตรเมื่อไหร่
จะรู้ทันทีว่าเพี้ยนจากของเดิมหรือเปล่า

### เพิ่ม — ย้ายตารางเรทค่าตอบแทนผู้สำรวจเข้าระบบเรา (ขั้นแรกของงานคิดเงิน)
เดิมเรทอยู่ในระบบ se-billing ที่ทำงานคู่กับ ISURVEY เท่านั้น — **งานที่ทำผ่านระบบใหม่
จึงไม่มีใครคิดค่าตอบแทนให้ และไม่มีที่บันทึกยอดจ่ายพนักงานเลย** เป็นช่องโหว่เรื่องเงิน
ที่จะกว้างขึ้นเรื่อย ๆ ตามจำนวนงานที่ย้ายมาระบบใหม่
- ยกเรทเข้าฐานข้อมูลของเราครบ: **337 อำเภอ · 25 จังหวัด · 2 ตำบลพิเศษ · 13 รหัสทีม**
- ยกเรทที่เดิม**ฝังอยู่ในโค้ดส่วนขยายเบราว์เซอร์**ออกมาเป็นข้อมูลด้วย (เรทตามคำนำหน้า
  เลขเซอร์เวย์ · งานต่อเนื่อง · ค่าคัดประจำวัน · ค่านอกพื้นที่/นอกเวลา) — ของเดิมแก้ทีต้อง
  สร้างส่วนขยายใหม่แล้วไล่ติดตั้งทุกเครื่อง
- เตรียมที่เก็บ**ยอดที่คิดจริงต่องาน** พร้อมบันทึกว่าใช้เรทอะไร ติ๊กอะไรไว้ ใครคิด เมื่อไหร่
  — ตารางเรทไม่เก็บประวัติ (ตามที่ตกลง) บันทึกตรงนี้จึงเป็นหลักฐานเดียวเวลาย้อนตรวจยอด

**จงใจแยกเป็นคนละชุดกับ se-billing ไม่ต่อสายถึงกัน** — คนที่ใช้ ISURVEY กับส่วนขยาย
ทำงานต่อไปโดยไม่ถูกแตะแม้แต่บรรทัดเดียว วันเลิกใช้ ISURVEY ก็ปิด se-billing ทิ้งได้เลย
ไม่ต้องย้ายอะไร · แลกกับความเสี่ยงว่ามีคนแก้เรทข้างเดียว จึงมี**เครื่องมือเทียบเรท 2 ชุด**
(อ่านอย่างเดียวทั้งคู่) ไว้รันก่อนปิดงวดจ่ายเงิน

### แก้บั๊ก — รัน migration บน production ต้องพิมพ์ SQL ทั้งไฟล์ด้วยมือ
ไฟล์ `.sql` ไม่ได้ถูกใส่เข้าไปในตัวเซิร์ฟเวอร์ตอน build (คอมไพล์แล้วเหลือแต่ JS) เวลาจะรัน
migration จึงต้องพิมพ์คำสั่งทั้งไฟล์ลงหน้าเทอร์มินัล — ไฟล์เรทมี 380 คำสั่ง ทำแบบนั้นไม่ไหว
ตอนนี้ไฟล์ติดไปกับเซิร์ฟเวอร์แล้ว รัน migration ได้ด้วยคำสั่งเดียว

### เพิ่ม — ออกใบเอกสารจากเคสจริงได้แล้ว เก็บเข้าสำนวนให้เอง (APK 1.0.51+53)
ปุ่ม **"ออกใบเอกสารหน้างาน"** ท้ายหน้างานสำรวจ — กดแล้วได้ใบที่กรอกข้อมูลของเคสนั้นให้เสร็จ
ไม่ต้องพิมพ์ซ้ำ · เลือกแบบใบ (และเลือกคันถ้าเป็นใบของคู่กรณี) → ยื่นให้เขาเซ็นบนจอ →
**พิมพ์ออกกระดาษ** ให้ถือกลับ + **กด "บันทึกเข้าสำนวน" แล้วใบเข้าไปอยู่ในรูปของเคส
หมวด "ใบแจ้งความเสียหาย" ให้เอง** — ไฟล์ที่คู่กรณีถือกับที่ส่งเข้าระบบเป็นใบเดียวกัน
- ออกใบได้**ตั้งแต่ยังไม่ส่งงาน** เพราะต้องยื่นให้เซ็นตั้งแต่อยู่หน้างาน
- ปุ่มบอกด้วยว่าออกไปแล้วกี่ใบ · เปลี่ยนแบบใบ = ล้างลายเซ็นให้ (คนละเอกสารกัน)
- **จำเครื่องพิมพ์ที่เคยใช้** ครั้งต่อไปกดพิมพ์ได้เลยไม่ต้องเลือกเครื่องซ้ำ
- **ไม่รู้จักบริษัทประกันของเคส = ไม่ยอมออกใบ** พร้อมบอกให้ไปแก้ชื่อบริษัทก่อน —
  ท้ายใบ (เบอร์ติดต่อ/กำหนดวัน) ต่างกันรายบริษัท เดาแทนแล้วลูกค้าโทรผิดที่
- เตือนถ้ายังไม่มีลายเซ็น แต่ไม่ห้าม (บางกรณีพิมพ์เปล่าไปให้เซ็นด้วยปากกา)

**ทดสอบกับเคสจริงบนเครื่องจริงครบวงจร** (สร้างเคส → จ่ายงาน → รับงาน → ถ่ายรูปยืนยัน →
ออกใบ → เซ็น → บันทึก → ส่งงาน) ผลบนเซิร์ฟเวอร์: รูป 2 ใบ หมวดถูกทั้งคู่
(`รูปยืนยันถึงที่เกิดเหตุ` + `ใบแจ้งความเสียหาย`) · ลบเคสทดสอบเรียบร้อยแล้ว

### เพิ่ม — ใบเอกสารหน้างานครบ 10 แบบ + ช่อง "ซ่อมที่" (APK 1.0.50+52)
ต่อจากงานพิมพ์ใบแจ้งความเสียหาย — ขยายจากใบเดียวเป็นระบบที่รองรับเอกสารหน้างานทุกแบบ
- **10 แบบ = 5 หมวด × 2** (รถประกัน · รถคู่กรณี · ผู้บาดเจ็บ/เสียชีวิต · ทรัพย์สิน · รับเงิน
  โดยแต่ละหมวดมี "ใบหลักฐาน/แจ้งความเสียหาย" กับ "ใบติดต่อ") — เลือกแบบใบก่อนพิมพ์
  **4 แบบแรกใช้งานได้จริง อีก 6 แบบขึ้นในรายการแต่กดไม่ได้** เพราะยังไม่เคยเห็นเนื้อในใบจริง
  (เดาเนื้อหาเอกสารที่คนนอกต้องเซ็นรับรองไม่ได้ — ขึ้นว่า "ยังไม่มีข้อมูล" ให้เห็นชัด)
- **ท้ายใบเปลี่ยนตามบริษัทประกันเอง** — ไม่ใช่แค่มี/ไม่มี QR แต่**กำหนดวันติดต่อคนละเลข**
  (ไอโออิ 7 วัน + QR · ไทยไพบูลย์ 15 วัน + พิมพ์ลิงก์เป็นข้อความ) เบอร์ก็คนละชุด
  จับบริษัทจากชื่อที่พนักงานพิมพ์ — **ไม่รู้จักบริษัทจะไม่ยอมพิมพ์** ดีกว่าให้เบอร์ผิดกับลูกค้า
- **ข้อมูลบนใบดึงจากเคสจริงทั้งหมด** ไม่ต้องพิมพ์ซ้ำ · ใบของคู่กรณีดึงจากคันที่เลือก
- **"ความเสียหายส่วนแรก" ขึ้นเฉพาะเคลมที่มีจริง** ไม่มีข้อมูล = ซ่อนทั้งบล็อก
- ระดับความเสียหายบนใบเขียน **`ต่ำ (L)` / `กลาง (M)` / `สูง (H)` / `สูงมาก (X)`** —
  ไทยเพราะคนที่เซ็นรับรองต้องอ่านออกว่าตัวเองเซ็นอะไร รหัสเพราะผู้ตรวจต้องเทียบกับหน้าระบบประกัน
- หัวกระดาษเป็น**โลโก้อย่างเดียว** ไม่มีบล็อกที่อยู่บริษัท
- QR ชี้หน้า **รายชื่อสาขา/ศูนย์-อู่ในสัญญาบนเว็บของเราเอง** (`/branches/<รหัสบริษัท>`)
  เปิดได้โดยไม่ต้องล็อกอิน (คนสแกนคือคู่กรณีที่ไม่มีบัญชี) ออกแบบให้อ่านง่ายบนมือถือ

### เพิ่ม — ช่อง "ซ่อมที่" (ชื่ออู่/ศูนย์ที่นำรถเข้าซ่อม)
เดิมผู้สำรวจจดไว้ในช่องหมายเหตุ ค้นย้อนหลังไม่ได้ ผู้ตรวจต้องไล่อ่านเอง
- เพิ่มช่องทั้ง**แอปมือถือ**และ**หน้าตรวจงานบนเว็บ** อยู่ในหมวดกรมธรรม์ ถัดจากค่าเสียหายส่วนแรก
- จำกัด 200 ตัวอักษรทั้งสองฝั่งให้ตรงกับฐานข้อมูล — **ยาวเกินแล้วฐานข้อมูลไม่ตัดปลายให้
  แต่ปฏิเสธทั้งใบ** ทำให้ส่งงาน/บันทึกไม่ผ่านทั้งที่ผิดแค่ช่องเดียว
- ⚠️ **ต้องรัน migration 034 บน production ก่อนแจก APK ใหม่** ไม่งั้นส่งงานสำรวจไม่ผ่านเลย
  (ยังรันไม่ได้ — เครื่องพัฒนาต่อฐานข้อมูลไม่ติดตอนนี้)

### แก้บั๊ก — หน้าสาธารณะถูกเด้งไปหน้าล็อกอิน
พนักงานที่เคยล็อกอินแล้วโทเคนหมดอายุ สแกน QR บนใบจะถูกเด้งไปหน้าล็อกอินทั้งที่หน้านั้น
ไม่ต้องล็อกอิน (คนนอกที่ไม่เคยล็อกอินไม่โดน) — ยกเว้นหน้ารายชื่อสาขาออกจากการเด้งแล้ว

### เพิ่ม — พิมพ์ "ใบแจ้งความเสียหาย" ออกเครื่องพิมพ์พกพาได้จากในแอป (APK 1.0.49+51)
เดิมพนักงานกรอกใบด้วยมือลงกระดาษแล้วถ่ายรูปเก็บเข้าสำนวน ตอนนี้แอปสร้างใบให้เอง
คู่กรณีเซ็นบนจอ แล้วพิมพ์ออกกระดาษให้ถือกลับบ้านได้ทันที
- **ใบที่พิมพ์ = ไฟล์เดียวกับที่เก็บเข้าสำนวน** — สร้างรูปครั้งเดียวส่ง 2 ทาง
  (เข้าเครื่องพิมพ์ + เข้าหมวดรูป "ใบแจ้งความเสียหาย" ที่มีอยู่แล้ว) ที่คู่กรณีถืออยู่กับ
  ที่ส่งเข้าระบบประกันจึงตรงกันเสมอ ไม่มีทางเป็นคนละใบ
- **เซ็นชื่อบนจอ** — เขียนเอง ไม่ต้องพึ่งไลบรารีนอก
- **QR ชี้หน้ารายชื่อสาขา/อู่ของเราเอง** (`survey.sesurvey.cloud`) แทนของเดิมที่ชี้ไปโดเมน
  ISURVEY ซึ่งกำลังจะเลิกใช้ — ใบที่แจกไปแล้วอยู่ในมือคนนอก เรียกกลับไม่ได้ ถ้าปิด ISURVEY
  วันไหน QR บนใบเก่าทุกใบจะตายพร้อมกัน
- ใบ**ประกอบจากบล็อก** ไม่ได้เขียนตายตัวต่อแบบ — ใบมีหลายแบบ (รถประกัน · รถคู่กรณี · …)
  ต่างกันแค่มีบรรทัดไหนบ้าง เพิ่มแบบใหม่ไม่ต้องแก้โครง
- ระดับความเสียหายบนใบใช้คำเต็ม **ต่ำ/กลาง/สูง/สูงมาก** ตามที่ระบบประกันกำหนด
  (ใบนี้ให้คนนอกอ่านและเซ็นรับรอง จึงไม่ใช้ตัวย่อ)

**ยังไม่เสร็จ:** ยังเป็นหน้าทดสอบที่เข้าจากเมนูหน้าหลัก ยังไม่ได้ผูกกับข้อมูลเคสจริง
และยังไม่มีช่อง "ซ่อมที่" (ต้องเพิ่มทั้งแอปมือถือและเว็บ)

**บทเรียนจากเครื่องจริง** (TSC Alpha-3R / RE310B กระดาษ 58 มม.)
- **สเปกบอกว่ารองรับ ESC/POS แต่ของจริงไม่รับ** — ส่งเข้าไปเครื่องรับข้อมูลครบแต่เงียบ
  ไม่พิมพ์อะไรเลย ต้องใช้ TSPL ซึ่งเป็นภาษาแม่ของ TSC · แอปจึงมีปุ่ม "ทดสอบ 3 โหมด"
  ที่ยิงแถบสั้น ๆ ซึ่ง**บอกชื่อโหมดของตัวเอง** เจอเครื่องรุ่นใหม่กดครั้งเดียวรู้เลยว่าพูดภาษาไหน
- **เดินกระดาษเปล่าทิ้ง 15-20 ซม. ทุกใบ** เพราะสั่งขนาดกระดาษโดยไม่ระบุหน่วย เครื่องเลยเข้าใจ
  ว่าเป็นสติกเกอร์แล้วออกไปหาช่องว่างระหว่างดวง — ระบุหน่วยแล้วหายสนิท
- **ต่อไม่ติด ≠ ต่อไม่เป็น** — เครื่องพิมพ์ปิดอยู่ก็ขึ้นอาการเดียวกัน ตอนนี้ข้อความบอกชัด
  ว่าให้ไปเช็คว่าเครื่องเปิดหรือยัง และบอกด้วยว่าต่อติดด้วยวิธีไหน
- ต้องเดินกระดาษท้ายใบ ~25 มม. ไม่งั้น QR อยู่ตรงฟันฉีกพอดี ฉีกแล้ว QR ขาด

### แก้บั๊ก — เซ็นชื่อแนวตั้งกลายเป็นการเลื่อนหน้าจอ
กรอบเซ็นชื่ออยู่ในหน้าที่เลื่อนได้ → ลากขึ้นลงหน้าจอเลื่อนแทนที่จะเป็นเส้น
**ลายเซ็นจริงมีเส้นแนวตั้งเกือบทุกลายมือ ผลคือเซ็นแล้วได้แต่เส้นแนวนอนขาด ๆ**
เจอตอนทดสอบบนเครื่องจริงก่อนถึงมือพนักงาน

### เพิ่ม
- **CHANGELOG.md** — บันทึกการเปลี่ยนแปลงทั้งโปรเจกต์ย้อนหลังตั้งแต่ มี.ค. 2569
- **CLAUDE.md** — กติกาการทำงาน: แก้อะไรต้องอัปเดต CHANGELOG ทุกครั้ง + กับดักที่เจอบ่อย
- **เครื่องมือสกัดสเปกฟอร์ม EMCS** (`se-autokey/tools/emcs_spec.py`) — อ่านไฟล์ HTML
  ที่เซฟไว้ (ไม่แตะ EMCS) แล้วดึง **ตัวเลือก dropdown ทุกตัว · ช่องบังคับจริงแยก
  "ทุกบริษัท" กับ "เฉพาะบริษัท" · รายการ element** ออกมาเป็น JSON
  โหมด `--diff` เทียบกับตารางแปลงรหัสในโปรเจกต์แล้วบอกว่าป้ายไหนหายไป
  (ตรวจรอบแรก: สาเหตุ 79 · ลักษณะความเสียหาย 21 · ความสัมพันธ์ 39 · ใบขับขี่ 21 · สี 55 — ครบทุกตัว)
  แทนการนั่งสกัดด้วยมือที่ทำมาแล้วหลายรอบ

### เพิ่ม — เครื่องมือดูดข้อมูลเคสจริงจาก EMCS เป็น XML
`se-autokey/tools/emcs_dump.py` — อ่านหน้า EMCS ที่กรอกครบแล้ว (จากไฟล์ที่เซฟไว้
ไม่แตะระบบจริง) เก็บ**เฉพาะค่าที่กรอกหรือถูกเลือกอยู่** แล้วประกอบเป็น XML
รูปแบบเดียวกับที่ระบบเราสร้าง ใช้เอาเคสจริงมาเป็นข้อมูลทดสอบ
- `--diff` เทียบ tag ต่อ tag ว่า "ที่เราส่งเข้าไป" ต่างจาก "ที่อยู่ใน EMCS ตอนนี้" ตรงไหน
  แยกออกได้ว่าเป็นเพราะเราไม่ได้ส่ง · EMCS เติมเอง · หรือหัวหน้าแก้ทีหลัง
- ทดสอบกับเคสจริงที่มีทั้งหน้าเว็บและ XML ของตัวเอง: ตรง 77 tag · ต่าง 15 อธิบายได้ครบ
- อ่านครบทั้ง 5 ส่วน: ข้อมูลทั่วไป · รถประกัน+คู่กรณี · ทรัพย์สิน · ผู้บาดเจ็บ · ใบค่าใช้จ่าย

### เพิ่ม — ให้บอทไปเปิด EMCS อ่านหน้าเคสมาเอง (ไม่ต้องเซฟหน้าด้วยมือ)
`se-autokey/tools/emcs_fetch.py` — login → ค้นเลขเคลม → เปิดเรื่อง → เซฟ 4 หน้า
→ ส่งต่อให้ตัวแปลง XML ทำงานต่อ
- **อ่านอย่างเดียว** ไม่กรอกช่อง ไม่กดบันทึก/แก้ไข/ส่งงาน — กดได้เฉพาะปุ่มสลับหน้า
  กับปุ่มออกจากเรื่อง ปุ่มอื่นโค้ดปฏิเสธทิ้ง
- **ไม่ใส่ `--fetch` = ไม่เปิดเรื่อง** แค่บอกว่าเคลมนี้มีเรื่องอะไรบ้าง
- เปิดเรื่องใน EMCS เท่ากับล็อกเรื่องนั้นไว้ คนอื่นเปิดต่อไม่ได้ → กด **"กลับหน้า
  Inbox/Outbox"** เพื่อออกจากเรื่องทุกกรณีแม้ระหว่างทางพัง และปิดเบราว์เซอร์เอง
  ไม่ปล่อยค้างถือล็อก · รวมถึงกรณีเปิดเรื่องติดแล้วหน้าโหลดไม่ทัน (เรื่องล็อกไปแล้ว
  ทั้งที่ยังอ่านอะไรไม่ได้) · ส่วนกรณีเปิดไม่ติดเพราะคนอื่นล็อกอยู่ จะไม่กดและไม่เตือนผิด
- **ทดสอบกับ EMCS จริงแล้ว** (เคลม 21BR10AVD-6906-000098) ดึงครบ 4 หน้าใน 12 วินาที
  ออกจากเรื่องปลดล็อกทุกรอบ · เทียบกับหน้าที่เซฟด้วยมือไว้เมื่อ 15 ก.ค. ได้ **87 จาก 93
  ช่องตรงกัน** ส่วนที่ต่างคือข้อมูลที่ถูกแก้ใน EMCS จริงระหว่างนั้น (สถานที่ · รายละเอียด ·
  เวลาเกิดเหตุ · ผู้แจ้ง · ชื่อผู้สำรวจ · ที่อยู่) ไม่ใช่อ่านพลาด · อ่านซ้ำอีกรอบห่างกัน
  1 ชั่วโมงครึ่งได้ค่าตรงกัน 117/117 ช่อง และใช้กับอีกเคลมที่โครงสร้างต่างกันได้เช่นกัน
- ตัวเลือก **ค้างหน้า EMCS ไว้บนจอ** สำหรับตอนอยากดูเทียบด้วยตาเอง — ปกติปิดให้
  อัตโนมัติ โหมดนี้เรื่องจะยังถูกล็อกจนกว่าจะกด "กลับหน้า Inbox/Outbox" เอง จึงเตือนไว้ชัด

### เพิ่ม — เตือนชื่อที่ EMCS จะลบทิ้ง (หน้าตรวจงาน)
EMCS มีตัวกรองอักขระบนช่อง **ชื่อคน** และมันไม่ได้แค่ตัดตัวอักษรทิ้ง — มัน
**ล้างข้อความทั้งช่อง** ทันทีที่มีคนคลิกเข้า-ออกช่องนั้น การนำเข้าด้วย XML ไม่โดนกรอง
ค่าจึงเข้าไปได้ แต่พอหัวหน้าเปิดมาตรวจแล้วคลิกโดน **ชื่อหายเกลี้ยงโดยไม่มีใครรู้**
- หน้าตรวจงานขึ้นแบนเนอร์เหลืองก่อนส่งเข้า EMCS บอกว่าชื่อไหนมีปัญหาและตัวไหนคืออักขระต้นเหตุ
- ครอบคลุม ผู้แจ้ง · ผู้สำรวจภัย · พนักงานสอบสวน · ผู้ขับขี่รถประกัน ·
  เจ้าของ/ผู้ขับขี่รถคู่กรณีทุกคัน · ผู้บาดเจ็บทุกคน · เจ้าของทรัพย์สินทุกรายการ
  (ผู้เอาประกันไม่มีตัวกรองนี้ ตรวจจากหน้า EMCS จริงแล้ว)
- ใช้ได้เฉพาะ ตัวอักษร · ตัวเลข · เว้นวรรค · จุด · ขีดกลาง — วงเล็บ จุลภาค สแลช
  เครื่องหมาย # และ**เลขไทย** ทำให้ชื่อหายทั้งช่อง
- ฝั่งบอทที่ดึง XML ไปเองไม่เห็นแบนเนอร์ จึงเขียนเตือนลงล็อกเซิร์ฟเวอร์ด้วย
- ข้อมูลปัจจุบันในระบบยังไม่มีเคสไหนติดเงื่อนไขนี้

### ตรวจแล้วไม่ต้องแก้ — ตัวเลือกผลตรวจแอลกอฮอล์ตรง EMCS อยู่แล้ว
แอปมือถือใช้ตัวเลือก 2 ค่าที่ข้อความตรงกับ EMCS ("ไม่มีการตรวจแอลกอฮอล์" /
"มีการตรวจแอลกอฮอล์") พร้อมช่อง "ระบุผล" ที่โผล่เมื่อเลือก "มีการตรวจ" — ตรงกันทั้ง
5 จุด (มือถือ · เว็บ · นำเข้า · ส่งออก · บอท) ทดสอบการแปลงค่าครบทุกทางแล้ว
- แก้คอมเมนต์เก่าในโค้ด 2 จุดที่ยังเขียนว่า "แอปมีกล่องข้อความเดียว" ซึ่งล้าสมัยไปนานแล้ว
  และเกือบทำให้ไปแก้แอปทั้งที่ไม่มีอะไรเสีย

### แอปมือถือ 1.0.28+30 — แก้บั๊กชิ้นส่วนที่ไม่มีด้าน (ที่คิดว่าแก้ไปแล้ว)
ชิ้นส่วนที่ EMCS ไม่มีปุ่มเลือกด้าน (กันชนหน้า/หลัง · หลังคา · กระบะ · ฝากระโปรง ฯลฯ
12 ชิ้น) **ยังเลือก ซ้าย/ขวา ได้อยู่** ทั้งที่แก้ไปแล้วตั้งแต่ 1.0.25+27
- ต้นเหตุ: หน้าจอเลือกด้านถูกก๊อปไว้ **4 ที่** รอบก่อนแก้ไปแค่ที่เดียว
- เจอตอนกรอกงานจริงบนเครื่อง แก้ 3 ที่แรกแล้ว build ใหม่ ยังโผล่อีก จึงเจอที่ 4
- ย้ายรายชื่อ 12 ชิ้นเป็นค่ากลางที่ทุกที่ใช้ร่วมกัน — เดิมซ่อนอยู่ในหน้าเดียว
  ที่อื่นจึงมองไม่เห็นแล้วต่างคนต่างเขียน
- ผลกระทบเดิม: ช่างเลือกด้านให้กันชนหลังได้ แต่ EMCS ไม่มีที่ลง ด้านที่เลือกหายเงียบ

### กติกาที่ยืนยันแล้ว — งานครั้งที่ 2 เป็นต้นไป ต้องใส่รูปด้วย
เดิมค้างไว้ไม่สรุป บอทจึงกรอกให้แค่เลขที่ใบแจ้งหนี้กับวันที่ ไม่เคยอัปรูปเลย
- ตอนนี้บอทอัปรูปของครั้งนั้นให้ก่อนกรอกใบแจ้งหนี้
- รูปใน EMCS ผูกกับ **เลขเคลม** ไม่ใช่ครั้งที่ → โควตา 80 ใบใช้ร่วมกันทั้งเคลม
  ครั้งที่ 2 จึงเติมต่อจากที่ครั้งแรกใช้ไป และบอทตัดให้พอดีโควตาที่เหลือเอง

### ถอดออก — แผง "ช่องที่ EMCS บังคับแต่ยังว่าง" บนหน้าตรวจงาน
ทำเสร็จแล้วแต่ถอดออกก่อน (user ตัดสินใจ) — **ให้ลองใช้งานจริงก่อน** ถ้าเจอปัญหาว่า
ช่างปล่อยช่องว่างบ่อยจนหัวหน้าเสียเวลาไล่แก้บน EMCS ค่อยเอากลับมา
- เหตุผล: ยังไม่มีหลักฐานจากหน้างานว่าเป็นปัญหาจริง — เพิ่มของบนหน้าจอก่อนรู้ว่าจำเป็น
  มีแต่ทำให้รก
- ของที่ทำไว้แล้วอยู่ใน git ย้อนกลับมาได้ทันที ไม่ต้องเริ่มใหม่
- **คำเตือนตอนอัปโหลด XML ยังอยู่เหมือนเดิม** (คนละอันกัน)

### เพิ่ม — คำเตือนตอนนำเข้า XML มาจากฟอร์ม EMCS จริง (ไม่ต้องเขียนมือ)
เดิมข้อความเตือน "ไฟล์นี้ไม่มีช่องนั้นช่องนี้" เขียนไว้ตายตัวทีละบรรทัด ถ้า EMCS เพิ่ม
ช่องบังคับใหม่ **ระบบจะเงียบ** จนกว่าจะมีคนไปเจอว่าบันทึกไม่ผ่านแล้วค่อยมาเพิ่มเอง
- ตอนนี้รายการช่องบังคับสกัดจากหน้า EMCS จริงด้วยเครื่องมือ แล้วให้ระบบเตือนเอง
  EMCS เปลี่ยนเมื่อไหร่ → รันเครื่องมือใหม่ครั้งเดียวจบ
- ผลทันที: **เจอช่องที่คนเขียนมือลืมไป** — "สาขาบริษัทประกัน" ไม่เคยเตือนมาก่อน
- ไฟล์ที่มีข้อมูลครบจะไม่ถูกเตือนเกินจำเป็น (แยกแยะตามค่าจริงในไฟล์)
- ครอบคลุมเฉพาะช่องที่บังคับทุกบริษัท — ช่องที่บังคับเฉพาะบางบริษัทยังไม่รวม
  เพราะตอนอ่านไฟล์ยังไม่รู้ว่าเป็นบริษัทไหน
- **ไม่เตือนช่องที่บอทเติมให้เองอยู่แล้ว** — "สาขาบริษัทประกัน" บอทเลือกให้ตอนนำเข้า
  คนตรวจไม่ต้องทำอะไร เตือนไปก็หาที่กรอกไม่เจอ (คำเตือนที่ไม่ต้องทำอะไร
  สอนให้คนเมินคำเตือน) → ไฟล์ ISURVEY จริงเตือน 2 ข้อ เหลือเฉพาะที่ต้องกรอกจริง

### กติกาที่ยืนยันแล้ว — XML 3 ที่มา ห้ามปนกัน
| ที่มา | มีข้อมูลแค่ไหน | ระบบทำอะไร |
|---|---|---|
| **ISURVEY** (ระบบเก่า) | ไม่มีหนัก/เบา · ลักษณะความเสียหาย ฯลฯ | เตือนให้หัวหน้ากรอกเอง · ยอดเงินส่งต่อเข้า EMCS |
| **se-survey** (แอปมือถือ) | ครบตามที่พนักงานกรอก | ไม่ผ่านเส้นทางนำเข้า XML เลย — ระบบสร้างไฟล์ให้เอง |
| **สกัดจากหน้าเว็บ EMCS** | ครบ เพราะผู้ใช้กรอกและตรวจแล้ว | **ข้อมูลทดสอบเท่านั้น** |

- ไฟล์ที่สกัดจาก EMCS ถูกประทับตราไว้ในไฟล์ตั้งแต่ตอนสร้าง ระบบอ่านออกและ
  **บันทึกเป็นคนละประเภทกับงาน ISURVEY** — เดิมบังคับเป็น "งานจากระบบเก่า" ตายตัว
  ซึ่งจะทำให้เคสทดสอบถูกนับเป็นงานจริงและ**ดันยอดเงินเข้าระบบประกันโดยไม่ตั้งใจ**
- ข้อความเตือนเปลี่ยนตามที่มาของไฟล์ — เดิมไฟล์จาก EMCS ก็ขึ้นว่า "ไฟล์ ISURVEY ไม่มี..."
- ตั้งใจ**ไม่**อ่าน HEV_CAR / LOSS_ID กลับในเส้นทางนำเข้า เพราะเป็นเส้นทางของระบบเก่า
  ซึ่งไฟล์จริงไม่เคยมี tag พวกนี้อยู่แล้ว

### แก้บั๊ก — จ่ายงานแล้วช่างไม่ได้รับแจ้งเตือน โดยไม่มีใครรู้ (APK 1.0.40+42)
ระบบแจ้งเตือนงานใหม่ล้มได้ 3 แบบ และ **ทั้ง 3 แบบเงียบสนิท** — หน้าจ่ายงานขึ้นว่าสำเร็จ
เหมือนกันหมด ทั้งที่ช่างอาจไม่ได้รับอะไรเลย
- **หน้าจ่ายงานบอกผลจริงแล้ว** — ถ้าแจ้งเตือนไปไม่ถึง ขึ้นแถบเหลือง *"มอบหมายงานแล้ว
  แต่แจ้งเตือนไปไม่ถึงเครื่องช่าง — โทรแจ้งช่างด้วย"* และค้างหน้าไว้ ไม่เด้งออกทันที
- แยกสาเหตุให้ด้วย: เครื่องช่างยังไม่เคยลงทะเบียน · ถอนการลงทะเบียนไปแล้ว ·
  ระบบแจ้งเตือนไม่ได้ตั้งค่าบนเซิร์ฟเวอร์ · ส่งไม่สำเร็จ
- **ตรวจพบว่าผู้สำรวจ 60 จาก 144 คนไม่มีการลงทะเบียนรับแจ้งเตือน** — จ่ายงานไปก็ไม่มี
  ทางรู้ตัว ต้องไล่ให้เปิดแอปใหม่ทีละเครื่อง
- ถ้าเครื่องถอนการลงทะเบียนไปแล้ว ระบบล้างข้อมูลค้างให้เอง ไม่หลอกว่ายังส่งได้
- **"เรียกพิกัดพนักงาน" เลิกโกหก** — เดิม log ว่า "ส่งให้ N คน" โดยนับจากจำนวนคนในระบบ
  ไม่ใช่จำนวนที่ส่งถึงจริง ตอนนี้แยกนับ สำเร็จ/ไม่มีการลงทะเบียน/ล้มเหลว
- ถ้าเซิร์ฟเวอร์ไม่ได้ตั้งค่าระบบแจ้งเตือน จะเตือนดัง ๆ ตอนเปิดเครื่อง + ดูได้ที่ `/health`
  (เดิมเป็นข้อความบรรทัดเดียวที่ไม่มีใครเห็น) — **ยังเปิดระบบได้ตามปกติ ไม่ล่ม**

### เพิ่ม — รู้ว่าเครื่องพนักงานแต่ละคนใช้แอปเวอร์ชันอะไร (APK 1.0.40+42)
APK แจกด้วยมือ ไม่มีใครรู้ว่าเครื่องไหนอยู่เวอร์ชันไหน แล้วของที่เปลี่ยนฝั่งเซิร์ฟเวอร์
ทำให้แอปเก่าพังเงียบ — ตรวจ log จริงวันนี้พบ **รูปลงเวลาโหลดไม่ขึ้นเลยสักใบ 191 ครั้ง
สำเร็จ 0 ครั้ง** เพราะเครื่องพนักงานยังเป็น APK เก่าที่ไม่แนบสิทธิ์
- แอปส่งเวอร์ชันมากับทุกคำขอ · หน้าจัดการผู้ใช้เพิ่มคอลัมน์ **"เวอร์ชันแอป"**
  (ยังไม่เคยส่ง = ขึ้น "ไม่ทราบ" สีเหลือง = ยังไม่ได้อัป)
- **ไม่บล็อกอะไรทั้งสิ้น** — แอปเก่ายังใช้งานได้ครบ แค่ขาดฟีเจอร์ใหม่ (ตามที่ตกลงกัน)
- บันทึกเฉพาะตอนเวอร์ชันเปลี่ยน ไม่ถ่วงการใช้งาน

### แก้บั๊ก — "นำเข้า EMCS แล้ว" ไม่ได้แปลว่าบริษัทประกันได้รับงาน
ป้ายเขียว "✓ นำเข้า EMCS แล้ว" ขึ้นตั้งแต่บอทสร้างเรื่องเสร็จ แต่**บอทไม่มีสิทธิ์กดปุ่ม
"ส่งงานใหม่"** (คนต้องกดเอง) → งานที่ค้างรอคนกดส่งดูเหมือนเสร็จแล้ว และไม่มีใครนับได้ว่า
ค้างอยู่กี่เรื่อง — ตรวจแล้วตอนนี้ค้างอยู่ **7 เรื่อง** ทั้งหมดที่มีในระบบ
- แยกเป็น 2 ป้าย: **⏳ draft ค้าง — รอกด "ส่งงานใหม่"** (เหลือง) กับ **✓ ส่งงานแล้ว** (เขียว)
- หน้ารายการงานตรวจสอบขึ้นแถบเตือนจำนวนเรื่องที่ค้างรอกดส่ง
- เก็บ**ข้อความสถานะดิบ**จากหน้า EMCS ไว้ด้วย ไม่ใช่แค่ "ส่งแล้ว/ยังไม่ส่ง"
- **บอทเลิกเดา** 2 จุด: (1) เดิมแยกเรื่องด้วยเลขเคลมอย่างเดียวแล้วเอาแถวสุดท้าย —
  1 เลขเคลมมีได้หลายเรื่อง จึงอ่านสถานะผิดเรื่องได้ ตอนนี้แยกด้วย**เลขเซอร์เวย์**
  แยกไม่ออกเมื่อไหร่ตอบ "ไม่ทราบ" (2) เดิมถือว่า "ไม่ใช่ draft = ส่งแล้ว" ซึ่งกว้างเกิน
  ตอนนี้ยืนยันเฉพาะสถานะที่รู้จักจริง สถานะแปลก ๆ รายงานตามจริงให้คนตัดสิน

### แก้บั๊ก — ใครล็อกอินได้ ก็เปิดรูปบัตรประชาชนของทุกเคสได้
ด่านตรวจรูปใน `/uploads` ตรวจแค่ว่า "โทเคนของจริงไหม" ไม่ได้ดูว่าคนขอ**มีสิทธิ์ดูรูปใบนั้น
หรือเปล่า** → ช่างคนไหนก็ได้เปิดรูปบัตรประชาชน/ใบขับขี่ของเคสที่ไม่ใช่ของตัวเองได้ ถ้ารู้ชื่อไฟล์
- ตอนนี้ตรวจรายไฟล์: **admin/callcenter** เห็นทุกอย่าง · **ผู้ตรวจ** เห็นเฉพาะไฟล์ของเคส
  (ไม่รวมรูปลงเวลา ให้ตรงกับรายงานลงเวลาที่จำกัดสิทธิ์อยู่แล้ว) · **ผู้สำรวจ** เห็นเฉพาะเคส
  ที่ได้รับมอบหมาย + รูปลงเวลาของตัวเอง
- ย้ายงาน/ปฏิเสธงาน = สิทธิ์ดูรูปเปลี่ยนทันที ไม่ค้าง
- ตัดสินสิทธิ์จาก**โครงสร้างโฟลเดอร์ที่ระบบสร้างเอง** ไม่ใช่ชื่อไฟล์ที่แอปส่งมา (ปลอมได้)
  และอ่านสิทธิ์จากฐานข้อมูลสด ๆ ไม่ใช่จากโทเคน — ปิดบัญชีแล้วต้องใช้ไม่ได้ทันที
- ทดสอบยิงจริง 15 กรณี (ทุกบทบาท × ทุกชนิดไฟล์ + path หลอก) ผ่านครบ

### แก้บั๊ก — เลขเซอร์เวย์ซ้ำได้ และงานที่ส่งไปแล้วถูกเขียนทับเงียบ ๆ (APK 1.0.39+41)
เจอจากการตรวจระบบทั้งหมด: กติกา "เลขเซอร์เวย์ห้ามซ้ำ" (เลขอ้างอิงเบิกเงิน) ถูกบังคับ
แค่ตอน**สร้างเคส/ส่งงาน/นำเข้าไฟล์เก่า** เท่านั้น — ช่องทาง **แก้ไข** ทั้ง 2 ทางไม่เคยตรวจเลย
และฐานข้อมูลไม่มีอะไรกันไว้เป็นชั้นสุดท้าย
- **ฐานข้อมูลกันเลขซ้ำเองแล้ว** 3 ชั้น: ซ้ำในช่องเดียวกันข้ามเคส · ซ้ำข้ามช่อง
  (เลขงานครั้งแรกของเคสหนึ่ง = เลขงานครั้งที่ 2 ของอีกเคส) · ซ้ำกันเองในเคสเดียว
- ช่องทางแก้ไขของผู้สำรวจและของผู้ตรวจ ตรวจเลขซ้ำแล้วทั้งคู่
- ถ้าเลขซ้ำจริง ระบบตอบเป็นคำเตือนที่อ่านรู้เรื่อง ไม่ใช่ "เกิดข้อผิดพลาด" ลอย ๆ
- **งานที่ส่งไปแล้ว/ผู้ตรวจแก้แล้ว/นำเข้าระบบประกันแล้ว แก้จากแอปไม่ได้อีก** — เดิมแอปที่
  เปิดฟอร์มค้างไว้เขียนทับของที่หัวหน้าแก้ไปแล้วได้เงียบ ๆ ไม่มีสัญญาณเลย

### แก้บั๊ก — งานที่กรอกทั้งวันหายเงียบเมื่อส่งไม่ผ่าน (APK 1.0.39+41)
คิวส่งงานออฟไลน์เหมาว่า error 400/409/422 คือ "ล้มถาวร" แล้ว **ลบฟอร์มทิ้ง** ทั้งที่เป็น
ความผิดของข้อมูลที่แก้ได้ — แบบเดียวกับ "Validation error" ที่เจอเมื่อวาน ถ้าตอนนั้นงาน
ไปเข้าคิวแทนที่จะส่งตรง ฟอร์มทั้งใบหายไปแล้วโดยไม่มีใครรู้
- ตอนนี้เก็บฟอร์มไว้ + จำเหตุผลที่ส่งไม่ผ่าน + หยุดลองเองอัตโนมัติ (เดิมวนลองทุก 15 นาที
  พร้อมอัปโหลดรูปทั้งโฟลเดอร์ใหม่ทุกรอบ)
- ผู้ใช้แก้ฟอร์มแล้วกดส่งใหม่ = กลับไปลองส่งเองอัตโนมัติ
- ที่ยังลบทิ้งเหมือนเดิมคือกรณีที่ไม่มีอะไรให้กู้จริง ๆ (เคสถูกส่งไปแล้ว/ถูกยกเลิก/หายไป)

### เพิ่ม — ช่องโทรศัพท์รับได้แค่ตัวเลข 10 หลัก (APK 1.0.38+40)
ต่อเนื่องจากเคส #124 ที่นำเข้าไม่ผ่านเพราะเลขบัตร 13 หลักถูกกรอกลงช่องโทรศัพท์ —
ช่องโทรศัพท์ของระบบประกันรับตัวเลข 10 หลัก ยาวกว่านั้น**ตีกลับทั้งไฟล์** ไม่ใช่แค่ตัดทิ้ง
- ผู้ขับขี่รถประกัน · ผู้ขับขี่คู่กรณี · ผู้บาดเจ็บ · โทรศัพท์สำรวจ — พิมพ์ได้เฉพาะตัวเลข
  และหยุดที่ 10 หลัก กรอกผิดช่องจะรู้ตัวทันทีตั้งแต่ตอนพิมพ์
- "โทรศัพท์ที่ติดต่อได้" ของเจ้าของทรัพย์สิน**ไม่จำกัด** เพราะช่องฝั่งประกันยาว 50 ตัว
  รองรับเบอร์บ้าน/เบอร์ต่อได้
- ติ๊ก "รอตรวจสอบ" ไม่เติมคำว่า "รอตรวจสอบ" ลงช่องโทรศัพท์อีกต่อไป (เป็นตัวอักษร
  ยังไงก็ถูกกรองทิ้ง) ปล่อยว่างแล้วให้ระบบใส่ "-" ให้เอง

### แก้บั๊ก — นำเข้า EMCS ไม่ผ่านทั้งไฟล์ "ข้อมูลนำเข้ามีขนาดเกิน" (2 ช่อง)
เคส #124 นำเข้าไม่ผ่านเลยสักบรรทัด ระบบประกันตีกลับทั้งไฟล์พร้อมตารางบอกว่าช่องไหนยาวเกิน
- **โทรศัพท์ผู้ขับขี่คู่กรณี** — ช่องรับได้ 10 หลัก แต่ส่งไป 13 (พนักงานกรอกเลข 13 หลัก
  ลงช่องโทรศัพท์) ช่องโทรศัพท์อื่นตัดความยาวกันหมดแล้ว ตกหล่นเฉพาะช่องนี้ช่องเดียว
- **การเรียกร้องค่าเสียหายจากคู่กรณี** — ช่องรับได้ 18 ตัว แต่ส่งป้ายไทยไปทั้งคำ
  ("รับหลักฐานจากคู่กรณี" = 20 ตัว) และถ้าติ๊กหลายข้อจะยิ่งยาว → **ส่งเป็นค่าว่างแทน**
  เหมือนไฟล์ของระบบเก่าทุกใบ · ข้อมูลไม่หาย เพราะบอทอ่านค่านี้จากอีกทางแล้วไปติ๊ก
  ให้บนหน้าจอเอง
- ⚠️ ข้อมูลต้นทางยังผิดอยู่ — "โทรศัพท์" ของคู่กรณีเคส #124 เป็นเลข 13 หลักที่ไม่ใช่
  เบอร์โทร ตัดให้เหลือ 10 ก็ยังเป็นเบอร์ผิด ต้องแก้ที่ตัวข้อมูล

### แก้บั๊ก — คำสั่งบันทึกหายกลางทาง บอทฟ้อง "กดปุ่มแล้วไม่มีอะไรเกิดขึ้น" (se-autokey)
อาการที่กวนใจมานาน: บอทกดบันทึก ปุ่มขึ้น "Please wait..." เรียบร้อย แต่ไม่มีป๊อปอัพ
ยืนยันสักที รอ 30 วินาทีแล้วฟ้องว่ากดปุ่มไม่ติด ทั้งที่คนกดเองบนหน้าเดียวกันด้วยข้อมูล
ชุดเดิมผ่านทันที — เจอครั้งแรกตั้งแต่ ก.ค. จับต้นเหตุได้ 10 ส.ค.
- **ต้นเหตุ:** ช่องอย่าง "จังหวัด/อำเภอ/ประเภทรถ" สั่งให้ระบบประกันโหลดหน้าใหม่ทุกครั้ง
  ที่เลือก ถ้ากดบันทึกจ่อท้ายเร็วเกินไป หน้าที่โหลดใหม่จะมา**ทับคำสั่งบันทึกที่ส่งไปแล้ว**
  — งานหายเงียบ ไม่มี error ไม่มีคำเตือน
- ตอนนี้รอให้หน้านิ่งจริงก่อนกดบันทึกทุกครั้ง (เช็คว่าหน้าไม่ถูกโหลดใหม่ติดกัน 2 วินาที
  ซึ่งจับได้จริงต่างจากการเช็คว่า "โหลดเสร็จ" แบบเดิมที่ตอบว่าเสร็จทั้งที่ของใหม่กำลังจะมา)
- ถ้ายังเงียบอีกและข้อมูลครบดี **กดใหม่ให้เอง** แทนที่จะฟ้องล้มทันที
- หลักฐาน: เคส #126 เลือกจังหวัด 14:28:50 → อำเภอ 14:28:51 → กดบันทึก 14:28:52 →
  ไม่มีป๊อปอัพ; 30 วินาทีให้หลังปุ่มกลับมาเป็น "แก้ไข" กดได้ = หน้าถูกโหลดใหม่ทับจริง

### ปรับ — โหมด "เติมส่วนที่ขาด" เลิกพิมพ์ทับทั้งหน้า (se-autokey)
ชื่อโหมดบอกว่าเติมเฉพาะที่ขาด แต่ของจริงไล่พิมพ์ทับทุกช่องใหม่หมดโดยไม่เคยดูว่า
ค่าเดิมตรงอยู่แล้วหรือเปล่า — เปิด draft ที่กรอกไปแล้วมาเติมนิดเดียวก็ต้องนั่งรอ
ระบบพิมพ์ใหม่ทั้งหน้า (ทีละตัวอักษร ~60 ช่อง) เจอตอนใช้งานจริง 10 ส.ค.
- ตอนนี้อ่านค่าที่อยู่บนหน้าก่อน ตรงอยู่แล้วข้าม เขียนเฉพาะช่องที่ต่างจริง
- ตัวเลือก (dropdown) ที่ค่าตรงอยู่แล้วก็ไม่เลือกซ้ำ — เลือกซ้ำ 1 ครั้งเท่ากับ
  โหลดหน้าใหม่ทั้งหน้า เป็นตัวกินเวลาที่สุด
- **มีผลเฉพาะโหมดเติมส่วนที่ขาด** การนำเข้าครั้งแรกยังเขียนทุกช่องเหมือนเดิม
  (หน้ายังว่าง + ตัวเลือกลูกอย่าง "ยี่ห้อ/อำเภอ" ต้องรอตัวแม่สั่งให้โหลดก่อน)

### แก้บั๊ก — ลบคู่กรณีทิ้งแล้ว "คู่กรณีคันที่" ชี้ไปคันที่ไม่มีอยู่ (APK 1.0.37+39)
ผลคดี "คู่กรณีผิด" ต้องระบุว่าคันไหนผิด แต่เลขนั้นอ้างอิงลำดับคันในหมวดคู่กรณี —
ลบคันที่ 1 ทิ้ง คันที่ 2 เลื่อนขึ้นเป็นคันที่ 1 แต่เลขที่กรอกไว้ยังเป็น 2 อยู่
ค่านี้ไหลเข้าช่องของระบบประกันตรง ๆ โดยไม่มีใครดักให้
- ลบคู่กรณีแล้ว **เลขขยับตามอัตโนมัติ** (3→2 เมื่อลบคันก่อนหน้า)
- ลบ**คันที่ถูกอ้างอิงเอง** → ล้างช่องให้เลือกใหม่ ไม่เดาแทน
- หน้าตรวจก่อนส่งเตือนถ้าเลขเกินจำนวนคันที่มีจริง เช่น "คู่กรณีคันที่ = 2 แต่ในหมวด 6 มี 1 คัน"
- รูปภาพเลื่อนตามอยู่แล้วตั้งแต่เดิม (ป้าย "คันที่ 2" → "คันที่ 1")

### เพิ่ม — ติ๊ก "รอตรวจสอบ" ที่หน้าคู่กรณี (APK 1.0.36+38)
คู่กรณีหลบหนีหรือยังไม่ให้รายละเอียด แต่ช่องบังคับก็ยังต้องมีค่า ไม่งั้นบันทึกเข้า
ระบบประกันไม่ผ่าน — ติ๊กช่องเดียวบนหัวหน้าจอ ระบบเติมให้ครบทุกช่องที่บังคับ
- ช่องพิมพ์ (เจ้าของ · ทะเบียน · ชื่อ · นามสกุล · โทรศัพท์ · ที่อยู่ · บัตร ปชช.) → "รอตรวจสอบ"
- ประเภทรถ → "รถอื่นๆ" · จังหวัด → "อื่นๆ" · มีประกันภัยที่ → "อื่นๆ" ·
  เพศ → "ชาย" · คำนำหน้า → "นาย" · วันเกิด → 1 ม.ค. 2525
- **"ความสัมพันธ์กับเจ้าของรถ" ถอดจุดแดงออก** — ตรวจหน้าจอจริงแล้วไม่ใช่ช่องบังคับ
  (บังคับเฉพาะฝั่งรถประกัน) จึงไม่เติมค่าให้ด้วย ปล่อยว่างไว้ตามเดิม
- **เติมเฉพาะช่องที่ยังว่าง** ที่กรอกไว้แล้วคือของจริง ไม่ทับ · แก้ทีหลังได้ทุกช่อง ·
  เอาติ๊กออกไม่ล้างค่าคืน (ล้างแล้วของที่พิมพ์เองจะหายไปด้วย)
- **อายุคำนวณจากวันเกิดให้เอง** ทั้งตอนติ๊กและตอนเลือกวันเกิดปกติ (พิมพ์ทับได้)
- จำสถานะติ๊กไว้ เปิดคันเดิมกลับมายังติ๊กอยู่

### เพิ่ม — บังคับ 7 ช่องของคู่กรณีที่ระบบประกันเติมแทนไม่ได้ (APK 1.0.34+36)
กรอกคู่กรณีแล้วกด "บันทึกคันนี้" ผ่านฉลุยทั้งที่ **เพศ · วันเกิด · อายุ · มีประกันภัยที่**
ว่างหมด — พอถึงคิวนำเข้า ระบบประกันบล็อกการบันทึก "ทั้งบล็อกคู่กรณี" แล้วหัวหน้าต้องมา
นั่งเติมเองทุกเคส (เจอจริงกับเคลม 21BR10AVD-6908-000097 ขาดพร้อมกันทั้ง 4 ช่อง)
- ช่องข้อความที่บังคับ (เจ้าของ · ที่อยู่ · โทร · บัตร ปชช. · ใบขับขี่ · กรมธรรม์ · เลขเคลม)
  ระบบใส่ "-" แทนให้ผ่านได้ แต่ **ตัวเลือก/วันที่/ตัวเลข ใส่ "-" ไม่ได้** → ต้องมีคนกรอกจริง
- กด "บันทึกคันนี้" ทั้งที่ยังขาด → ขึ้นกล่องบอกว่าขาดช่องไหน เลือก "กลับไปกรอก" หรือ
  "บันทึกทั้งที่ยังไม่ครบ" ได้ — **ไม่บล็อกตาย** เพราะคู่กรณีหนี/ยังไม่ให้ข้อมูลก็ต้องเก็บ
  ทะเบียนกับความเสียหายไว้ก่อน (บล็อกตาย = พนักงานลบคันทิ้ง ข้อมูลที่ถ่ายมาหายหมด)
- หน้าตรวจก่อนส่งฟ้องซ้ำอีกชั้น ระบุเป็นรายคัน เช่น "คู่กรณี คันที่ 1: ขาด เพศผู้ขับขี่, อายุ"
  — เดิมหมวดคู่กรณีเป็นหมวดเดียวที่ไม่เคยตรวจรายคัน (หมวดผู้บาดเจ็บ/ทรัพย์สินมีมาก่อนแล้ว)
- ช่อง **เพศผู้ขับขี่** เพิ่งมีป้ายกำกับ + จุดแดงครั้งแรก (เดิมเป็นปุ่ม ชาย/หญิง ลอย ๆ ไม่มีป้าย)

### แก้บั๊ก — ส่งรายงานไม่ผ่านทุกครั้งที่ผลคดี = "คู่กรณีผิด" (APK 1.0.33+35)
พนักงานกรอกครบทุกหมวดแล้วกด "ส่งรายงาน" ขึ้นแดงว่า **Validation error** เฉย ๆ
ไม่บอกว่าช่องไหน — เกิดกับทุกงานที่เลือกผลคดี "คู่กรณีผิด" แล้วกรอก "คู่กรณีคันที่"
- ต้นเหตุ: ช่อง "คู่กรณีคันที่" เก็บเป็นข้อความ แต่แอปส่งเป็นตัวเลข server เลยตีกลับ
- แก้ทั้งสองฝั่ง: แอปส่งเป็นข้อความ + **server รับได้ทั้งสองแบบ** เพื่อให้เครื่องที่ยัง
  ใช้แอปเวอร์ชันเก่าส่งงานได้ทันทีโดยไม่ต้องรออัปแอป
- ข้อความ error บนแอปบอกชื่อช่องที่ไม่ผ่านแล้ว (server ส่งมาให้อยู่แล้ว แต่แอปทิ้ง)

### เพิ่ม — ติ๊ก "ไทย" ที่ช่องเลขบัตร (APK 1.0.32+34)
ทุกช่องเลขบัตร (ผู้ขับขี่รถประกัน · คู่กรณี · ผู้บาดเจ็บ) มีช่องติ๊ก **"ไทย"** อยู่แถวเดียวกับช่อง
- **ติ๊กไว้** (ค่าเริ่มต้น) — บังคับ 13 หลัก + ตรวจเลขถูกต้อง เหมือนเดิมทุกอย่าง
- **เอาติ๊กออก** — บัตรต่างด้าว/หนังสือเดินทาง พิมพ์ตัวอักษรได้ ไม่ตรวจ 13 หลัก
  (ป้ายช่องเปลี่ยนตามให้เห็นชัดว่าอยู่โหมดไหน)
- แก้ต่อ 1.0.32+34: เอาติ๊กออกแล้ว**คีย์บอร์ดยังเป็นตัวเลข พิมพ์ตัวอักษรไม่ได้**
  — ปิดคีย์บอร์ดก่อนสลับ + สร้างช่องใหม่ ให้ได้คีย์บอร์ดตรงชนิดจริง
- จำกัด 13 ตัวทั้งสองแบบ เพราะช่องรับข้อมูลของบริษัทประกันยาวได้แค่ 13 —
  พิมพ์ยาวกว่านั้นจะโดนตัดทิ้งเงียบ ๆ ตอนนำเข้า
- จำค่าที่เลือกไว้ เปิดงานเดิมกลับมาไม่ต้องเลือกใหม่ · งานเก่าถือว่าเป็นคนไทยตามเดิม
- **ต้องรัน migration 029 บน production ก่อนขึ้นเวอร์ชันนี้** (รันแล้ว)

### แก้บั๊ก — รูป "ยืนยันถึงที่เกิดเหตุ" ไปกองอยู่ "ไม่ระบุหมวด"
รูปที่แอปถ่ายให้ตอนกดปุ่มถึงที่เกิดเหตุ ไม่เคยมีป้ายหมวด เลยไปกองรวมกับรูป
ไม่ระบุหมวดบนหน้าเคส ดูเหมือนรูปหลงมา
- ตอนส่งงานจะติดป้าย **"รูปยืนยันถึงที่เกิดเหตุ"** ให้เองตั้งแต่บันทึก
- เคสเก่า 4 เคส (104, 105, 120, 125) ใส่ป้ายย้อนหลังให้แล้ว
- ไม่กระทบการนำเข้า EMCS — รูปนี้ถูกกรองทิ้งด้วยชื่อไฟล์อยู่แล้ว ไม่ได้ดูหมวด

### แก้บั๊ก — การ์ดลอยแจ้งงานใหม่ไม่เคยโผล่เลย (APK 1.0.29+31)
เปิดแอปทีไรก็มี error ซ่อนอยู่ใน log ว่าเปิดหน้าต่างลอยไม่ได้ ("หาโปรแกรมเริ่มต้น
ของหน้าต่างลอยไม่เจอ") — การ์ดที่ควรเด้งทับแอปอื่นตอนมีงานใหม่จึงไม่เคยขึ้นสักครั้ง
- ต้นเหตุ: โค้ดเริ่มต้นของหน้าต่างลอยไปอยู่ผิดไฟล์ ตัวปลั๊กอินหาไม่เจอ
- **การแจ้งเตือน + เสียง ยังทำงานปกติมาตลอด** (คนละกลไกกัน) ที่หายคือการ์ดลอยอย่างเดียว
- ต้องติดตั้ง APK ใหม่ทับถึงจะได้ — push อย่างเดียวไม่พอ

### แก้บั๊ก — นำเข้า XML แล้วเบอร์โทรผู้สำรวจภัยหายทุกครั้ง
เจอตอนลองวงจร EMCS → XML → ระบบเรา: เราส่ง `ACC_TEL` ออกไปตั้งแต่ต้นเดือน
แต่ฝั่งนำเข้าไม่เคยอ่าน tag นี้กลับเลย เบอร์จึงหายทุกไฟล์ ทั้งที่ EMCS บังคับช่องนี้
- ไฟล์เก่าจาก ISURVEY ที่ไม่มี tag นี้ยังนำเข้าได้ปกติ (ได้ค่าว่าง ไม่ error)

### แก้บั๊ก — บอทส่งเลขผิดรูปแบบ EMCS ตีกลับตอนกดบันทึก (เจอด้วยเครื่องมือใหม่)
EMCS มีชั้นตรวจที่เราไม่เคยรู้: `validForm()` ผูกกับปุ่มบันทึกหน้าหลัก บังคับ
**รูปแบบข้อความแยกตามบริษัทประกัน** 4 ช่อง (เลขที่รับแจ้ง 13 บริษัท · เลขที่เคลม 11 ·
กรมธรรม์ 7 · พ.ร.บ. 1) รูปแบบไม่ตรง = บันทึกไม่ผ่าน แม้ข้อมูลจะถูกต้อง
- เดิมบอทเช็คแค่ "มีเครื่องหมาย /" — เลขอย่าง `BR10/6905/12524` ผ่านด่านเรา
  แต่ไอโออิรับแค่ `HY2010/000001` · `ABI12345/0753` · `ABCSR0001/01/53`
- ตอนนี้เทียบแพตเทิร์นจริงรายบริษัท และเตือนล่วงหน้าว่าช่องไหนจะทำให้บันทึกไม่ผ่าน
- **พ.ร.บ.** — แก้ความเข้าใจผิด: ไม่ได้บังคับทุกบริษัท แต่บังคับเฉพาะเมื่อติ๊ก
  "มี พ.ร.บ." เท่านั้น → เลิกยัด `-` ให้เคสที่ไม่มี พ.ร.บ. (ตรงกับดีไซน์แอปมือถือพอดี)

### แอปมือถือ 1.0.27+29
- **ตัวนับโควตารูป** — หน้ารูปภาพแสดง "จำนวนรูปภาพ (52 / 80)" พร้อมป้ายเตือน
  เหลืองเมื่อถึง 60 ใบ · แดงเมื่อเกิน 80 (โควตาของ EMCS ต่อเคลม)
  **ไม่บล็อกการถ่าย** — ระบบเราเก็บรูปได้ไม่จำกัด และบล็อกแล้วช่างเสียหลักฐานถาวร
  ส่วนบอทตัดให้พอดีโควตาจริงตอนอัปพร้อมบอกว่าตกไปกี่ใบ
- **ผู้บาดเจ็บ: เลขทะเบียน** บังคับแบบมีเงื่อนไข — EMCS บังคับทุกประเภท ยกเว้น
  "บุคคลภายนอกรถ" (คนเดินถนนไม่มีทะเบียน) เดิมแอปไม่บังคับเลย

### แอปมือถือ 1.0.26+28
- **โทรศัพท์สำรวจ** ในหมวด 5 ใส่จุดแดง (บังคับ) + นับเข้าตัวนับ — EMCS บังคับช่องนี้

### แก้บั๊ก — XML ที่ส่ง EMCS ตกช่องบังคับ 3 ช่อง
เจอตอนกรอกงานจริงจนจบแล้วเปิด XML ดู (เคส #120):
- **ระดับความเสียหาย หนัก/เบา** ที่ช่างเลือก ไม่เคยไปถึง EMCS — `HEV_CAR` ถูก hardcode ว่าง
- **ลักษณะความเสียหาย** (ชนคู่กรณีเสียหาย ฯลฯ) เหมือนกัน — `LOSS_ID` hardcode ว่าง เพิ่มตารางแปลง 21 รหัส
- **โทรศัพท์ผู้สำรวจภัย** ไม่มี tag `ACC_TEL` ใน XML เลย ทั้งที่ EMCS บังคับ

### กติกาที่ยืนยันแล้ว — ใครเป็นเจ้าของข้อมูลช่องไหน
3 ช่องความเห็นบนหน้า "ค่าใช้จ่าย" ของ EMCS (ผลการดำเนินงาน · ความเห็นผู้ตรวจสอบ ·
ความเห็นเซอร์เวย์) **หัวหน้ากรอกเองทั้งหมด** ระบบเราไม่ส่งไปทับ — สิ่งที่ผู้สำรวจเขียน
คือ "รายละเอียดการเกิดเหตุ" ซึ่งเป็นคนละช่อง (ไปที่ `ACC_DETAIL` หน้าข้อมูลทั่วไป)

### แอปมือถือ 1.0.25+27 — แก้ 4 จุดที่เจอจากการทดสอบกรอกงานจริง
- **ตัวนับ "ครบ N/5 หมวด" โกหก** — หมวด 3 ผู้ขับขี่ ตรวจแค่ 4 ช่องจาก 11 ช่องที่มีจุดแดง
  กรอกแค่ ชื่อ/โทร/บัตร ก็ขึ้น "ครบ" แล้ว ทั้งที่ **ที่อยู่ปัจจุบัน** กับ **จังหวัด** ยังว่าง
  → ช่างส่งงาน แล้วหัวหน้าเปิด EMCS บันทึกไม่ผ่าน ตอนนี้ตรวจครบทั้ง 11 ช่อง
- **เขต/อำเภอ ผู้ขับขี่** ไม่เคยมีจุดแดง ทั้งที่ EMCS บังคับ → ใส่จุดแดง + นับเข้าตัวนับ
- **ฝ่ายประมาท** ไม่เคยมีจุดแดง ทั้งที่ EMCS บังคับ → ใส่จุดแดง + นับเข้าตัวนับ
- **ชิ้นส่วนที่ไม่มีด้าน** (กันชนหน้า/หลังคา/กระบะ ฯลฯ 12 ชิ้น) เดิมเลือก ซ้าย/ขวา ได้
  ทั้งที่ EMCS ไม่มีปุ่มให้ลง → ซ่อนตัวเลือกด้าน บังคับ "ทั้งหมด" ให้ตรงกับหน้าเว็บ

### แอปมือถือ 1.0.24+26
- หน้า **คู่กรณี** ตรวจทุกช่องเทียบฟอร์ม EMCS จริงแล้ว — ตรงครบ
- เปลี่ยนชื่อช่อง "วันออกบัตร" → **"ออกให้วันที่"** ให้ตรง EMCS
- ถอดช่อง "จังหวัดตามบัตร ปชช." ออก (EMCS ซ่อนช่องนี้ในบล็อกคู่กรณี)

### แก้บั๊ก
- **แบนเนอร์ "เคลมคู่" ขึ้นมั่ว** — เลขเคลมสั้นไปจับคู่กับเลขบัตรประชาชนของเคสอื่น ตอนนี้ตรวจเฉพาะเลขที่ยาวพอเป็นเลขเคลมจริง
- ไม่บังคับกรอก "เลข กม." กับ "ออกให้วันที่" — ดอกจันแดงบน EMCS สองช่องนี้ไม่ได้บังคับจริง

---

## กรกฎาคม 2569

### ⭐ นำเข้างานเก่าจาก ISURVEY (flow ที่ 2)
สำหรับงานที่ยังทำบนระบบเดิม — อัปโหลดไฟล์ XML แล้วเข้าสู่ระบบเราได้เลย

- หน้า **"นำเข้าจากไฟล์ XML"** — อัปโหลด XML + zip รูป เลือกบริษัทประกัน
- สร้างเคสอัตโนมัติ · จ่ายงานตามรหัสผู้สำรวจในไฟล์ · แตกรูปแยก 4 หมวด
- แจ้งเตือนสิ่งที่ไฟล์ไม่มีให้ผู้ตรวจกรอกเอง (ความเสียหาย · หนัก/เบา · ประเภทเคลม)
- **หน้าตรวจงานแก้ได้ทุกส่วน** — ความเสียหาย 22 ชิ้น · ผู้บาดเจ็บ 19 ช่อง · ทรัพย์สิน 7 ช่อง
- ยอดเงินจากระบบเก่าส่งต่อเข้า EMCS ได้ (งานจากแอปส่ง 0 เพราะแอปไม่มีหน้าค่าใช้จ่าย)

### ⭐ สะพานเชื่อม EMCS (se-autokey)
- ปุ่ม **"นำเข้า EMCS"** บนหน้าตรวจงาน — ส่งงานให้บอทกรอกฟอร์มแทนการคีย์มือ
- endpoint สำหรับบอท: รายการเคส · ข้อมูลเคส · รูป · บริษัทประกัน
- สถานะ "นำเข้า EMCS แล้ว" กันเลขเคลมเดิมถูก import ซ้ำ
- ปุ่มดาวน์โหลด XML สำรอง (กรณีบอทรันไม่ได้)

### ⭐ ปรับทุกช่องให้ตรง EMCS
- ตัวเลือกทั้งหมด sync จาก master จริงของ EMCS — ยี่ห้อรถ **350 ตัวแยกตามประเภทรถ** · สี 55 · ความสัมพันธ์ 40 · ประเภทใบขับขี่ 21 · ผู้บาดเจ็บ 5 · ระดับบาดเจ็บ 6
- **ลักษณะการเกิดเหตุครบ 79 ตัว** (เดิมแปลงรหัสได้แค่ 26 → 53 สาเหตุที่เลือกได้ส่งออกเป็นค่าว่าง)
- ชื่อชิ้นส่วนความเสียหายตรง checklist 22 ชิ้นของ EMCS แบบคำต่อคำ
- แปลงหน่วยให้ EMCS รับ: ปี พ.ศ.→ค.ศ. · เบอร์โทร · จำนวนเงิน · เพศเป็นรหัส M/F
- เขต/อำเภอ cascade ตามจังหวัด (เดิมโชว์ 51 เขต กทม. กับทุกจังหวัด)

### ⭐ ยกเครื่องหน้ากรอกข้อมูลสำรวจ (Hub 8 หมวด)
- เปลี่ยนจากฟอร์มยาวเป็น **Hub 6+ หมวด** กดเข้าทีละหมวด
- คู่กรณี / ผู้บาดเจ็บ / ทรัพย์สิน เพิ่มได้หลายรายการ (สูงสุด 20/32/30)
- **แผนภาพความเสียหาย** แตะเลือกชิ้นส่วน + ระดับ
- **OCR สแกนบัตรประชาชน + ใบขับขี่** — ถ่าย 1 ครั้งได้ทั้งรูปและข้อมูล พร้อมธงเตือนเมื่อ OCR ไม่มั่นใจ
- กล้องในแอป — ถ่ายรัว · สลับกล้อง · เลือกความละเอียด · เลือกหมวดก่อนถ่าย · เพิ่มจากคลังภาพได้
- autosave ทุกช่อง + คิวออฟไลน์ + กันกดส่งซ้ำ
- ตรวจความครบถ้วน 3 ชั้น + หน้าสรุปก่อนส่ง

### แก้บั๊กสำคัญ
- **ความปลอดภัย** — ปิด `/uploads` ต้องยืนยันตัวตน · จำกัดจำนวนครั้ง login · กัน path traversal · ยกเลิก session ได้
- ข้อมูลฟอร์มหายหลังปิด-เปิดแอป (รายการความเสียหาย · ชื่อผู้ขับขี่ · ค่าที่ล้าง)
- placeholder "-- ระบุ --" หลุดเป็นค่าจริงเข้า DB → รหัสจังหวัดใน XML ว่าง
- กันข้อมูล **"เคลมคู่"** (อุบัติเหตุเดียวกัน คนละเคลม) ปนกัน — แบนเนอร์เตือนบนแอป
- **เลขเซอร์เวย์ห้ามซ้ำ** (เป็นเลขอ้างอิงเบิกเงิน) ส่วนเลขเคลมซ้ำได้
- รูปถ่ายผูกกับเคสถูกใบ + อัปโหลดทีละไฟล์แบบ resume ได้

---

## มิถุนายน 2569 — ระบบงานบุคคล/ปฏิบัติการ + ขึ้น production

### ⭐ ขึ้นใช้งานจริง
- deploy บน VPS ผ่าน Dokploy — `survey.sesurvey.cloud` + `api.sesurvey.cloud`
- แอปมือถือชี้ production + เซ็น APK ด้วย release key แจกพนักงาน

### ⭐ ลงเวลาเข้า-ออกงาน + ลางาน
- ลงเวลาพร้อม **ถ่ายรูป + พิกัด GPS** · ลงได้หลายรอบ/วัน · logout = ลงเวลาออกอัตโนมัติ
- ปิดรอบค้างข้ามวันอัตโนมัติ · ใช้เวลาไทย (Asia/Bangkok) ทั้งระบบ
- ระบบใบลา — ยื่นจากแอป อนุมัติโดย admin
- หน้าเว็บ: ตารางเวลาเข้า-ออกรายเดือน · ค้นหา · สรุป · ส่งออก CSV

### ⭐ ตารางเวรประจำจุด + บอร์ดเข้างาน
- ตารางเวรจากไฟล์ Excel จริง · เวร 1/2/3 + FIX (FIX8/FIX10/FIX14) + เวรหยุด
- **เดือนใหม่หมุนเวรต่อจากเดือนก่อนอัตโนมัติ** (ตั้งไว้ถึง ก.ค. 2570)
- บอร์ด "เวลาเข้างานพนักงาน" — การ์ดรายจุด แยกในระบบ/นอกระบบ/หยุด-ลา · เรียงคิวความสำคัญ · อาสาสมัคร
- ครอบคลุมทั้ง กรุงเทพ · ปริมณฑล · **ต่างจังหวัด 17 จังหวัด**
- บังคับ 1 คน 1 รหัส SE + เตือนเมื่อรหัสซ้ำในตารางเวร

### อื่น ๆ
- **OCR ใบรับแจ้งเคลม** — เปลี่ยนจาก Typhoon เป็น Gemini + Vision ดึง 5 เลขสำคัญอัตโนมัติ
- หน้าสร้างเคสใช้รูปใบเคลมทั้งใบ + รวมขั้นมอบหมายช่างไว้หน้าเดียว
- รายงานการโทรปรึกษาหัวหน้า (เลือกช่วงวันที่ได้)
- แผนที่พิกัดพนักงาน — เรียกพิกัดสดผ่าน FCM
- รองรับสถานะ "ปฏิเสธงาน"

---

## เมษายน 2569 — ระบบแจ้งเตือนงานใหม่

- **แจ้งเตือนแบบสายเรียกเข้า** — เต็มจอ + เสียงเตือนวนจนกว่าจะกดรับ
- ทำงานได้ทุกสถานะของแอป (เปิด/ปิด/ถูก kill) ผ่าน FCM + service ฝั่ง Android
- เลือกรูปแบบตามสถานะหน้าจอ (เต็มจอ / แถบแจ้งเตือน) + ปุ่มปิดเสียง
- ยืนยันถึงที่เกิดเหตุด้วยการถ่ายรูป + เก็บรูปลงโฟลเดอร์ประจำเคส
- ปฏิเสธงานได้ + รายการงานรีเฟรชอัตโนมัติ

---

## มีนาคม 2569 — วางรากฐาน

- โครงระบบ 3 ส่วน: มือถือ · เว็บ · backend + PostgreSQL
- ระบบผู้ใช้ 4 บทบาท: ผู้สำรวจ · call center · ผู้ตรวจ · admin
- สร้างเคส → มอบหมาย → สำรวจ → ตรวจ → อนุมัติ
- หน้ารายละเอียดเคสบนเว็บ ออกแบบตามฟอร์มประกันจริง + แก้ไขได้
- อัลบั้มรูป (ซูม · เลื่อน · thumbnail)
- ตารางค่าใช้จ่าย
- แผนที่ตำแหน่งพนักงาน (Leaflet/OpenStreetMap)
- OCR ใบเคลมรุ่นแรก + date picker แบบ พ.ศ.

---

## หมายเหตุการดูแลระบบ

- **migration รันด้วยมือบน production** — ล่าสุดคือ `028_case_source.sql`
- `DATABASE_URL` ในเครื่องพัฒนาชี้ฐานข้อมูลเดียวกับ production — ระวังตอนทดสอบ
- push ขึ้น `main` → Dokploy deploy อัตโนมัติ (backend กับ web แยกกัน)
- แอปมือถือต้อง build APK ใหม่ + ติดตั้งทับทุกเครื่อง โค้ดที่ push ไม่ถึงมือถือเอง
