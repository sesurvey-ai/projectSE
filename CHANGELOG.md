# บันทึกการเปลี่ยนแปลง — SE Survey

ระบบสำรวจภัยรถยนต์: แอปมือถือ (Flutter) · เว็บ (Next.js) · backend (Node/TS + PostgreSQL)
เชื่อมกับ **EMCS** ผ่านบอท `se-autokey` และรองรับงานเก่าจากระบบ **ISURVEY**

ไล่จากใหม่ → เก่า · วันที่เป็น พ.ศ. ตามที่ใช้ในระบบ

---

## สิงหาคม 2569

### เพิ่ม — การ์ดคู่กรณีโชว์ครบขึ้น (เพศ · วันเกิด · ประเภทใบขับขี่ · เลขตัวถัง)
วันเกิดเป็นช่องบังคับของระบบประกัน แต่การ์ดคู่กรณีตอนยังไม่กดแก้ไขกลับไม่แสดงเลย
ผู้ตรวจจึงไม่เห็นว่าขาดจนกว่าจะไปติดตอนนำเข้า

### ปรับ — รวมรายการตัวเลือกของแอปไว้ที่เดียว (แอป 1.0.53+55)
ลิสต์ที่ลอกมาจากระบบประกัน (ประเภทประกัน · คำนำหน้า · ความสัมพันธ์ 39 รายการ · ประเภทรถ ·
ประเภทรถไฟฟ้า) ถูกประกาศซ้ำในหน้าฟอร์มอีกชุด — แก้ที่ไฟล์กลางแล้ว**ไม่มีผลกับหน้าฟอร์ม**
เนื้อหายังตรงกันดีอยู่ แต่เป็นระเบิดเวลาแบบเดียวกับที่เคยทำให้รถประกันกับคู่กรณีเก็บค่าคนละแบบ
· ตรวจแล้วทั้ง 3 ลิสต์ตรงกันเป๊ะก่อนรวม (39/7/6 รายการ) ไม่มีค่าไหนเปลี่ยน

### แก้บั๊ก — เวลาแจ้งความ/เวลานัดหมาย หายทุกครั้งที่ผู้ตรวจกดบันทึก
ช่างกรอกเวลามาจากหน้างาน แต่หน้าตรวจงานไม่เคยดึงเวลามาแสดงในช่องชั่วโมง/นาที (ขึ้นว่างเสมอ)
พอกดบันทึกครั้งเดียว **เวลาถูกล้างทิ้งทั้งคู่** เหลือแต่วันที่ แล้วไหลต่อเป็นเวลาว่างเข้าระบบประกัน

### เพิ่ม — ระบบเติม "วันเวลาสำรวจเสร็จ" ให้เองตอนกดส่งงาน
ระบบประกันบังคับช่องนี้เสมอ แต่ไม่มีใครในระบบเรากรอก — ไฟล์ที่ส่งไปจึงมีช่องว่างและ
**บันทึกหน้าหลักไม่ผ่าน** (เรื่องถูกสร้างแล้วแต่ค้างกลางทาง) · ตอนนี้ระบบจับเวลาไทยให้ตอนช่างกดส่ง
ไม่ทับของเดิมถ้ามีอยู่แล้ว · หน้าตรวจงานติดดาวช่องนี้และไทม์ไลน์อีก 3 ช่องให้เห็นด้วย

### แก้บั๊ก — "ผลคดี" ที่ผู้ตรวจแก้บนเว็บ ทำให้แอปเลิกตรวจช่องบังคับ
เว็บกับแอปเก็บผลคดีคนละสำเนียง (เว็บเก็บป้ายเต็ม แอปเก็บค่าสั้น) พอผู้ตรวจแก้บนเว็บ
ตรรกะในแอปที่เทียบตรง ๆ ก็เลิกทำงาน → **เลิกบังคับ "คู่กรณีคันที่" กับ "การเรียกร้องค่าเสียหาย"**
ทั้งที่ระบบประกันยังบังคับ · ตอนนี้เว็บเก็บค่าเดียวกับแอป และแอปรองรับของเก่าที่ค้างอยู่ใน DB ด้วย

### แก้บั๊ก — ช่องที่พิมพ์อิสระแล้วค่าหายเงียบ (ประเภทประกัน · ประเภทรถไฟฟ้า)
ปลายทางอ่านเฉพาะรูปแบบที่กำหนด — พิมพ์ "ชั้นหนึ่ง" แทน "ชั้น 1" ได้ค่าว่างโดยไม่มีอะไรเตือน
· เปลี่ยนเป็นตัวเลือกที่ตรงกับระบบประกัน ทั้งของรถประกันและของคู่กรณี
· ถอดช่อง "สาขาผู้สำรวจ" ที่มีตัวเลือกเดียวและเขียนค่าว่างทับทุกครั้งออก

### เพิ่ม — ช่องบังคับของคู่กรณีครบตามระบบประกัน (แอป 1.0.52+54)
ระบบประกันบังคับ 8 ช่องต่อคัน แต่แอปเช็คแค่ 7 และ**ไม่มี "เจ้าของรถ"** — บอทเลยต้องใส่ `-` แทน
ทุกใบ สำนวนคู่กรณีของบริษัทประกันจึงได้ชื่อเจ้าของรถเป็นขีดกลางมาตลอด
- เพิ่ม **เจ้าของรถ** เข้าเงื่อนไขทั้งในแอปและหน้าแก้คู่กรณี
- **เวลาเกิดเหตุ** (ชั่วโมง/นาที) เดิมส่งงานได้ทั้งที่เว้นว่าง — ระบบประกันบังคับ
- **เลข พ.ร.บ.** เมื่อเปิดสวิตช์ "มี พ.ร.บ." — เดิมมีจุดแดงแต่ไม่มีใครตรวจ และเลขว่าง
  ทำให้ติ๊ก "มี พ.ร.บ." หายทั้งใบ
- **เลขทะเบียนรถของผู้บาดเจ็บ** (ยกเว้นบุคคลภายนอกรถ) — เดิมตรวจแค่ตอนกดบันทึกรายคน
  ระเบียนที่มาจากงานค้าง/นำเข้าไฟล์เลยหลุด
- **ถอดจุดแดงของ "ยี่ห้อรถ"** — ระบบประกันไม่ได้บังคับ และไม่เคยมีใครตรวจ = จุดแดงลอย

### แก้บั๊ก — บอทยังเอา "หมายเหตุ" ไปใส่ช่องความเห็นของเซอร์เวย์
เมื่อวานแก้ฝั่งไฟล์ XML ไปแล้วว่าหมายเหตุเป็นโน้ตภายใน ห้ามส่งเข้าระบบประกัน แต่ฝั่งบอท
ยังใช้ของเดิมอยู่ → เคสที่มีแต่หมายเหตุ ข้อความภายในจะถูกพิมพ์ลงสำนวนประกันแบบเงียบ ๆ
· และเติม 3 ช่องที่บอทลืมกรอกในเส้นทางงานมือถือ (อีเมลผู้เอาประกัน · ค่าเสียหายส่วนแรก · ลำดับผู้ขับขี่)

### เพิ่ม — แก้ข้อมูลคู่กรณีบนหน้าตรวจงานได้แล้ว (เดิมดูได้อย่างเดียว)
คู่กรณีเป็นบล็อกเดียวที่ผู้ตรวจแก้ไม่ได้เลย ทั้งที่ระบบประกันบังคับ 7 ช่องต่อคัน และ 4 ใน 7
(ประเภทรถ · เพศ · วันเกิด · อายุ) เป็นตัวเลือก/วันที่/ตัวเลข ที่ใส่ "-" แทนไม่ได้ — ขาดแล้ว
บอทค้างกลางทาง ผู้ตรวจได้แต่โทรตามให้ช่างแก้จากแอป
- แก้ได้ 25 ช่อง · เพิ่ม/ลบคันได้ · ติ๊ก KFK ได้ · **แก้ค้างไว้ได้จนกว่าจะกดบันทึก** เหมือนบล็อกอื่น
- ป้ายเตือน "ยังขาด N ช่องบังคับ" อัปเดตสด ๆ ระหว่างพิมพ์ กรอกครบแล้วป้ายหายเอง
- ตัวเลือกยี่ห้อรถเปลี่ยนตามประเภทรถ · เขต/อำเภอเปลี่ยนตามจังหวัด เหมือนบล็อกรถประกัน
- **รายการความเสียหายของคู่กรณียังแก้ได้เฉพาะในแอป** — หน้าเว็บแสดงจำนวนไว้เฉย ๆ
  แต่รับประกันว่าไม่หายตอนบันทึก

### เพิ่ม — ดอกจันช่องบังคับบนหน้าตรวจงาน (เดิมมีอยู่ช่องเดียวจาก 31 ช่อง)
ผู้สำรวจเห็นจุดแดงบนแอปมาตลอด แต่หน้าตรวจงานไม่เคยมี — ผู้ตรวจแก้ข้อมูลแล้วเผลอลบ
ช่องบังคับทิ้งได้โดยไม่มีอะไรเตือน กว่าจะรู้ก็ตอนส่งเข้าระบบประกันไม่ผ่าน
- **แดง 25 ช่อง** = บังคับเสมอ · **ส้ม 6 ช่อง** = บังคับเฉพาะบางกรณี (ชี้เมาส์ดูเงื่อนไขได้)
  เช่น เลขใบขับขี่ (เมื่อมีใบขับขี่) · คู่กรณีคันที่ + การเรียกร้อง (เมื่อคู่กรณีผิด) ·
  ชื่อพนักงานสอบสวน + สถานีตำรวจ (เมื่อรอสรุปผลคดี/มีการแจ้งความ)
- ผู้บาดเจ็บ/ทรัพย์สินติดดาว**รายคน-รายชิ้น** (เดิมมีดาวช่องเดียว) — ขาดช่องเดียว
  บันทึกทั้งบล็อกไม่ผ่าน ไม่ใช่แค่ช่องนั้น
- การ์ดคู่กรณีขึ้นเตือนว่า**คันไหนขาดช่องบังคับอะไรบ้าง** — 4 ใน 7 ช่องเป็นตัวเลือก/วันที่/ตัวเลข
  ที่ใส่ "-" แทนไม่ได้ ขาดแล้วบอทค้างกลางทาง · ⚠️ หน้านี้ยัง**แก้คู่กรณีไม่ได้** ต้องให้ผู้สำรวจ
  แก้จากแอป (ทำหน้าแก้คู่กรณีเป็นงานแยก)
- ลิสต์ทั้งหมดยกมาจากแอปมือถือซึ่ง sync กับตัวตรวจของระบบประกันอยู่แล้ว — ที่เดียวกัน ไม่แยกกติกา

### เพิ่ม — 5 ช่องที่แอปเก็บมาแล้วแต่หน้าตรวจงานไม่เคยแสดง
ข้อมูลอยู่ในระบบครบ แต่ผู้ตรวจมองไม่เห็นและแก้ไม่ได้
- **รถไฟฟ้า 3 ช่อง**: หมายเลขแบตเตอรี่ · วันเริ่มใช้แบตเตอรี่ · หมายเลขเครื่องชาร์จ
- **สัญชาติผู้ถือบัตร** (คนไทย/ต่างชาติ) — ตัวกำหนดว่าเลขบัตรต้องผ่านการตรวจ 13 หลักหรือไม่
- **หมายเหตุเพิ่มเติม** ที่ผู้สำรวจจดจากหน้างาน (เป็นโน้ตภายใน ไม่ส่งเข้าระบบประกัน)

### เพิ่ม — 3 ช่องความเห็นที่หัวหน้าเขียนบนเว็บ ส่งไปกับไฟล์ XML แล้ว
"ผลการดำเนินงาน" · "ความเห็นของผู้ตรวจสอบ" · "ความเห็นของเซอร์เวย์" — มีช่องให้กรอกบนหน้า
ตรวจงานมานานแล้ว แต่ไฟล์ที่ส่งออกไปกลับส่งค่าว่างเสมอ หัวหน้าต้องไปพิมพ์ซ้ำอีกรอบในระบบประกัน
· ตอนนี้กรอกที่เดียวจบ
- **ไม่มีข้อมูล = ส่งช่องว่าง** ซึ่งฝั่งบอทข้ามให้เอง **ไม่ไปลบของเดิม**ที่หัวหน้าเคยพิมพ์ไว้ในระบบประกัน
- **คงการขึ้นบรรทัดใหม่** — งานจริงเขียนเป็น bullet หลายบรรทัด ยุบบรรทัดแล้วอ่านไม่ออก
- "รายละเอียดการเกิดเหตุ" เป็นคนละช่อง (อยู่แท็บข้อมูลทั่วไป) — ต่อสายไว้อยู่แล้ว ไม่ได้แก้อะไร
- **"หมายเหตุเพิ่มเติม" ที่ผู้สำรวจจดในแอป จะไม่ถูกเอาไปใช้เป็น "ความเห็นของเซอร์เวย์" อีกแล้ว** —
  คนละเรื่องกัน อันหนึ่งเป็นโน้ตส่วนตัว อีกอันเป็นข้อความทางการบนสำนวนของบริษัทประกัน
  · ไม่ได้กรอกช่องความเห็น = ส่งว่างไปเลย ไม่หยิบของอื่นมาแทน

### แก้บั๊ก — "ความเห็นของเซอร์เวย์" ส่งออกไป 2 ที่ ค่าไม่ตรงกัน
ไฟล์มีช่องนี้อยู่ 2 จุดตามรูปแบบที่ระบบประกันกำหนด (ไฟล์ของระบบเก่าก็มี 2 จุดเหมือนกัน)
แต่ของเราจุดหนึ่งใส่ค่า อีกจุดปล่อยว่าง — **ผลที่ได้จึงขึ้นอยู่กับว่าปลายทางอ่านจุดไหน**
ซึ่งไม่มีใครรู้ · แก้เป็นค่าเดียวกันทั้ง 2 จุด ได้ผลเหมือนกันไม่ว่าอ่านทางไหน

### เพิ่ม — งานจากแอปมือถือส่งยอด "เรียกเก็บประกัน" ไปกับไฟล์ XML ได้แล้ว
เดิมยอดค่าใช้จ่ายจะติดไปกับไฟล์**เฉพาะงานที่นำเข้าจากระบบเก่า**เท่านั้น งานจากแอปมือถือ
ต่อให้ผู้ตรวจกรอกราคาไว้บนเว็บ ไฟล์ก็ออกไปเป็น 0.00 ทุกช่องแบบเงียบ ๆ ไม่มีอะไรฟ้อง
— กติกานั้นมาจากตอนที่หัวหน้ายังกรอกราคาในระบบประกันเอง แต่ตอนนี้กรอกบนเว็บเราแล้ว
- ช่อง "ราคา/หน่วย · เรียกเก็บประกัน" บนหน้าตรวจงาน**มีผลจริงกับไฟล์แล้ว**
- งานนำเข้าจากระบบเก่ายังทำงานเหมือนเดิมทุกประการ
- **ไฟล์ที่สกัดกลับมาจากระบบประกัน (ข้อมูลทดสอบ) ยังถูกกันไว้เหมือนเดิม** — ส่งกลับเข้าไป
  เท่ากับเอาตัวเลขของบริษัทประกันเองยัดกลับไปเป็นบิลใบใหม่
- ที่มาแบบใหม่ที่ยังไม่มีในระบบจะถูกกันไว้ก่อนโดยอัตโนมัติ (ได้ 0.00) ไม่ใช่ปล่อยผ่าน

### แก้บั๊ก — ล้างช่องค่าใช้จ่ายทิ้งแล้วยอดเดิมไม่หาย (ยังติดไปกับไฟล์อยู่)
กรอกราคาไว้แล้วเปลี่ยนใจ ลบตัวเลขออกให้หมดทุกช่องแล้วกดบันทึก — **ระบบไม่ทำอะไรเลย**
ยอดเดิมยังอยู่ในฐานข้อมูลและยังติดไปกับไฟล์ที่ส่งเข้าระบบประกัน ทั้งที่หน้าจอว่างแล้ว
· การล้างช่องเป็นวิธีเดียวที่หน้าเว็บมีให้ลบบิล จึงเป็นวิธีที่ห้ามถูกเมิน
· บันทึกฟอร์มอื่นที่ไม่มีช่องค่าใช้จ่าย (ส่งงานจากมือถือ / หน้ารีวิว) ยังไม่แตะยอดเดิมเหมือนเดิม

### เพิ่ม — เทสคุมตารางค่าใช้จ่ายในไฟล์ XML (เดิมไม่มีเลยสักตัว)
ยอดฝั่ง**จ่ายพนักงาน**มีเทสคุมอยู่แล้ว แต่ยอดฝั่ง**เรียกเก็บประกัน** ซึ่งเป็นตัวเลขที่วิ่ง
เข้าระบบของบริษัทประกันจริง กลับไม่มีอะไรคุม · เพิ่ม 2 ชุด: ไม่มียอด → ต้องเป็น 0.00 ทุกช่อง
(ไม่ใช่หายไปทั้งบล็อก) · มียอดครบ → ต้องออกตรงทุกช่อง
- **บวกการ์ดกันเงินสองฝั่งปนกัน** — ตารางยอดจ่ายพนักงานกับยอดเรียกเก็บประกันมีชื่อช่อง
  ซ้ำกันเป๊ะ 2 ตัว (ค่าโทรศัพท์ · ค่าประกันตัว) ซึ่งเป็นช่องที่ไปโผล่ในไฟล์พอดี
  ถ้าวันหน้ามีใครเผลอต่อสายผิดตาราง เงินเดือนพนักงานจะกลายเป็นบิลเรียกเก็บบริษัทประกัน
  **โดยไม่มี error อะไรเลย** — เทสตัวนี้คือด่านเดียวที่จับได้

### เพิ่ม — หน้า "เรทค่าตอบแทน" ของผู้ดูแลระบบ (แก้เรทเองได้ ไม่ต้องพึ่งคนเขียนโปรแกรม)
เดิมเรทอยู่ในฐานข้อมูลของระบบเก่าและ**ไม่มีหน้าจอให้แก้เลย** ใครจะเปลี่ยนต้องให้คนเขียน
โปรแกรมเข้าไปแก้ให้ ตอนนี้อยู่ในเมนูผู้ดูแลระบบ แบ่งเป็น 6 แท็บ
- **เรทรายอำเภอ** (337 อำเภอ) — กรองตามจังหวัด/ค้นหาชื่อได้ · เลือกจังหวัดหรือค้นหาแล้วจะเห็น
  **อำเภอที่ยังไม่มีเรท**ด้วย กดแก้เพื่อเพิ่มเรทให้พื้นที่ให้บริการใหม่ได้ทันที
- **เรทรายจังหวัด** (ตัวสำรองเมื่ออำเภอไม่มีเรทของตัวเอง) · **ตำบลพิเศษ** · **ทีมผู้สำรวจ**
- **ค่าคงที่** — ค่านอกพื้นที่/นอกเวลา และเรทที่เดิม**ฝังอยู่ในโค้ดส่วนขยาย** ซึ่งแก้ทีต้องสร้างใหม่
  แล้วแจกทุกเครื่อง
- **แยกสีให้เห็นว่าช่องไหนคือเงินพนักงาน ช่องไหนคือเงินที่เรียกเก็บประกัน** — เงินสองฝั่งนี้
  หน้าตาเหมือนกันแต่คนละเรื่อง กรอกสลับกันแล้วจ่ายผิดโดยไม่มีอะไรฟ้อง
- **เว้นว่าง ≠ ใส่ 0** — เว้นว่างคือไม่มีเรท (ไปหาจากระดับที่กว้างกว่า) ส่วน 0 คือมีเรทและเป็นศูนย์
- เตือนเมื่อมี**ผู้สำรวจรหัส SEC ที่ยังไม่ได้จับคู่ทีม** (คนกลุ่มนี้จะได้เรทกลางของอำเภอเสมอ
  ต่อให้อำเภอนั้นตั้งเรทรายทีมไว้) · รหัส SE ของกรุงเทพฯ ไม่นับเพราะใช้เรทกลางอยู่แล้ว
- แถบเตือนบนหัวหน้า: **เรทชุดนี้เป็นของ se-survey แยกต่างหาก** แก้ที่นี่แล้วระบบเก่าไม่เปลี่ยนตาม

### เพิ่ม — ประวัติการแก้เรท (ใครแก้ช่องไหน จากเท่าไหร่เป็นเท่าไหร่)
เรทคือเงินของพนักงาน แก้ผิดช่องเดียว = จ่ายผิดทั้งอำเภอโดยไม่มีใครรู้ · พอเปิดให้แก้ผ่าน
หน้าเว็บได้ ความเสี่ยงก็ขยับขึ้นทันที ทุกการแก้จึงถูกบันทึกไว้ดูย้อนหลังได้ในแท็บของมันเอง
**ไม่ใช่ระบบเวอร์ชันเรท** — ตัวคิดเงินยังใช้ค่าปัจจุบันค่าเดียวเหมือนเดิม แก้แล้วทับตามที่ตกลงกันไว้
- ⚠️ ต้องรัน migration `038_billing_rate_changes.sql` บน production (รันแล้ว)

### เพิ่ม — ใบเบิกเงินค่าตอบแทนผู้สำรวจ (ดาวน์โหลด Excel จากหน้ารายการงานตรวจสอบ)
เลือกช่วงวันที่แล้วกดปุ่มเดียว ได้ไฟล์ Excel เอาไปทำเรื่องเบิกจ่ายได้เลย
- **คอลัมน์เรียงตามใบเดิมที่ใช้กันอยู่** — คนทำเรื่องเบิกเงินอ่านใบนี้มาตลอด เปลี่ยนลำดับ
  หรือชื่อคอลัมน์แล้วต้องมาไล่หาใหม่ทุกเดือน
- แยกให้เห็นทั้ง **ฝั่งเรียกเก็บประกัน** และ **ฝั่งจ่ายพนักงาน** ในแถวเดียวกัน
  พร้อมค่านอกพื้นที่/นอกเวลา/หักเงิน/เหตุผล แยกคอลัมน์
- หักเงินแสดงเป็นเลขติดลบ · มีแถวรวมยอดท้ายตาราง · ตรึงหัวตารางไว้ตอนเลื่อน
- กรองตามวันที่คิดเงิน **เทียบเป็นวันตามเวลาไทย** ไม่ใช่ UTC — ไม่งั้นงานตอนเย็นตกไปอยู่วันถัดไป
- ครอบคลุมเฉพาะงานที่คิดเงินผ่านระบบนี้ · งานที่ยังทำผ่านระบบเก่ายังออกใบจากที่เดิม

### แก้บั๊ก — ติ๊ก "นอกพื้นที่/นอกเวลา" แล้วยอดไม่ขยับ
ระบบเก็บแค่ว่า "ติ๊กหรือไม่ติ๊ก" แต่ไม่มีที่เก็บ**จำนวนเงิน** — โค้ดจึงไปอ่านค่าที่ไม่มีอยู่จริง
แล้วได้ 0 ทุกครั้ง **ติ๊กไปก็เท่านั้น ยอดที่ระบบแนะนำเท่าเดิม** และใบเบิกเงินก็แยกไม่ออกว่า
จ่ายค่าอะไรไปเท่าไหร่ · ตอนนี้เก็บยอดจริงแยกช่อง (ไม่ใส่เลข = ใช้ค่าตั้งต้น นอกพื้นที่ 50 ·
นอกเวลา 100 แต่แก้เป็นเลขอื่นได้รายงาน เหมือนระบบเดิม)

### เพิ่ม — ช่อง "ราคาพนักงาน" บนหน้าตรวจงาน (ผู้ตรวจกรอกยอดจ่ายผู้สำรวจได้แล้ว)
ตารางค่าใช้จ่ายเดิมมีแต่ยอดที่**เรียกเก็บบริษัทประกัน** — ยอดที่**จ่ายผู้สำรวจ**ไม่มีที่กรอก
และไม่มีที่เก็บ ตอนนี้เพิ่มคอลัมน์ที่สองข้าง ๆ กัน เห็นทั้งสองฝั่งพร้อมกันในแถวเดียว
เหมือนที่ระบบเก่าทำ
- ครบทั้ง 8 รายการ: ค่าบริการ · ค่าเดินทาง · ค่ารูปถ่าย · ค่าโทรศัพท์ · ค่าประกันตัว ·
  ค่าเรียกร้อง · ค่าคัดประจำวัน · ค่าใช้จ่ายอื่นๆ
- **ใต้ตารางมีตัวปรับ 4 อย่าง**: นอกพื้นที่ · นอกเวลา · ตำบลพิเศษ · ค่าคัดประจำวัน (ถูก/ผิด/รอผล)
- **แยก "หักเงิน" เป็นแถวของตัวเอง** พร้อมเหตุผล (ส่งช้า · เอกสารไม่ครบ · หรือพิมพ์เอง) —
  ระบบเดิมยัดหักเงินไว้ในช่อง "ค่าใช้จ่ายอื่นๆ" เพราะส่วนขยายแทรกช่องใหม่ลงฟอร์มระบบเก่าไม่ได้
  ผลคือ**กรอกรายจ่ายอื่นจริง ๆ ไม่ได้เลย** เพราะจะกลายเป็นหักเงินทันที · เว็บนี้เราคุมเอง
  จึงแยกให้ถูกความหมาย และ **หักเงินโดยไม่ระบุเหตุผลจะบันทึกไม่ผ่าน**
- **บอกที่มาของยอดใต้ตาราง** — ใช้เรทของจังหวัด/อำเภอไหน ทีมไหน ระบบแนะนำเท่าไหร่
  รวมที่บันทึกไว้เท่าไหร่ · ถ้าแปลงพื้นที่ของเคสไม่ได้จะขึ้นแถบเหลืองบอกตรง ๆ ว่า
  หาเรทอัตโนมัติไม่ได้ ให้กรอกเอง — **ไม่เงียบแล้วปล่อยให้ยอดว่าง**
- เฉพาะผู้ตรวจกับผู้ดูแลระบบเท่านั้นที่เข้าถึงได้ — **ผู้สำรวจไม่เกี่ยวกับการตั้งยอดจ่ายตัวเอง**

### เพิ่ม — แปลงชื่อจังหวัด/อำเภอในเคส → รหัสพื้นที่ ให้หาเรทได้อัตโนมัติ
เคสเก็บพื้นที่เป็น**ข้อความไทย** ("อำเภอธัญบุรี") แต่ตารางเรทอ้าง**รหัส** — ถ้าแปลงไม่ได้
ก็หาเรทไม่เจอ คิดค่าตอบแทนไม่ได้ทั้งงาน · ตอนนี้แปลงให้อัตโนมัติแล้ว
- **ครอบคลุมทุกพื้นที่ที่เลือกได้ในแอป: 78/78 จังหวัด · 928/928 อำเภอ**
- รองรับที่เขียนไม่ตรงกัน: "อำเภอเมือง" ↔ "เมืองชลบุรี" · "กรุงเทพ ฯ" ↔ "กรุงเทพฯ" ·
  บางจังหวัดแอปเขียนอำเภอเมืองด้วยชื่อจังหวัด ("อำเภอบึงกาฬ" ↔ "เมืองบึงกาฬ")
- **"เบตง" ในแอปอยู่ในระดับจังหวัด** ทั้งที่จริงเป็นอำเภอหนึ่งของยะลา — จับให้ลงยะลา
  ไม่งั้นงานในพื้นที่นั้นหาเรทไม่เจอ

⚠️ **รหัสพื้นที่ในระบบมี 2 ชุด อย่าสลับกัน** — ชุดของพอร์ทัลประกัน (ชลบุรี = 9) กับ
ชุดมาตรฐานไทยที่ตารางเรทใช้ (ชลบุรี = 20) สลับแล้วจะได้เรทของจังหวัดอื่นแบบไม่มีใครรู้
มีเทสดักไว้แล้ว

### เพิ่ม — ระบบคิดค่าตอบแทนผู้สำรวจย้ายมาอยู่ที่เซิร์ฟเวอร์เรา
เดิมสูตรคิดเงินอยู่ในส่วนขยายเบราว์เซอร์ (2,119 บรรทัด) ทำงานได้เฉพาะตอนเปิดหน้าระบบเก่า —
**ปิดระบบเก่าเมื่อไหร่ก็ไม่มีใครคิดเงินได้ ถึงตารางเรทจะยังอยู่ครบ** ตอนนี้ย้ายมาคิดที่
เซิร์ฟเวอร์ของเราแล้ว ไม่ผูกกับหน้าเว็บไหนอีก
- คิดครบทุกกรณีที่ระบบเดิมทำ: เรทตามอำเภอ · เรทที่ต่างกันตามทีมผู้สำรวจ ·
  ตำบลที่จ่ายไม่เท่าอำเภอแม่ · ค่านอกพื้นที่ · ค่านอกเวลา · หักเงิน ·
  ค่าเรียกเก็บที่ต่างกันตามประเภทเคลม · งานของผู้รับเหมาที่ไม่คิดเรทให้อัตโนมัติ
- **บันทึกที่มาของยอดติดไปกับงาน** — ใช้เรทอะไร ของอำเภอไหน ทีมไหน ติ๊กอะไรไว้
  เวลาถูกถามว่า "ทำไมงานนี้ได้เท่านี้" ตอบได้ทันทีโดยไม่ต้องเดา

**ตรวจด้วยการเล่นซ้ำงานจริง 32 งานจากระบบเดิม — ยอดตรงกันทุกบาท (30/30 ที่เทียบได้
อีก 2 งานระบบเดิมไม่ได้เติมยอดให้)** เทสตัวนี้ผูกเข้า `npm test` แล้ว แก้สูตรเมื่อไหร่
จะรู้ทันทีว่าเพี้ยนจากของเดิมหรือเปล่า

### เพิ่ม — ย้ายตารางเรทค่าตอบแทนผู้สำรวจเข้าระบบเรา (ขั้นแรกของงานคิดเงิน)
เดิมเรทอยู่ในระบบ se-billing ที่ทำงานคู่กับ ISURVEY เท่านั้น — **งานที่ทำผ่านระบบใหม่
จึงไม่มีใครคิดค่าตอบแทนให้ และไม่มีที่บันทึกยอดจ่ายพนักงานเลย** เป็นช่องโหว่เรื่องเงิน
ที่จะกว้างขึ้นเรื่อย ๆ ตามจำนวนงานที่ย้ายมาระบบใหม่
- ยกเรทเข้าฐานข้อมูลของเราครบ: **337 อำเภอ · 25 จังหวัด · 2 ตำบลพิเศษ · 13 รหัสทีม**
- ยกเรทที่เดิม**ฝังอยู่ในโค้ดส่วนขยายเบราว์เซอร์**ออกมาเป็นข้อมูลด้วย (เรทตามคำนำหน้า
  เลขเซอร์เวย์ · งานต่อเนื่อง · ค่าคัดประจำวัน · ค่านอกพื้นที่/นอกเวลา) — ของเดิมแก้ทีต้อง
  สร้างส่วนขยายใหม่แล้วไล่ติดตั้งทุกเครื่อง
- เตรียมที่เก็บ**ยอดที่คิดจริงต่องาน** พร้อมบันทึกว่าใช้เรทอะไร ติ๊กอะไรไว้ ใครคิด เมื่อไหร่
  — ตารางเรทไม่เก็บประวัติ (ตามที่ตกลง) บันทึกตรงนี้จึงเป็นหลักฐานเดียวเวลาย้อนตรวจยอด

**จงใจแยกเป็นคนละชุดกับ se-billing ไม่ต่อสายถึงกัน** — คนที่ใช้ ISURVEY กับส่วนขยาย
ทำงานต่อไปโดยไม่ถูกแตะแม้แต่บรรทัดเดียว วันเลิกใช้ ISURVEY ก็ปิด se-billing ทิ้งได้เลย
ไม่ต้องย้ายอะไร · แลกกับความเสี่ยงว่ามีคนแก้เรทข้างเดียว จึงมี**เครื่องมือเทียบเรท 2 ชุด**
(อ่านอย่างเดียวทั้งคู่) ไว้รันก่อนปิดงวดจ่ายเงิน

### แก้บั๊ก — รัน migration บน production ต้องพิมพ์ SQL ทั้งไฟล์ด้วยมือ
ไฟล์ `.sql` ไม่ได้ถูกใส่เข้าไปในตัวเซิร์ฟเวอร์ตอน build (คอมไพล์แล้วเหลือแต่ JS) เวลาจะรัน
migration จึงต้องพิมพ์คำสั่งทั้งไฟล์ลงหน้าเทอร์มินัล — ไฟล์เรทมี 380 คำสั่ง ทำแบบนั้นไม่ไหว
ตอนนี้ไฟล์ติดไปกับเซิร์ฟเวอร์แล้ว รัน migration ได้ด้วยคำสั่งเดียว

### เพิ่ม — ออกใบเอกสารจากเคสจริงได้แล้ว เก็บเข้าสำนวนให้เอง (APK 1.0.51+53)
ปุ่ม **"ออกใบเอกสารหน้างาน"** ท้ายหน้างานสำรวจ — กดแล้วได้ใบที่กรอกข้อมูลของเคสนั้นให้เสร็จ
ไม่ต้องพิมพ์ซ้ำ · เลือกแบบใบ (และเลือกคันถ้าเป็นใบของคู่กรณี) → ยื่นให้เขาเซ็นบนจอ →
**พิมพ์ออกกระดาษ** ให้ถือกลับ + **กด "บันทึกเข้าสำนวน" แล้วใบเข้าไปอยู่ในรูปของเคส
หมวด "ใบแจ้งความเสียหาย" ให้เอง** — ไฟล์ที่คู่กรณีถือกับที่ส่งเข้าระบบเป็นใบเดียวกัน
- ออกใบได้**ตั้งแต่ยังไม่ส่งงาน** เพราะต้องยื่นให้เซ็นตั้งแต่อยู่หน้างาน
- ปุ่มบอกด้วยว่าออกไปแล้วกี่ใบ · เปลี่ยนแบบใบ = ล้างลายเซ็นให้ (คนละเอกสารกัน)
- **จำเครื่องพิมพ์ที่เคยใช้** ครั้งต่อไปกดพิมพ์ได้เลยไม่ต้องเลือกเครื่องซ้ำ
- **ไม่รู้จักบริษัทประกันของเคส = ไม่ยอมออกใบ** พร้อมบอกให้ไปแก้ชื่อบริษัทก่อน —
  ท้ายใบ (เบอร์ติดต่อ/กำหนดวัน) ต่างกันรายบริษัท เดาแทนแล้วลูกค้าโทรผิดที่
- เตือนถ้ายังไม่มีลายเซ็น แต่ไม่ห้าม (บางกรณีพิมพ์เปล่าไปให้เซ็นด้วยปากกา)

**ทดสอบกับเคสจริงบนเครื่องจริงครบวงจร** (สร้างเคส → จ่ายงาน → รับงาน → ถ่ายรูปยืนยัน →
ออกใบ → เซ็น → บันทึก → ส่งงาน) ผลบนเซิร์ฟเวอร์: รูป 2 ใบ หมวดถูกทั้งคู่
(`รูปยืนยันถึงที่เกิดเหตุ` + `ใบแจ้งความเสียหาย`) · ลบเคสทดสอบเรียบร้อยแล้ว

### เพิ่ม — ใบเอกสารหน้างานครบ 10 แบบ + ช่อง "ซ่อมที่" (APK 1.0.50+52)
ต่อจากงานพิมพ์ใบแจ้งความเสียหาย — ขยายจากใบเดียวเป็นระบบที่รองรับเอกสารหน้างานทุกแบบ
- **10 แบบ = 5 หมวด × 2** (รถประกัน · รถคู่กรณี · ผู้บาดเจ็บ/เสียชีวิต · ทรัพย์สิน · รับเงิน
  โดยแต่ละหมวดมี "ใบหลักฐาน/แจ้งความเสียหาย" กับ "ใบติดต่อ") — เลือกแบบใบก่อนพิมพ์
  **4 แบบแรกใช้งานได้จริง อีก 6 แบบขึ้นในรายการแต่กดไม่ได้** เพราะยังไม่เคยเห็นเนื้อในใบจริง
  (เดาเนื้อหาเอกสารที่คนนอกต้องเซ็นรับรองไม่ได้ — ขึ้นว่า "ยังไม่มีข้อมูล" ให้เห็นชัด)
- **ท้ายใบเปลี่ยนตามบริษัทประกันเอง** — ไม่ใช่แค่มี/ไม่มี QR แต่**กำหนดวันติดต่อคนละเลข**
  (ไอโออิ 7 วัน + QR · ไทยไพบูลย์ 15 วัน + พิมพ์ลิงก์เป็นข้อความ) เบอร์ก็คนละชุด
  จับบริษัทจากชื่อที่พนักงานพิมพ์ — **ไม่รู้จักบริษัทจะไม่ยอมพิมพ์** ดีกว่าให้เบอร์ผิดกับลูกค้า
- **ข้อมูลบนใบดึงจากเคสจริงทั้งหมด** ไม่ต้องพิมพ์ซ้ำ · ใบของคู่กรณีดึงจากคันที่เลือก
- **"ความเสียหายส่วนแรก" ขึ้นเฉพาะเคลมที่มีจริง** ไม่มีข้อมูล = ซ่อนทั้งบล็อก
- ระดับความเสียหายบนใบเขียน **`ต่ำ (L)` / `กลาง (M)` / `สูง (H)` / `สูงมาก (X)`** —
  ไทยเพราะคนที่เซ็นรับรองต้องอ่านออกว่าตัวเองเซ็นอะไร รหัสเพราะผู้ตรวจต้องเทียบกับหน้าระบบประกัน
- หัวกระดาษเป็น**โลโก้อย่างเดียว** ไม่มีบล็อกที่อยู่บริษัท
- QR ชี้หน้า **รายชื่อสาขา/ศูนย์-อู่ในสัญญาบนเว็บของเราเอง** (`/branches/<รหัสบริษัท>`)
  เปิดได้โดยไม่ต้องล็อกอิน (คนสแกนคือคู่กรณีที่ไม่มีบัญชี) ออกแบบให้อ่านง่ายบนมือถือ

### เพิ่ม — ช่อง "ซ่อมที่" (ชื่ออู่/ศูนย์ที่นำรถเข้าซ่อม)
เดิมผู้สำรวจจดไว้ในช่องหมายเหตุ ค้นย้อนหลังไม่ได้ ผู้ตรวจต้องไล่อ่านเอง
- เพิ่มช่องทั้ง**แอปมือถือ**และ**หน้าตรวจงานบนเว็บ** อยู่ในหมวดกรมธรรม์ ถัดจากค่าเสียหายส่วนแรก
- จำกัด 200 ตัวอักษรทั้งสองฝั่งให้ตรงกับฐานข้อมูล — **ยาวเกินแล้วฐานข้อมูลไม่ตัดปลายให้
  แต่ปฏิเสธทั้งใบ** ทำให้ส่งงาน/บันทึกไม่ผ่านทั้งที่ผิดแค่ช่องเดียว
- ⚠️ **ต้องรัน migration 034 บน production ก่อนแจก APK ใหม่** ไม่งั้นส่งงานสำรวจไม่ผ่านเลย
  (ยังรันไม่ได้ — เครื่องพัฒนาต่อฐานข้อมูลไม่ติดตอนนี้)

### แก้บั๊ก — หน้าสาธารณะถูกเด้งไปหน้าล็อกอิน
พนักงานที่เคยล็อกอินแล้วโทเคนหมดอายุ สแกน QR บนใบจะถูกเด้งไปหน้าล็อกอินทั้งที่หน้านั้น
ไม่ต้องล็อกอิน (คนนอกที่ไม่เคยล็อกอินไม่โดน) — ยกเว้นหน้ารายชื่อสาขาออกจากการเด้งแล้ว

### เพิ่ม — พิมพ์ "ใบแจ้งความเสียหาย" ออกเครื่องพิมพ์พกพาได้จากในแอป (APK 1.0.49+51)
เดิมพนักงานกรอกใบด้วยมือลงกระดาษแล้วถ่ายรูปเก็บเข้าสำนวน ตอนนี้แอปสร้างใบให้เอง
คู่กรณีเซ็นบนจอ แล้วพิมพ์ออกกระดาษให้ถือกลับบ้านได้ทันที
- **ใบที่พิมพ์ = ไฟล์เดียวกับที่เก็บเข้าสำนวน** — สร้างรูปครั้งเดียวส่ง 2 ทาง
  (เข้าเครื่องพิมพ์ + เข้าหมวดรูป "ใบแจ้งความเสียหาย" ที่มีอยู่แล้ว) ที่คู่กรณีถืออยู่กับ
  ที่ส่งเข้าระบบประกันจึงตรงกันเสมอ ไม่มีทางเป็นคนละใบ
- **เซ็นชื่อบนจอ** — เขียนเอง ไม่ต้องพึ่งไลบรารีนอก
- **QR ชี้หน้ารายชื่อสาขา/อู่ของเราเอง** (`survey.sesurvey.cloud`) แทนของเดิมที่ชี้ไปโดเมน
  ISURVEY ซึ่งกำลังจะเลิกใช้ — ใบที่แจกไปแล้วอยู่ในมือคนนอก เรียกกลับไม่ได้ ถ้าปิด ISURVEY
  วันไหน QR บนใบเก่าทุกใบจะตายพร้อมกัน
- ใบ**ประกอบจากบล็อก** ไม่ได้เขียนตายตัวต่อแบบ — ใบมีหลายแบบ (รถประกัน · รถคู่กรณี · …)
  ต่างกันแค่มีบรรทัดไหนบ้าง เพิ่มแบบใหม่ไม่ต้องแก้โครง
- ระดับความเสียหายบนใบใช้คำเต็ม **ต่ำ/กลาง/สูง/สูงมาก** ตามที่ระบบประกันกำหนด
  (ใบนี้ให้คนนอกอ่านและเซ็นรับรอง จึงไม่ใช้ตัวย่อ)

**ยังไม่เสร็จ:** ยังเป็นหน้าทดสอบที่เข้าจากเมนูหน้าหลัก ยังไม่ได้ผูกกับข้อมูลเคสจริง
และยังไม่มีช่อง "ซ่อมที่" (ต้องเพิ่มทั้งแอปมือถือและเว็บ)

**บทเรียนจากเครื่องจริง** (TSC Alpha-3R / RE310B กระดาษ 58 มม.)
- **สเปกบอกว่ารองรับ ESC/POS แต่ของจริงไม่รับ** — ส่งเข้าไปเครื่องรับข้อมูลครบแต่เงียบ
  ไม่พิมพ์อะไรเลย ต้องใช้ TSPL ซึ่งเป็นภาษาแม่ของ TSC · แอปจึงมีปุ่ม "ทดสอบ 3 โหมด"
  ที่ยิงแถบสั้น ๆ ซึ่ง**บอกชื่อโหมดของตัวเอง** เจอเครื่องรุ่นใหม่กดครั้งเดียวรู้เลยว่าพูดภาษาไหน
- **เดินกระดาษเปล่าทิ้ง 15-20 ซม. ทุกใบ** เพราะสั่งขนาดกระดาษโดยไม่ระบุหน่วย เครื่องเลยเข้าใจ
  ว่าเป็นสติกเกอร์แล้วออกไปหาช่องว่างระหว่างดวง — ระบุหน่วยแล้วหายสนิท
- **ต่อไม่ติด ≠ ต่อไม่เป็น** — เครื่องพิมพ์ปิดอยู่ก็ขึ้นอาการเดียวกัน ตอนนี้ข้อความบอกชัด
  ว่าให้ไปเช็คว่าเครื่องเปิดหรือยัง และบอกด้วยว่าต่อติดด้วยวิธีไหน
- ต้องเดินกระดาษท้ายใบ ~25 มม. ไม่งั้น QR อยู่ตรงฟันฉีกพอดี ฉีกแล้ว QR ขาด

### แก้บั๊ก — เซ็นชื่อแนวตั้งกลายเป็นการเลื่อนหน้าจอ
กรอบเซ็นชื่ออยู่ในหน้าที่เลื่อนได้ → ลากขึ้นลงหน้าจอเลื่อนแทนที่จะเป็นเส้น
**ลายเซ็นจริงมีเส้นแนวตั้งเกือบทุกลายมือ ผลคือเซ็นแล้วได้แต่เส้นแนวนอนขาด ๆ**
เจอตอนทดสอบบนเครื่องจริงก่อนถึงมือพนักงาน

### เพิ่ม
- **CHANGELOG.md** — บันทึกการเปลี่ยนแปลงทั้งโปรเจกต์ย้อนหลังตั้งแต่ มี.ค. 2569
- **CLAUDE.md** — กติกาการทำงาน: แก้อะไรต้องอัปเดต CHANGELOG ทุกครั้ง + กับดักที่เจอบ่อย
- **เครื่องมือสกัดสเปกฟอร์ม EMCS** (`se-autokey/tools/emcs_spec.py`) — อ่านไฟล์ HTML
  ที่เซฟไว้ (ไม่แตะ EMCS) แล้วดึง **ตัวเลือก dropdown ทุกตัว · ช่องบังคับจริงแยก
  "ทุกบริษัท" กับ "เฉพาะบริษัท" · รายการ element** ออกมาเป็น JSON
  โหมด `--diff` เทียบกับตารางแปลงรหัสในโปรเจกต์แล้วบอกว่าป้ายไหนหายไป
  (ตรวจรอบแรก: สาเหตุ 79 · ลักษณะความเสียหาย 21 · ความสัมพันธ์ 39 · ใบขับขี่ 21 · สี 55 — ครบทุกตัว)
  แทนการนั่งสกัดด้วยมือที่ทำมาแล้วหลายรอบ

### เพิ่ม — เครื่องมือดูดข้อมูลเคสจริงจาก EMCS เป็น XML
`se-autokey/tools/emcs_dump.py` — อ่านหน้า EMCS ที่กรอกครบแล้ว (จากไฟล์ที่เซฟไว้
ไม่แตะระบบจริง) เก็บ**เฉพาะค่าที่กรอกหรือถูกเลือกอยู่** แล้วประกอบเป็น XML
รูปแบบเดียวกับที่ระบบเราสร้าง ใช้เอาเคสจริงมาเป็นข้อมูลทดสอบ
- `--diff` เทียบ tag ต่อ tag ว่า "ที่เราส่งเข้าไป" ต่างจาก "ที่อยู่ใน EMCS ตอนนี้" ตรงไหน
  แยกออกได้ว่าเป็นเพราะเราไม่ได้ส่ง · EMCS เติมเอง · หรือหัวหน้าแก้ทีหลัง
- ทดสอบกับเคสจริงที่มีทั้งหน้าเว็บและ XML ของตัวเอง: ตรง 77 tag · ต่าง 15 อธิบายได้ครบ
- อ่านครบทั้ง 5 ส่วน: ข้อมูลทั่วไป · รถประกัน+คู่กรณี · ทรัพย์สิน · ผู้บาดเจ็บ · ใบค่าใช้จ่าย

### เพิ่ม — ให้บอทไปเปิด EMCS อ่านหน้าเคสมาเอง (ไม่ต้องเซฟหน้าด้วยมือ)
`se-autokey/tools/emcs_fetch.py` — login → ค้นเลขเคลม → เปิดเรื่อง → เซฟ 4 หน้า
→ ส่งต่อให้ตัวแปลง XML ทำงานต่อ
- **อ่านอย่างเดียว** ไม่กรอกช่อง ไม่กดบันทึก/แก้ไข/ส่งงาน — กดได้เฉพาะปุ่มสลับหน้า
  กับปุ่มออกจากเรื่อง ปุ่มอื่นโค้ดปฏิเสธทิ้ง
- **ไม่ใส่ `--fetch` = ไม่เปิดเรื่อง** แค่บอกว่าเคลมนี้มีเรื่องอะไรบ้าง
- เปิดเรื่องใน EMCS เท่ากับล็อกเรื่องนั้นไว้ คนอื่นเปิดต่อไม่ได้ → กด **"กลับหน้า
  Inbox/Outbox"** เพื่อออกจากเรื่องทุกกรณีแม้ระหว่างทางพัง และปิดเบราว์เซอร์เอง
  ไม่ปล่อยค้างถือล็อก · รวมถึงกรณีเปิดเรื่องติดแล้วหน้าโหลดไม่ทัน (เรื่องล็อกไปแล้ว
  ทั้งที่ยังอ่านอะไรไม่ได้) · ส่วนกรณีเปิดไม่ติดเพราะคนอื่นล็อกอยู่ จะไม่กดและไม่เตือนผิด
- **ทดสอบกับ EMCS จริงแล้ว** (เคลม 21BR10AVD-6906-000098) ดึงครบ 4 หน้าใน 12 วินาที
  ออกจากเรื่องปลดล็อกทุกรอบ · เทียบกับหน้าที่เซฟด้วยมือไว้เมื่อ 15 ก.ค. ได้ **87 จาก 93
  ช่องตรงกัน** ส่วนที่ต่างคือข้อมูลที่ถูกแก้ใน EMCS จริงระหว่างนั้น (สถานที่ · รายละเอียด ·
  เวลาเกิดเหตุ · ผู้แจ้ง · ชื่อผู้สำรวจ · ที่อยู่) ไม่ใช่อ่านพลาด · อ่านซ้ำอีกรอบห่างกัน
  1 ชั่วโมงครึ่งได้ค่าตรงกัน 117/117 ช่อง และใช้กับอีกเคลมที่โครงสร้างต่างกันได้เช่นกัน
- ตัวเลือก **ค้างหน้า EMCS ไว้บนจอ** สำหรับตอนอยากดูเทียบด้วยตาเอง — ปกติปิดให้
  อัตโนมัติ โหมดนี้เรื่องจะยังถูกล็อกจนกว่าจะกด "กลับหน้า Inbox/Outbox" เอง จึงเตือนไว้ชัด

### เพิ่ม — เตือนชื่อที่ EMCS จะลบทิ้ง (หน้าตรวจงาน)
EMCS มีตัวกรองอักขระบนช่อง **ชื่อคน** และมันไม่ได้แค่ตัดตัวอักษรทิ้ง — มัน
**ล้างข้อความทั้งช่อง** ทันทีที่มีคนคลิกเข้า-ออกช่องนั้น การนำเข้าด้วย XML ไม่โดนกรอง
ค่าจึงเข้าไปได้ แต่พอหัวหน้าเปิดมาตรวจแล้วคลิกโดน **ชื่อหายเกลี้ยงโดยไม่มีใครรู้**
- หน้าตรวจงานขึ้นแบนเนอร์เหลืองก่อนส่งเข้า EMCS บอกว่าชื่อไหนมีปัญหาและตัวไหนคืออักขระต้นเหตุ
- ครอบคลุม ผู้แจ้ง · ผู้สำรวจภัย · พนักงานสอบสวน · ผู้ขับขี่รถประกัน ·
  เจ้าของ/ผู้ขับขี่รถคู่กรณีทุกคัน · ผู้บาดเจ็บทุกคน · เจ้าของทรัพย์สินทุกรายการ
  (ผู้เอาประกันไม่มีตัวกรองนี้ ตรวจจากหน้า EMCS จริงแล้ว)
- ใช้ได้เฉพาะ ตัวอักษร · ตัวเลข · เว้นวรรค · จุด · ขีดกลาง — วงเล็บ จุลภาค สแลช
  เครื่องหมาย # และ**เลขไทย** ทำให้ชื่อหายทั้งช่อง
- ฝั่งบอทที่ดึง XML ไปเองไม่เห็นแบนเนอร์ จึงเขียนเตือนลงล็อกเซิร์ฟเวอร์ด้วย
- ข้อมูลปัจจุบันในระบบยังไม่มีเคสไหนติดเงื่อนไขนี้

### ตรวจแล้วไม่ต้องแก้ — ตัวเลือกผลตรวจแอลกอฮอล์ตรง EMCS อยู่แล้ว
แอปมือถือใช้ตัวเลือก 2 ค่าที่ข้อความตรงกับ EMCS ("ไม่มีการตรวจแอลกอฮอล์" /
"มีการตรวจแอลกอฮอล์") พร้อมช่อง "ระบุผล" ที่โผล่เมื่อเลือก "มีการตรวจ" — ตรงกันทั้ง
5 จุด (มือถือ · เว็บ · นำเข้า · ส่งออก · บอท) ทดสอบการแปลงค่าครบทุกทางแล้ว
- แก้คอมเมนต์เก่าในโค้ด 2 จุดที่ยังเขียนว่า "แอปมีกล่องข้อความเดียว" ซึ่งล้าสมัยไปนานแล้ว
  และเกือบทำให้ไปแก้แอปทั้งที่ไม่มีอะไรเสีย

### แอปมือถือ 1.0.28+30 — แก้บั๊กชิ้นส่วนที่ไม่มีด้าน (ที่คิดว่าแก้ไปแล้ว)
ชิ้นส่วนที่ EMCS ไม่มีปุ่มเลือกด้าน (กันชนหน้า/หลัง · หลังคา · กระบะ · ฝากระโปรง ฯลฯ
12 ชิ้น) **ยังเลือก ซ้าย/ขวา ได้อยู่** ทั้งที่แก้ไปแล้วตั้งแต่ 1.0.25+27
- ต้นเหตุ: หน้าจอเลือกด้านถูกก๊อปไว้ **4 ที่** รอบก่อนแก้ไปแค่ที่เดียว
- เจอตอนกรอกงานจริงบนเครื่อง แก้ 3 ที่แรกแล้ว build ใหม่ ยังโผล่อีก จึงเจอที่ 4
- ย้ายรายชื่อ 12 ชิ้นเป็นค่ากลางที่ทุกที่ใช้ร่วมกัน — เดิมซ่อนอยู่ในหน้าเดียว
  ที่อื่นจึงมองไม่เห็นแล้วต่างคนต่างเขียน
- ผลกระทบเดิม: ช่างเลือกด้านให้กันชนหลังได้ แต่ EMCS ไม่มีที่ลง ด้านที่เลือกหายเงียบ

### กติกาที่ยืนยันแล้ว — งานครั้งที่ 2 เป็นต้นไป ต้องใส่รูปด้วย
เดิมค้างไว้ไม่สรุป บอทจึงกรอกให้แค่เลขที่ใบแจ้งหนี้กับวันที่ ไม่เคยอัปรูปเลย
- ตอนนี้บอทอัปรูปของครั้งนั้นให้ก่อนกรอกใบแจ้งหนี้
- รูปใน EMCS ผูกกับ **เลขเคลม** ไม่ใช่ครั้งที่ → โควตา 80 ใบใช้ร่วมกันทั้งเคลม
  ครั้งที่ 2 จึงเติมต่อจากที่ครั้งแรกใช้ไป และบอทตัดให้พอดีโควตาที่เหลือเอง

### ถอดออก — แผง "ช่องที่ EMCS บังคับแต่ยังว่าง" บนหน้าตรวจงาน
ทำเสร็จแล้วแต่ถอดออกก่อน (user ตัดสินใจ) — **ให้ลองใช้งานจริงก่อน** ถ้าเจอปัญหาว่า
ช่างปล่อยช่องว่างบ่อยจนหัวหน้าเสียเวลาไล่แก้บน EMCS ค่อยเอากลับมา
- เหตุผล: ยังไม่มีหลักฐานจากหน้างานว่าเป็นปัญหาจริง — เพิ่มของบนหน้าจอก่อนรู้ว่าจำเป็น
  มีแต่ทำให้รก
- ของที่ทำไว้แล้วอยู่ใน git ย้อนกลับมาได้ทันที ไม่ต้องเริ่มใหม่
- **คำเตือนตอนอัปโหลด XML ยังอยู่เหมือนเดิม** (คนละอันกัน)

### เพิ่ม — คำเตือนตอนนำเข้า XML มาจากฟอร์ม EMCS จริง (ไม่ต้องเขียนมือ)
เดิมข้อความเตือน "ไฟล์นี้ไม่มีช่องนั้นช่องนี้" เขียนไว้ตายตัวทีละบรรทัด ถ้า EMCS เพิ่ม
ช่องบังคับใหม่ **ระบบจะเงียบ** จนกว่าจะมีคนไปเจอว่าบันทึกไม่ผ่านแล้วค่อยมาเพิ่มเอง
- ตอนนี้รายการช่องบังคับสกัดจากหน้า EMCS จริงด้วยเครื่องมือ แล้วให้ระบบเตือนเอง
  EMCS เปลี่ยนเมื่อไหร่ → รันเครื่องมือใหม่ครั้งเดียวจบ
- ผลทันที: **เจอช่องที่คนเขียนมือลืมไป** — "สาขาบริษัทประกัน" ไม่เคยเตือนมาก่อน
- ไฟล์ที่มีข้อมูลครบจะไม่ถูกเตือนเกินจำเป็น (แยกแยะตามค่าจริงในไฟล์)
- ครอบคลุมเฉพาะช่องที่บังคับทุกบริษัท — ช่องที่บังคับเฉพาะบางบริษัทยังไม่รวม
  เพราะตอนอ่านไฟล์ยังไม่รู้ว่าเป็นบริษัทไหน
- **ไม่เตือนช่องที่บอทเติมให้เองอยู่แล้ว** — "สาขาบริษัทประกัน" บอทเลือกให้ตอนนำเข้า
  คนตรวจไม่ต้องทำอะไร เตือนไปก็หาที่กรอกไม่เจอ (คำเตือนที่ไม่ต้องทำอะไร
  สอนให้คนเมินคำเตือน) → ไฟล์ ISURVEY จริงเตือน 2 ข้อ เหลือเฉพาะที่ต้องกรอกจริง

### กติกาที่ยืนยันแล้ว — XML 3 ที่มา ห้ามปนกัน
| ที่มา | มีข้อมูลแค่ไหน | ระบบทำอะไร |
|---|---|---|
| **ISURVEY** (ระบบเก่า) | ไม่มีหนัก/เบา · ลักษณะความเสียหาย ฯลฯ | เตือนให้หัวหน้ากรอกเอง · ยอดเงินส่งต่อเข้า EMCS |
| **se-survey** (แอปมือถือ) | ครบตามที่พนักงานกรอก | ไม่ผ่านเส้นทางนำเข้า XML เลย — ระบบสร้างไฟล์ให้เอง |
| **สกัดจากหน้าเว็บ EMCS** | ครบ เพราะผู้ใช้กรอกและตรวจแล้ว | **ข้อมูลทดสอบเท่านั้น** |

- ไฟล์ที่สกัดจาก EMCS ถูกประทับตราไว้ในไฟล์ตั้งแต่ตอนสร้าง ระบบอ่านออกและ
  **บันทึกเป็นคนละประเภทกับงาน ISURVEY** — เดิมบังคับเป็น "งานจากระบบเก่า" ตายตัว
  ซึ่งจะทำให้เคสทดสอบถูกนับเป็นงานจริงและ**ดันยอดเงินเข้าระบบประกันโดยไม่ตั้งใจ**
- ข้อความเตือนเปลี่ยนตามที่มาของไฟล์ — เดิมไฟล์จาก EMCS ก็ขึ้นว่า "ไฟล์ ISURVEY ไม่มี..."
- ตั้งใจ**ไม่**อ่าน HEV_CAR / LOSS_ID กลับในเส้นทางนำเข้า เพราะเป็นเส้นทางของระบบเก่า
  ซึ่งไฟล์จริงไม่เคยมี tag พวกนี้อยู่แล้ว

### แก้บั๊ก — จ่ายงานแล้วช่างไม่ได้รับแจ้งเตือน โดยไม่มีใครรู้ (APK 1.0.40+42)
ระบบแจ้งเตือนงานใหม่ล้มได้ 3 แบบ และ **ทั้ง 3 แบบเงียบสนิท** — หน้าจ่ายงานขึ้นว่าสำเร็จ
เหมือนกันหมด ทั้งที่ช่างอาจไม่ได้รับอะไรเลย
- **หน้าจ่ายงานบอกผลจริงแล้ว** — ถ้าแจ้งเตือนไปไม่ถึง ขึ้นแถบเหลือง *"มอบหมายงานแล้ว
  แต่แจ้งเตือนไปไม่ถึงเครื่องช่าง — โทรแจ้งช่างด้วย"* และค้างหน้าไว้ ไม่เด้งออกทันที
- แยกสาเหตุให้ด้วย: เครื่องช่างยังไม่เคยลงทะเบียน · ถอนการลงทะเบียนไปแล้ว ·
  ระบบแจ้งเตือนไม่ได้ตั้งค่าบนเซิร์ฟเวอร์ · ส่งไม่สำเร็จ
- **ตรวจพบว่าผู้สำรวจ 60 จาก 144 คนไม่มีการลงทะเบียนรับแจ้งเตือน** — จ่ายงานไปก็ไม่มี
  ทางรู้ตัว ต้องไล่ให้เปิดแอปใหม่ทีละเครื่อง
- ถ้าเครื่องถอนการลงทะเบียนไปแล้ว ระบบล้างข้อมูลค้างให้เอง ไม่หลอกว่ายังส่งได้
- **"เรียกพิกัดพนักงาน" เลิกโกหก** — เดิม log ว่า "ส่งให้ N คน" โดยนับจากจำนวนคนในระบบ
  ไม่ใช่จำนวนที่ส่งถึงจริง ตอนนี้แยกนับ สำเร็จ/ไม่มีการลงทะเบียน/ล้มเหลว
- ถ้าเซิร์ฟเวอร์ไม่ได้ตั้งค่าระบบแจ้งเตือน จะเตือนดัง ๆ ตอนเปิดเครื่อง + ดูได้ที่ `/health`
  (เดิมเป็นข้อความบรรทัดเดียวที่ไม่มีใครเห็น) — **ยังเปิดระบบได้ตามปกติ ไม่ล่ม**

### เพิ่ม — รู้ว่าเครื่องพนักงานแต่ละคนใช้แอปเวอร์ชันอะไร (APK 1.0.40+42)
APK แจกด้วยมือ ไม่มีใครรู้ว่าเครื่องไหนอยู่เวอร์ชันไหน แล้วของที่เปลี่ยนฝั่งเซิร์ฟเวอร์
ทำให้แอปเก่าพังเงียบ — ตรวจ log จริงวันนี้พบ **รูปลงเวลาโหลดไม่ขึ้นเลยสักใบ 191 ครั้ง
สำเร็จ 0 ครั้ง** เพราะเครื่องพนักงานยังเป็น APK เก่าที่ไม่แนบสิทธิ์
- แอปส่งเวอร์ชันมากับทุกคำขอ · หน้าจัดการผู้ใช้เพิ่มคอลัมน์ **"เวอร์ชันแอป"**
  (ยังไม่เคยส่ง = ขึ้น "ไม่ทราบ" สีเหลือง = ยังไม่ได้อัป)
- **ไม่บล็อกอะไรทั้งสิ้น** — แอปเก่ายังใช้งานได้ครบ แค่ขาดฟีเจอร์ใหม่ (ตามที่ตกลงกัน)
- บันทึกเฉพาะตอนเวอร์ชันเปลี่ยน ไม่ถ่วงการใช้งาน

### แก้บั๊ก — "นำเข้า EMCS แล้ว" ไม่ได้แปลว่าบริษัทประกันได้รับงาน
ป้ายเขียว "✓ นำเข้า EMCS แล้ว" ขึ้นตั้งแต่บอทสร้างเรื่องเสร็จ แต่**บอทไม่มีสิทธิ์กดปุ่ม
"ส่งงานใหม่"** (คนต้องกดเอง) → งานที่ค้างรอคนกดส่งดูเหมือนเสร็จแล้ว และไม่มีใครนับได้ว่า
ค้างอยู่กี่เรื่อง — ตรวจแล้วตอนนี้ค้างอยู่ **7 เรื่อง** ทั้งหมดที่มีในระบบ
- แยกเป็น 2 ป้าย: **⏳ draft ค้าง — รอกด "ส่งงานใหม่"** (เหลือง) กับ **✓ ส่งงานแล้ว** (เขียว)
- หน้ารายการงานตรวจสอบขึ้นแถบเตือนจำนวนเรื่องที่ค้างรอกดส่ง
- เก็บ**ข้อความสถานะดิบ**จากหน้า EMCS ไว้ด้วย ไม่ใช่แค่ "ส่งแล้ว/ยังไม่ส่ง"
- **บอทเลิกเดา** 2 จุด: (1) เดิมแยกเรื่องด้วยเลขเคลมอย่างเดียวแล้วเอาแถวสุดท้าย —
  1 เลขเคลมมีได้หลายเรื่อง จึงอ่านสถานะผิดเรื่องได้ ตอนนี้แยกด้วย**เลขเซอร์เวย์**
  แยกไม่ออกเมื่อไหร่ตอบ "ไม่ทราบ" (2) เดิมถือว่า "ไม่ใช่ draft = ส่งแล้ว" ซึ่งกว้างเกิน
  ตอนนี้ยืนยันเฉพาะสถานะที่รู้จักจริง สถานะแปลก ๆ รายงานตามจริงให้คนตัดสิน

### แก้บั๊ก — ใครล็อกอินได้ ก็เปิดรูปบัตรประชาชนของทุกเคสได้
ด่านตรวจรูปใน `/uploads` ตรวจแค่ว่า "โทเคนของจริงไหม" ไม่ได้ดูว่าคนขอ**มีสิทธิ์ดูรูปใบนั้น
หรือเปล่า** → ช่างคนไหนก็ได้เปิดรูปบัตรประชาชน/ใบขับขี่ของเคสที่ไม่ใช่ของตัวเองได้ ถ้ารู้ชื่อไฟล์
- ตอนนี้ตรวจรายไฟล์: **admin/callcenter** เห็นทุกอย่าง · **ผู้ตรวจ** เห็นเฉพาะไฟล์ของเคส
  (ไม่รวมรูปลงเวลา ให้ตรงกับรายงานลงเวลาที่จำกัดสิทธิ์อยู่แล้ว) · **ผู้สำรวจ** เห็นเฉพาะเคส
  ที่ได้รับมอบหมาย + รูปลงเวลาของตัวเอง
- ย้ายงาน/ปฏิเสธงาน = สิทธิ์ดูรูปเปลี่ยนทันที ไม่ค้าง
- ตัดสินสิทธิ์จาก**โครงสร้างโฟลเดอร์ที่ระบบสร้างเอง** ไม่ใช่ชื่อไฟล์ที่แอปส่งมา (ปลอมได้)
  และอ่านสิทธิ์จากฐานข้อมูลสด ๆ ไม่ใช่จากโทเคน — ปิดบัญชีแล้วต้องใช้ไม่ได้ทันที
- ทดสอบยิงจริง 15 กรณี (ทุกบทบาท × ทุกชนิดไฟล์ + path หลอก) ผ่านครบ

### แก้บั๊ก — เลขเซอร์เวย์ซ้ำได้ และงานที่ส่งไปแล้วถูกเขียนทับเงียบ ๆ (APK 1.0.39+41)
เจอจากการตรวจระบบทั้งหมด: กติกา "เลขเซอร์เวย์ห้ามซ้ำ" (เลขอ้างอิงเบิกเงิน) ถูกบังคับ
แค่ตอน**สร้างเคส/ส่งงาน/นำเข้าไฟล์เก่า** เท่านั้น — ช่องทาง **แก้ไข** ทั้ง 2 ทางไม่เคยตรวจเลย
และฐานข้อมูลไม่มีอะไรกันไว้เป็นชั้นสุดท้าย
- **ฐานข้อมูลกันเลขซ้ำเองแล้ว** 3 ชั้น: ซ้ำในช่องเดียวกันข้ามเคส · ซ้ำข้ามช่อง
  (เลขงานครั้งแรกของเคสหนึ่ง = เลขงานครั้งที่ 2 ของอีกเคส) · ซ้ำกันเองในเคสเดียว
- ช่องทางแก้ไขของผู้สำรวจและของผู้ตรวจ ตรวจเลขซ้ำแล้วทั้งคู่
- ถ้าเลขซ้ำจริง ระบบตอบเป็นคำเตือนที่อ่านรู้เรื่อง ไม่ใช่ "เกิดข้อผิดพลาด" ลอย ๆ
- **งานที่ส่งไปแล้ว/ผู้ตรวจแก้แล้ว/นำเข้าระบบประกันแล้ว แก้จากแอปไม่ได้อีก** — เดิมแอปที่
  เปิดฟอร์มค้างไว้เขียนทับของที่หัวหน้าแก้ไปแล้วได้เงียบ ๆ ไม่มีสัญญาณเลย

### แก้บั๊ก — งานที่กรอกทั้งวันหายเงียบเมื่อส่งไม่ผ่าน (APK 1.0.39+41)
คิวส่งงานออฟไลน์เหมาว่า error 400/409/422 คือ "ล้มถาวร" แล้ว **ลบฟอร์มทิ้ง** ทั้งที่เป็น
ความผิดของข้อมูลที่แก้ได้ — แบบเดียวกับ "Validation error" ที่เจอเมื่อวาน ถ้าตอนนั้นงาน
ไปเข้าคิวแทนที่จะส่งตรง ฟอร์มทั้งใบหายไปแล้วโดยไม่มีใครรู้
- ตอนนี้เก็บฟอร์มไว้ + จำเหตุผลที่ส่งไม่ผ่าน + หยุดลองเองอัตโนมัติ (เดิมวนลองทุก 15 นาที
  พร้อมอัปโหลดรูปทั้งโฟลเดอร์ใหม่ทุกรอบ)
- ผู้ใช้แก้ฟอร์มแล้วกดส่งใหม่ = กลับไปลองส่งเองอัตโนมัติ
- ที่ยังลบทิ้งเหมือนเดิมคือกรณีที่ไม่มีอะไรให้กู้จริง ๆ (เคสถูกส่งไปแล้ว/ถูกยกเลิก/หายไป)

### เพิ่ม — ช่องโทรศัพท์รับได้แค่ตัวเลข 10 หลัก (APK 1.0.38+40)
ต่อเนื่องจากเคส #124 ที่นำเข้าไม่ผ่านเพราะเลขบัตร 13 หลักถูกกรอกลงช่องโทรศัพท์ —
ช่องโทรศัพท์ของระบบประกันรับตัวเลข 10 หลัก ยาวกว่านั้น**ตีกลับทั้งไฟล์** ไม่ใช่แค่ตัดทิ้ง
- ผู้ขับขี่รถประกัน · ผู้ขับขี่คู่กรณี · ผู้บาดเจ็บ · โทรศัพท์สำรวจ — พิมพ์ได้เฉพาะตัวเลข
  และหยุดที่ 10 หลัก กรอกผิดช่องจะรู้ตัวทันทีตั้งแต่ตอนพิมพ์
- "โทรศัพท์ที่ติดต่อได้" ของเจ้าของทรัพย์สิน**ไม่จำกัด** เพราะช่องฝั่งประกันยาว 50 ตัว
  รองรับเบอร์บ้าน/เบอร์ต่อได้
- ติ๊ก "รอตรวจสอบ" ไม่เติมคำว่า "รอตรวจสอบ" ลงช่องโทรศัพท์อีกต่อไป (เป็นตัวอักษร
  ยังไงก็ถูกกรองทิ้ง) ปล่อยว่างแล้วให้ระบบใส่ "-" ให้เอง

### แก้บั๊ก — นำเข้า EMCS ไม่ผ่านทั้งไฟล์ "ข้อมูลนำเข้ามีขนาดเกิน" (2 ช่อง)
เคส #124 นำเข้าไม่ผ่านเลยสักบรรทัด ระบบประกันตีกลับทั้งไฟล์พร้อมตารางบอกว่าช่องไหนยาวเกิน
- **โทรศัพท์ผู้ขับขี่คู่กรณี** — ช่องรับได้ 10 หลัก แต่ส่งไป 13 (พนักงานกรอกเลข 13 หลัก
  ลงช่องโทรศัพท์) ช่องโทรศัพท์อื่นตัดความยาวกันหมดแล้ว ตกหล่นเฉพาะช่องนี้ช่องเดียว
- **การเรียกร้องค่าเสียหายจากคู่กรณี** — ช่องรับได้ 18 ตัว แต่ส่งป้ายไทยไปทั้งคำ
  ("รับหลักฐานจากคู่กรณี" = 20 ตัว) และถ้าติ๊กหลายข้อจะยิ่งยาว → **ส่งเป็นค่าว่างแทน**
  เหมือนไฟล์ของระบบเก่าทุกใบ · ข้อมูลไม่หาย เพราะบอทอ่านค่านี้จากอีกทางแล้วไปติ๊ก
  ให้บนหน้าจอเอง
- ⚠️ ข้อมูลต้นทางยังผิดอยู่ — "โทรศัพท์" ของคู่กรณีเคส #124 เป็นเลข 13 หลักที่ไม่ใช่
  เบอร์โทร ตัดให้เหลือ 10 ก็ยังเป็นเบอร์ผิด ต้องแก้ที่ตัวข้อมูล

### แก้บั๊ก — คำสั่งบันทึกหายกลางทาง บอทฟ้อง "กดปุ่มแล้วไม่มีอะไรเกิดขึ้น" (se-autokey)
อาการที่กวนใจมานาน: บอทกดบันทึก ปุ่มขึ้น "Please wait..." เรียบร้อย แต่ไม่มีป๊อปอัพ
ยืนยันสักที รอ 30 วินาทีแล้วฟ้องว่ากดปุ่มไม่ติด ทั้งที่คนกดเองบนหน้าเดียวกันด้วยข้อมูล
ชุดเดิมผ่านทันที — เจอครั้งแรกตั้งแต่ ก.ค. จับต้นเหตุได้ 10 ส.ค.
- **ต้นเหตุ:** ช่องอย่าง "จังหวัด/อำเภอ/ประเภทรถ" สั่งให้ระบบประกันโหลดหน้าใหม่ทุกครั้ง
  ที่เลือก ถ้ากดบันทึกจ่อท้ายเร็วเกินไป หน้าที่โหลดใหม่จะมา**ทับคำสั่งบันทึกที่ส่งไปแล้ว**
  — งานหายเงียบ ไม่มี error ไม่มีคำเตือน
- ตอนนี้รอให้หน้านิ่งจริงก่อนกดบันทึกทุกครั้ง (เช็คว่าหน้าไม่ถูกโหลดใหม่ติดกัน 2 วินาที
  ซึ่งจับได้จริงต่างจากการเช็คว่า "โหลดเสร็จ" แบบเดิมที่ตอบว่าเสร็จทั้งที่ของใหม่กำลังจะมา)
- ถ้ายังเงียบอีกและข้อมูลครบดี **กดใหม่ให้เอง** แทนที่จะฟ้องล้มทันที
- หลักฐาน: เคส #126 เลือกจังหวัด 14:28:50 → อำเภอ 14:28:51 → กดบันทึก 14:28:52 →
  ไม่มีป๊อปอัพ; 30 วินาทีให้หลังปุ่มกลับมาเป็น "แก้ไข" กดได้ = หน้าถูกโหลดใหม่ทับจริง

### ปรับ — โหมด "เติมส่วนที่ขาด" เลิกพิมพ์ทับทั้งหน้า (se-autokey)
ชื่อโหมดบอกว่าเติมเฉพาะที่ขาด แต่ของจริงไล่พิมพ์ทับทุกช่องใหม่หมดโดยไม่เคยดูว่า
ค่าเดิมตรงอยู่แล้วหรือเปล่า — เปิด draft ที่กรอกไปแล้วมาเติมนิดเดียวก็ต้องนั่งรอ
ระบบพิมพ์ใหม่ทั้งหน้า (ทีละตัวอักษร ~60 ช่อง) เจอตอนใช้งานจริง 10 ส.ค.
- ตอนนี้อ่านค่าที่อยู่บนหน้าก่อน ตรงอยู่แล้วข้าม เขียนเฉพาะช่องที่ต่างจริง
- ตัวเลือก (dropdown) ที่ค่าตรงอยู่แล้วก็ไม่เลือกซ้ำ — เลือกซ้ำ 1 ครั้งเท่ากับ
  โหลดหน้าใหม่ทั้งหน้า เป็นตัวกินเวลาที่สุด
- **มีผลเฉพาะโหมดเติมส่วนที่ขาด** การนำเข้าครั้งแรกยังเขียนทุกช่องเหมือนเดิม
  (หน้ายังว่าง + ตัวเลือกลูกอย่าง "ยี่ห้อ/อำเภอ" ต้องรอตัวแม่สั่งให้โหลดก่อน)

### แก้บั๊ก — ลบคู่กรณีทิ้งแล้ว "คู่กรณีคันที่" ชี้ไปคันที่ไม่มีอยู่ (APK 1.0.37+39)
ผลคดี "คู่กรณีผิด" ต้องระบุว่าคันไหนผิด แต่เลขนั้นอ้างอิงลำดับคันในหมวดคู่กรณี —
ลบคันที่ 1 ทิ้ง คันที่ 2 เลื่อนขึ้นเป็นคันที่ 1 แต่เลขที่กรอกไว้ยังเป็น 2 อยู่
ค่านี้ไหลเข้าช่องของระบบประกันตรง ๆ โดยไม่มีใครดักให้
- ลบคู่กรณีแล้ว **เลขขยับตามอัตโนมัติ** (3→2 เมื่อลบคันก่อนหน้า)
- ลบ**คันที่ถูกอ้างอิงเอง** → ล้างช่องให้เลือกใหม่ ไม่เดาแทน
- หน้าตรวจก่อนส่งเตือนถ้าเลขเกินจำนวนคันที่มีจริง เช่น "คู่กรณีคันที่ = 2 แต่ในหมวด 6 มี 1 คัน"
- รูปภาพเลื่อนตามอยู่แล้วตั้งแต่เดิม (ป้าย "คันที่ 2" → "คันที่ 1")

### เพิ่ม — ติ๊ก "รอตรวจสอบ" ที่หน้าคู่กรณี (APK 1.0.36+38)
คู่กรณีหลบหนีหรือยังไม่ให้รายละเอียด แต่ช่องบังคับก็ยังต้องมีค่า ไม่งั้นบันทึกเข้า
ระบบประกันไม่ผ่าน — ติ๊กช่องเดียวบนหัวหน้าจอ ระบบเติมให้ครบทุกช่องที่บังคับ
- ช่องพิมพ์ (เจ้าของ · ทะเบียน · ชื่อ · นามสกุล · โทรศัพท์ · ที่อยู่ · บัตร ปชช.) → "รอตรวจสอบ"
- ประเภทรถ → "รถอื่นๆ" · จังหวัด → "อื่นๆ" · มีประกันภัยที่ → "อื่นๆ" ·
  เพศ → "ชาย" · คำนำหน้า → "นาย" · วันเกิด → 1 ม.ค. 2525
- **"ความสัมพันธ์กับเจ้าของรถ" ถอดจุดแดงออก** — ตรวจหน้าจอจริงแล้วไม่ใช่ช่องบังคับ
  (บังคับเฉพาะฝั่งรถประกัน) จึงไม่เติมค่าให้ด้วย ปล่อยว่างไว้ตามเดิม
- **เติมเฉพาะช่องที่ยังว่าง** ที่กรอกไว้แล้วคือของจริง ไม่ทับ · แก้ทีหลังได้ทุกช่อง ·
  เอาติ๊กออกไม่ล้างค่าคืน (ล้างแล้วของที่พิมพ์เองจะหายไปด้วย)
- **อายุคำนวณจากวันเกิดให้เอง** ทั้งตอนติ๊กและตอนเลือกวันเกิดปกติ (พิมพ์ทับได้)
- จำสถานะติ๊กไว้ เปิดคันเดิมกลับมายังติ๊กอยู่

### เพิ่ม — บังคับ 7 ช่องของคู่กรณีที่ระบบประกันเติมแทนไม่ได้ (APK 1.0.34+36)
กรอกคู่กรณีแล้วกด "บันทึกคันนี้" ผ่านฉลุยทั้งที่ **เพศ · วันเกิด · อายุ · มีประกันภัยที่**
ว่างหมด — พอถึงคิวนำเข้า ระบบประกันบล็อกการบันทึก "ทั้งบล็อกคู่กรณี" แล้วหัวหน้าต้องมา
นั่งเติมเองทุกเคส (เจอจริงกับเคลม 21BR10AVD-6908-000097 ขาดพร้อมกันทั้ง 4 ช่อง)
- ช่องข้อความที่บังคับ (เจ้าของ · ที่อยู่ · โทร · บัตร ปชช. · ใบขับขี่ · กรมธรรม์ · เลขเคลม)
  ระบบใส่ "-" แทนให้ผ่านได้ แต่ **ตัวเลือก/วันที่/ตัวเลข ใส่ "-" ไม่ได้** → ต้องมีคนกรอกจริง
- กด "บันทึกคันนี้" ทั้งที่ยังขาด → ขึ้นกล่องบอกว่าขาดช่องไหน เลือก "กลับไปกรอก" หรือ
  "บันทึกทั้งที่ยังไม่ครบ" ได้ — **ไม่บล็อกตาย** เพราะคู่กรณีหนี/ยังไม่ให้ข้อมูลก็ต้องเก็บ
  ทะเบียนกับความเสียหายไว้ก่อน (บล็อกตาย = พนักงานลบคันทิ้ง ข้อมูลที่ถ่ายมาหายหมด)
- หน้าตรวจก่อนส่งฟ้องซ้ำอีกชั้น ระบุเป็นรายคัน เช่น "คู่กรณี คันที่ 1: ขาด เพศผู้ขับขี่, อายุ"
  — เดิมหมวดคู่กรณีเป็นหมวดเดียวที่ไม่เคยตรวจรายคัน (หมวดผู้บาดเจ็บ/ทรัพย์สินมีมาก่อนแล้ว)
- ช่อง **เพศผู้ขับขี่** เพิ่งมีป้ายกำกับ + จุดแดงครั้งแรก (เดิมเป็นปุ่ม ชาย/หญิง ลอย ๆ ไม่มีป้าย)

### แก้บั๊ก — ส่งรายงานไม่ผ่านทุกครั้งที่ผลคดี = "คู่กรณีผิด" (APK 1.0.33+35)
พนักงานกรอกครบทุกหมวดแล้วกด "ส่งรายงาน" ขึ้นแดงว่า **Validation error** เฉย ๆ
ไม่บอกว่าช่องไหน — เกิดกับทุกงานที่เลือกผลคดี "คู่กรณีผิด" แล้วกรอก "คู่กรณีคันที่"
- ต้นเหตุ: ช่อง "คู่กรณีคันที่" เก็บเป็นข้อความ แต่แอปส่งเป็นตัวเลข server เลยตีกลับ
- แก้ทั้งสองฝั่ง: แอปส่งเป็นข้อความ + **server รับได้ทั้งสองแบบ** เพื่อให้เครื่องที่ยัง
  ใช้แอปเวอร์ชันเก่าส่งงานได้ทันทีโดยไม่ต้องรออัปแอป
- ข้อความ error บนแอปบอกชื่อช่องที่ไม่ผ่านแล้ว (server ส่งมาให้อยู่แล้ว แต่แอปทิ้ง)

### เพิ่ม — ติ๊ก "ไทย" ที่ช่องเลขบัตร (APK 1.0.32+34)
ทุกช่องเลขบัตร (ผู้ขับขี่รถประกัน · คู่กรณี · ผู้บาดเจ็บ) มีช่องติ๊ก **"ไทย"** อยู่แถวเดียวกับช่อง
- **ติ๊กไว้** (ค่าเริ่มต้น) — บังคับ 13 หลัก + ตรวจเลขถูกต้อง เหมือนเดิมทุกอย่าง
- **เอาติ๊กออก** — บัตรต่างด้าว/หนังสือเดินทาง พิมพ์ตัวอักษรได้ ไม่ตรวจ 13 หลัก
  (ป้ายช่องเปลี่ยนตามให้เห็นชัดว่าอยู่โหมดไหน)
- แก้ต่อ 1.0.32+34: เอาติ๊กออกแล้ว**คีย์บอร์ดยังเป็นตัวเลข พิมพ์ตัวอักษรไม่ได้**
  — ปิดคีย์บอร์ดก่อนสลับ + สร้างช่องใหม่ ให้ได้คีย์บอร์ดตรงชนิดจริง
- จำกัด 13 ตัวทั้งสองแบบ เพราะช่องรับข้อมูลของบริษัทประกันยาวได้แค่ 13 —
  พิมพ์ยาวกว่านั้นจะโดนตัดทิ้งเงียบ ๆ ตอนนำเข้า
- จำค่าที่เลือกไว้ เปิดงานเดิมกลับมาไม่ต้องเลือกใหม่ · งานเก่าถือว่าเป็นคนไทยตามเดิม
- **ต้องรัน migration 029 บน production ก่อนขึ้นเวอร์ชันนี้** (รันแล้ว)

### แก้บั๊ก — รูป "ยืนยันถึงที่เกิดเหตุ" ไปกองอยู่ "ไม่ระบุหมวด"
รูปที่แอปถ่ายให้ตอนกดปุ่มถึงที่เกิดเหตุ ไม่เคยมีป้ายหมวด เลยไปกองรวมกับรูป
ไม่ระบุหมวดบนหน้าเคส ดูเหมือนรูปหลงมา
- ตอนส่งงานจะติดป้าย **"รูปยืนยันถึงที่เกิดเหตุ"** ให้เองตั้งแต่บันทึก
- เคสเก่า 4 เคส (104, 105, 120, 125) ใส่ป้ายย้อนหลังให้แล้ว
- ไม่กระทบการนำเข้า EMCS — รูปนี้ถูกกรองทิ้งด้วยชื่อไฟล์อยู่แล้ว ไม่ได้ดูหมวด

### แก้บั๊ก — การ์ดลอยแจ้งงานใหม่ไม่เคยโผล่เลย (APK 1.0.29+31)
เปิดแอปทีไรก็มี error ซ่อนอยู่ใน log ว่าเปิดหน้าต่างลอยไม่ได้ ("หาโปรแกรมเริ่มต้น
ของหน้าต่างลอยไม่เจอ") — การ์ดที่ควรเด้งทับแอปอื่นตอนมีงานใหม่จึงไม่เคยขึ้นสักครั้ง
- ต้นเหตุ: โค้ดเริ่มต้นของหน้าต่างลอยไปอยู่ผิดไฟล์ ตัวปลั๊กอินหาไม่เจอ
- **การแจ้งเตือน + เสียง ยังทำงานปกติมาตลอด** (คนละกลไกกัน) ที่หายคือการ์ดลอยอย่างเดียว
- ต้องติดตั้ง APK ใหม่ทับถึงจะได้ — push อย่างเดียวไม่พอ

### แก้บั๊ก — นำเข้า XML แล้วเบอร์โทรผู้สำรวจภัยหายทุกครั้ง
เจอตอนลองวงจร EMCS → XML → ระบบเรา: เราส่ง `ACC_TEL` ออกไปตั้งแต่ต้นเดือน
แต่ฝั่งนำเข้าไม่เคยอ่าน tag นี้กลับเลย เบอร์จึงหายทุกไฟล์ ทั้งที่ EMCS บังคับช่องนี้
- ไฟล์เก่าจาก ISURVEY ที่ไม่มี tag นี้ยังนำเข้าได้ปกติ (ได้ค่าว่าง ไม่ error)

### แก้บั๊ก — บอทส่งเลขผิดรูปแบบ EMCS ตีกลับตอนกดบันทึก (เจอด้วยเครื่องมือใหม่)
EMCS มีชั้นตรวจที่เราไม่เคยรู้: `validForm()` ผูกกับปุ่มบันทึกหน้าหลัก บังคับ
**รูปแบบข้อความแยกตามบริษัทประกัน** 4 ช่อง (เลขที่รับแจ้ง 13 บริษัท · เลขที่เคลม 11 ·
กรมธรรม์ 7 · พ.ร.บ. 1) รูปแบบไม่ตรง = บันทึกไม่ผ่าน แม้ข้อมูลจะถูกต้อง
- เดิมบอทเช็คแค่ "มีเครื่องหมาย /" — เลขอย่าง `BR10/6905/12524` ผ่านด่านเรา
  แต่ไอโออิรับแค่ `HY2010/000001` · `ABI12345/0753` · `ABCSR0001/01/53`
- ตอนนี้เทียบแพตเทิร์นจริงรายบริษัท และเตือนล่วงหน้าว่าช่องไหนจะทำให้บันทึกไม่ผ่าน
- **พ.ร.บ.** — แก้ความเข้าใจผิด: ไม่ได้บังคับทุกบริษัท แต่บังคับเฉพาะเมื่อติ๊ก
  "มี พ.ร.บ." เท่านั้น → เลิกยัด `-` ให้เคสที่ไม่มี พ.ร.บ. (ตรงกับดีไซน์แอปมือถือพอดี)

### แอปมือถือ 1.0.27+29
- **ตัวนับโควตารูป** — หน้ารูปภาพแสดง "จำนวนรูปภาพ (52 / 80)" พร้อมป้ายเตือน
  เหลืองเมื่อถึง 60 ใบ · แดงเมื่อเกิน 80 (โควตาของ EMCS ต่อเคลม)
  **ไม่บล็อกการถ่าย** — ระบบเราเก็บรูปได้ไม่จำกัด และบล็อกแล้วช่างเสียหลักฐานถาวร
  ส่วนบอทตัดให้พอดีโควตาจริงตอนอัปพร้อมบอกว่าตกไปกี่ใบ
- **ผู้บาดเจ็บ: เลขทะเบียน** บังคับแบบมีเงื่อนไข — EMCS บังคับทุกประเภท ยกเว้น
  "บุคคลภายนอกรถ" (คนเดินถนนไม่มีทะเบียน) เดิมแอปไม่บังคับเลย

### แอปมือถือ 1.0.26+28
- **โทรศัพท์สำรวจ** ในหมวด 5 ใส่จุดแดง (บังคับ) + นับเข้าตัวนับ — EMCS บังคับช่องนี้

### แก้บั๊ก — XML ที่ส่ง EMCS ตกช่องบังคับ 3 ช่อง
เจอตอนกรอกงานจริงจนจบแล้วเปิด XML ดู (เคส #120):
- **ระดับความเสียหาย หนัก/เบา** ที่ช่างเลือก ไม่เคยไปถึง EMCS — `HEV_CAR` ถูก hardcode ว่าง
- **ลักษณะความเสียหาย** (ชนคู่กรณีเสียหาย ฯลฯ) เหมือนกัน — `LOSS_ID` hardcode ว่าง เพิ่มตารางแปลง 21 รหัส
- **โทรศัพท์ผู้สำรวจภัย** ไม่มี tag `ACC_TEL` ใน XML เลย ทั้งที่ EMCS บังคับ

### กติกาที่ยืนยันแล้ว — ใครเป็นเจ้าของข้อมูลช่องไหน
3 ช่องความเห็นบนหน้า "ค่าใช้จ่าย" ของ EMCS (ผลการดำเนินงาน · ความเห็นผู้ตรวจสอบ ·
ความเห็นเซอร์เวย์) **หัวหน้ากรอกเองทั้งหมด** ระบบเราไม่ส่งไปทับ — สิ่งที่ผู้สำรวจเขียน
คือ "รายละเอียดการเกิดเหตุ" ซึ่งเป็นคนละช่อง (ไปที่ `ACC_DETAIL` หน้าข้อมูลทั่วไป)

### แอปมือถือ 1.0.25+27 — แก้ 4 จุดที่เจอจากการทดสอบกรอกงานจริง
- **ตัวนับ "ครบ N/5 หมวด" โกหก** — หมวด 3 ผู้ขับขี่ ตรวจแค่ 4 ช่องจาก 11 ช่องที่มีจุดแดง
  กรอกแค่ ชื่อ/โทร/บัตร ก็ขึ้น "ครบ" แล้ว ทั้งที่ **ที่อยู่ปัจจุบัน** กับ **จังหวัด** ยังว่าง
  → ช่างส่งงาน แล้วหัวหน้าเปิด EMCS บันทึกไม่ผ่าน ตอนนี้ตรวจครบทั้ง 11 ช่อง
- **เขต/อำเภอ ผู้ขับขี่** ไม่เคยมีจุดแดง ทั้งที่ EMCS บังคับ → ใส่จุดแดง + นับเข้าตัวนับ
- **ฝ่ายประมาท** ไม่เคยมีจุดแดง ทั้งที่ EMCS บังคับ → ใส่จุดแดง + นับเข้าตัวนับ
- **ชิ้นส่วนที่ไม่มีด้าน** (กันชนหน้า/หลังคา/กระบะ ฯลฯ 12 ชิ้น) เดิมเลือก ซ้าย/ขวา ได้
  ทั้งที่ EMCS ไม่มีปุ่มให้ลง → ซ่อนตัวเลือกด้าน บังคับ "ทั้งหมด" ให้ตรงกับหน้าเว็บ

### แอปมือถือ 1.0.24+26
- หน้า **คู่กรณี** ตรวจทุกช่องเทียบฟอร์ม EMCS จริงแล้ว — ตรงครบ
- เปลี่ยนชื่อช่อง "วันออกบัตร" → **"ออกให้วันที่"** ให้ตรง EMCS
- ถอดช่อง "จังหวัดตามบัตร ปชช." ออก (EMCS ซ่อนช่องนี้ในบล็อกคู่กรณี)

### แก้บั๊ก
- **แบนเนอร์ "เคลมคู่" ขึ้นมั่ว** — เลขเคลมสั้นไปจับคู่กับเลขบัตรประชาชนของเคสอื่น ตอนนี้ตรวจเฉพาะเลขที่ยาวพอเป็นเลขเคลมจริง
- ไม่บังคับกรอก "เลข กม." กับ "ออกให้วันที่" — ดอกจันแดงบน EMCS สองช่องนี้ไม่ได้บังคับจริง

---

## กรกฎาคม 2569

### ⭐ นำเข้างานเก่าจาก ISURVEY (flow ที่ 2)
สำหรับงานที่ยังทำบนระบบเดิม — อัปโหลดไฟล์ XML แล้วเข้าสู่ระบบเราได้เลย

- หน้า **"นำเข้าจากไฟล์ XML"** — อัปโหลด XML + zip รูป เลือกบริษัทประกัน
- สร้างเคสอัตโนมัติ · จ่ายงานตามรหัสผู้สำรวจในไฟล์ · แตกรูปแยก 4 หมวด
- แจ้งเตือนสิ่งที่ไฟล์ไม่มีให้ผู้ตรวจกรอกเอง (ความเสียหาย · หนัก/เบา · ประเภทเคลม)
- **หน้าตรวจงานแก้ได้ทุกส่วน** — ความเสียหาย 22 ชิ้น · ผู้บาดเจ็บ 19 ช่อง · ทรัพย์สิน 7 ช่อง
- ยอดเงินจากระบบเก่าส่งต่อเข้า EMCS ได้ (งานจากแอปส่ง 0 เพราะแอปไม่มีหน้าค่าใช้จ่าย)

### ⭐ สะพานเชื่อม EMCS (se-autokey)
- ปุ่ม **"นำเข้า EMCS"** บนหน้าตรวจงาน — ส่งงานให้บอทกรอกฟอร์มแทนการคีย์มือ
- endpoint สำหรับบอท: รายการเคส · ข้อมูลเคส · รูป · บริษัทประกัน
- สถานะ "นำเข้า EMCS แล้ว" กันเลขเคลมเดิมถูก import ซ้ำ
- ปุ่มดาวน์โหลด XML สำรอง (กรณีบอทรันไม่ได้)

### ⭐ ปรับทุกช่องให้ตรง EMCS
- ตัวเลือกทั้งหมด sync จาก master จริงของ EMCS — ยี่ห้อรถ **350 ตัวแยกตามประเภทรถ** · สี 55 · ความสัมพันธ์ 40 · ประเภทใบขับขี่ 21 · ผู้บาดเจ็บ 5 · ระดับบาดเจ็บ 6
- **ลักษณะการเกิดเหตุครบ 79 ตัว** (เดิมแปลงรหัสได้แค่ 26 → 53 สาเหตุที่เลือกได้ส่งออกเป็นค่าว่าง)
- ชื่อชิ้นส่วนความเสียหายตรง checklist 22 ชิ้นของ EMCS แบบคำต่อคำ
- แปลงหน่วยให้ EMCS รับ: ปี พ.ศ.→ค.ศ. · เบอร์โทร · จำนวนเงิน · เพศเป็นรหัส M/F
- เขต/อำเภอ cascade ตามจังหวัด (เดิมโชว์ 51 เขต กทม. กับทุกจังหวัด)

### ⭐ ยกเครื่องหน้ากรอกข้อมูลสำรวจ (Hub 8 หมวด)
- เปลี่ยนจากฟอร์มยาวเป็น **Hub 6+ หมวด** กดเข้าทีละหมวด
- คู่กรณี / ผู้บาดเจ็บ / ทรัพย์สิน เพิ่มได้หลายรายการ (สูงสุด 20/32/30)
- **แผนภาพความเสียหาย** แตะเลือกชิ้นส่วน + ระดับ
- **OCR สแกนบัตรประชาชน + ใบขับขี่** — ถ่าย 1 ครั้งได้ทั้งรูปและข้อมูล พร้อมธงเตือนเมื่อ OCR ไม่มั่นใจ
- กล้องในแอป — ถ่ายรัว · สลับกล้อง · เลือกความละเอียด · เลือกหมวดก่อนถ่าย · เพิ่มจากคลังภาพได้
- autosave ทุกช่อง + คิวออฟไลน์ + กันกดส่งซ้ำ
- ตรวจความครบถ้วน 3 ชั้น + หน้าสรุปก่อนส่ง

### แก้บั๊กสำคัญ
- **ความปลอดภัย** — ปิด `/uploads` ต้องยืนยันตัวตน · จำกัดจำนวนครั้ง login · กัน path traversal · ยกเลิก session ได้
- ข้อมูลฟอร์มหายหลังปิด-เปิดแอป (รายการความเสียหาย · ชื่อผู้ขับขี่ · ค่าที่ล้าง)
- placeholder "-- ระบุ --" หลุดเป็นค่าจริงเข้า DB → รหัสจังหวัดใน XML ว่าง
- กันข้อมูล **"เคลมคู่"** (อุบัติเหตุเดียวกัน คนละเคลม) ปนกัน — แบนเนอร์เตือนบนแอป
- **เลขเซอร์เวย์ห้ามซ้ำ** (เป็นเลขอ้างอิงเบิกเงิน) ส่วนเลขเคลมซ้ำได้
- รูปถ่ายผูกกับเคสถูกใบ + อัปโหลดทีละไฟล์แบบ resume ได้

---

## มิถุนายน 2569 — ระบบงานบุคคล/ปฏิบัติการ + ขึ้น production

### ⭐ ขึ้นใช้งานจริง
- deploy บน VPS ผ่าน Dokploy — `survey.sesurvey.cloud` + `api.sesurvey.cloud`
- แอปมือถือชี้ production + เซ็น APK ด้วย release key แจกพนักงาน

### ⭐ ลงเวลาเข้า-ออกงาน + ลางาน
- ลงเวลาพร้อม **ถ่ายรูป + พิกัด GPS** · ลงได้หลายรอบ/วัน · logout = ลงเวลาออกอัตโนมัติ
- ปิดรอบค้างข้ามวันอัตโนมัติ · ใช้เวลาไทย (Asia/Bangkok) ทั้งระบบ
- ระบบใบลา — ยื่นจากแอป อนุมัติโดย admin
- หน้าเว็บ: ตารางเวลาเข้า-ออกรายเดือน · ค้นหา · สรุป · ส่งออก CSV

### ⭐ ตารางเวรประจำจุด + บอร์ดเข้างาน
- ตารางเวรจากไฟล์ Excel จริง · เวร 1/2/3 + FIX (FIX8/FIX10/FIX14) + เวรหยุด
- **เดือนใหม่หมุนเวรต่อจากเดือนก่อนอัตโนมัติ** (ตั้งไว้ถึง ก.ค. 2570)
- บอร์ด "เวลาเข้างานพนักงาน" — การ์ดรายจุด แยกในระบบ/นอกระบบ/หยุด-ลา · เรียงคิวความสำคัญ · อาสาสมัคร
- ครอบคลุมทั้ง กรุงเทพ · ปริมณฑล · **ต่างจังหวัด 17 จังหวัด**
- บังคับ 1 คน 1 รหัส SE + เตือนเมื่อรหัสซ้ำในตารางเวร

### อื่น ๆ
- **OCR ใบรับแจ้งเคลม** — เปลี่ยนจาก Typhoon เป็น Gemini + Vision ดึง 5 เลขสำคัญอัตโนมัติ
- หน้าสร้างเคสใช้รูปใบเคลมทั้งใบ + รวมขั้นมอบหมายช่างไว้หน้าเดียว
- รายงานการโทรปรึกษาหัวหน้า (เลือกช่วงวันที่ได้)
- แผนที่พิกัดพนักงาน — เรียกพิกัดสดผ่าน FCM
- รองรับสถานะ "ปฏิเสธงาน"

---

## เมษายน 2569 — ระบบแจ้งเตือนงานใหม่

- **แจ้งเตือนแบบสายเรียกเข้า** — เต็มจอ + เสียงเตือนวนจนกว่าจะกดรับ
- ทำงานได้ทุกสถานะของแอป (เปิด/ปิด/ถูก kill) ผ่าน FCM + service ฝั่ง Android
- เลือกรูปแบบตามสถานะหน้าจอ (เต็มจอ / แถบแจ้งเตือน) + ปุ่มปิดเสียง
- ยืนยันถึงที่เกิดเหตุด้วยการถ่ายรูป + เก็บรูปลงโฟลเดอร์ประจำเคส
- ปฏิเสธงานได้ + รายการงานรีเฟรชอัตโนมัติ

---

## มีนาคม 2569 — วางรากฐาน

- โครงระบบ 3 ส่วน: มือถือ · เว็บ · backend + PostgreSQL
- ระบบผู้ใช้ 4 บทบาท: ผู้สำรวจ · call center · ผู้ตรวจ · admin
- สร้างเคส → มอบหมาย → สำรวจ → ตรวจ → อนุมัติ
- หน้ารายละเอียดเคสบนเว็บ ออกแบบตามฟอร์มประกันจริง + แก้ไขได้
- อัลบั้มรูป (ซูม · เลื่อน · thumbnail)
- ตารางค่าใช้จ่าย
- แผนที่ตำแหน่งพนักงาน (Leaflet/OpenStreetMap)
- OCR ใบเคลมรุ่นแรก + date picker แบบ พ.ศ.

---

## หมายเหตุการดูแลระบบ

- **migration รันด้วยมือบน production** — ล่าสุดคือ `028_case_source.sql`
- `DATABASE_URL` ในเครื่องพัฒนาชี้ฐานข้อมูลเดียวกับ production — ระวังตอนทดสอบ
- push ขึ้น `main` → Dokploy deploy อัตโนมัติ (backend กับ web แยกกัน)
- แอปมือถือต้อง build APK ใหม่ + ติดตั้งทับทุกเครื่อง โค้ดที่ push ไม่ถึงมือถือเอง
